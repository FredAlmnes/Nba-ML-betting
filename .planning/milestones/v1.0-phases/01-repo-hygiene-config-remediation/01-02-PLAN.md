---
phase: 01-repo-hygiene-config-remediation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [.gitignore, modell_utils.py]
autonomous: true
requirements: [HYG-02]
user_setup: []

must_haves:
  truths:
    - "A fresh git clone of this repo contains modell_utils.py"
    - "A fresh clone can run `from modell_utils import KalibrertModell` without ModuleNotFoundError, so nba_modell.pkl can be unpickled"
    - "The local pip/build scratch artifacts no longer appear as untracked files in git status, so they can never be swept into a commit"
    - "D-04: modell_utils.py is git-added and committed as-is, no code changes, matching CONTEXT.md's decision that the file is already correct"
  artifacts:
    - path: "modell_utils.py"
      provides: "KalibrertModell wrapper class required to unpickle nba_modell.pkl"
      contains: "class KalibrertModell"
    - path: ".gitignore"
      provides: "Exclusion patterns for local pip/build scratch artifacts"
      contains: "_wheels/"
  key_links:
    - from: "03_tren_modell.py"
      to: "modell_utils.KalibrertModell"
      via: "import"
      pattern: "from modell_utils import"
    - from: "04_value_detector.py"
      to: "modell_utils.KalibrertModell"
      via: "import"
      pattern: "from modell_utils import"
---

<objective>
Close HYG-02: make `modell_utils.py` tracked in git so a fresh clone can unpickle `nba_modell.pkl`, and harden `.gitignore` against the local pip/build scratch artifacts first so they can never be accidentally staged by this or any later plan.

Purpose: `modell_utils.py` defines `KalibrertModell`, which pickle needs by name at load time. It is currently untracked (`git ls-files` returns nothing for it), so a fresh clone breaks with `ModuleNotFoundError` on both `03_tren_modell.py` and `04_value_detector.py`. Per D-04 this needs no code change — only git tracking.
Output: `.gitignore` with scratch patterns; `modell_utils.py` committed; fresh-clone import proven working.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-repo-hygiene-config-remediation/01-CONTEXT.md
@.planning/phases/01-repo-hygiene-config-remediation/01-RESEARCH.md
@.planning/phases/01-repo-hygiene-config-remediation/01-01-SUMMARY.md
@.gitignore
@modell_utils.py
</context>

<staging_rule>
**NEVER run `git add -A` or `git add .` in this phase.** The working tree contains a ~100MB `_test.bin`, three pip-scratch directories, and ~1200 lines of uncommitted changes in scripts this plan must not touch. Stage by explicit filename only, and run `git status --short` between staging and committing to confirm exactly the intended paths are staged.
</staging_rule>

<tasks>

<task type="auto">
  <name>Task 1: Add scratch-artifact exclusion patterns to .gitignore</name>
  <files>.gitignore</files>
  <read_first>
    - `.gitignore` (26 lines, full file — note the existing section-comment style: a `# Kommentar` header line above each group of patterns, and the existing `.env` entry on line 7)
    - `.planning/phases/01-repo-hygiene-config-remediation/01-RESEARCH.md` section "`.gitignore` additions for scratch artifacts (D-08)" and Open Question 2
    - `.planning/phases/01-repo-hygiene-config-remediation/01-01-SUMMARY.md` — the "Developer decisions" section, for the scratch-artifact disposition
  </read_first>
  <action>
    Append a new section to the end of `.gitignore`, matching the file's existing Norwegian section-comment style. The section header comment is `# Lokale build/pip-scratch-artefakter (ikke del av prosjektet)` and the six patterns beneath it, one per line, are: `_linux_pkgs/`, `_pip_tmp/`, `_pip_home/`, `_wheels/`, `_test.bin`, `test_write.tmp`.

    `_pip_home/` is included per RESEARCH.md Open Question 2 — it is the same pip-scratch naming family as its three siblings and is currently an empty directory, so it is invisible to `git status` but would surface later if repopulated. Adopt this recommendation.

    Do NOT add patterns for `debug_kamp.py` (D-09: intentional manual debug utility, out of scope, leave untracked as-is), `ENDRINGER_SUMMARY.txt` or `KALIBRERING_RAPPORT.md` (plan 04 commits those deliberately). Do NOT modify or reorder any existing line in `.gitignore` — append only.

    Then read the scratch-artifact disposition recorded in `01-01-SUMMARY.md`. If it is `ignore-only`, delete nothing. If and only if it is `ignore-and-delete`, remove `_linux_pkgs/`, `_pip_tmp/`, `_wheels/`, `_test.bin` and `test_write.tmp` from disk, and record in the SUMMARY how much space was reclaimed. If the SUMMARY does not contain an unambiguous disposition, default to `ignore-only` and note the ambiguity — never delete on a guess (D-08).
  </action>
  <verify>
    <automated>for p in '_linux_pkgs/' '_pip_tmp/' '_pip_home/' '_wheels/' '_test.bin' 'test_write.tmp'; do grep -v '^#' .gitignore | grep -qxF "$p" || echo "MANGLER: $p"; done; echo "---"; git status --porcelain | grep -E '_test\.bin|_wheels|_pip_tmp|_linux_pkgs|test_write\.tmp' || echo "INGEN SCRATCH I GIT STATUS"</automated>
  </verify>
  <acceptance_criteria>
    - First command block prints no `MANGLER:` lines (all six patterns present as exact non-comment lines)
    - Second command block prints `INGEN SCRATCH I GIT STATUS` (the artifacts are now ignored, not untracked)
    - `git diff -- .gitignore` shows only appended lines — zero deletions and zero modifications to lines 1-26
    - `git check-ignore -v modell_utils.py` still exits non-zero (the new patterns did not accidentally capture it)
    - `git check-ignore -v debug_kamp.py` exits non-zero (D-09: not ignored, just untracked)
  </acceptance_criteria>
  <done>`.gitignore` ends with the six scratch patterns under a Norwegian section comment; the scratch artifacts no longer appear in `git status --short`; existing entries untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Track modell_utils.py in git and prove a fresh clone can import it</name>
  <files>modell_utils.py, .gitignore</files>
  <read_first>
    - `modell_utils.py` (full file — confirm it defines `class KalibrertModell` with `predict_proba`, and that no code change is needed per D-04)
    - `.planning/phases/01-repo-hygiene-config-remediation/01-RESEARCH.md` "Pattern 3: Safe `git add` of a previously-untracked, already-correct file"
  </read_first>
  <action>
    Make no code change to `modell_utils.py` — D-04 states it is already correct, only untracked.

    First confirm it is not ignored: run `git check-ignore -v modell_utils.py` and require no output. Then stage exactly two paths by explicit filename: `git add modell_utils.py .gitignore`. Then run `git status --short` and confirm the staged set is exactly `A  modell_utils.py` and `M  .gitignore` and nothing else — if any other path shows as staged, unstage it with `git restore --staged <path>` before continuing. Commit with message `fix(01-02): track modell_utils.py so a fresh clone can unpickle nba_modell.pkl`.

    Then prove the fresh-clone behaviour empirically rather than assuming it: clone the repository into a temp directory, and from inside that clone run the `KalibrertModell` import using this repo's existing venv interpreter (the clone has no venv of its own — `venv/` is gitignored — but the venv provides numpy, which `modell_utils.py` imports). Clean up the temp directory afterwards.

    Do not run `git add -A`, `git add .`, or `git commit -a` at any point.
  </action>
  <verify>
    <automated>git ls-files --error-unmatch modell_utils.py && REPO=$(pwd) && TMP=$(mktemp -d) && git clone -q "$REPO" "$TMP/klone" && cd "$TMP/klone" && "$REPO/venv/bin/python" -c "from modell_utils import KalibrertModell; print('IMPORT OK:', KalibrertModell.__name__)" && cd "$REPO" && rm -rf "$TMP"</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files --error-unmatch modell_utils.py` exits 0 (file is tracked)
    - The temp-clone command prints `IMPORT OK: KalibrertModell` — proving HYG-02's success criterion "a fresh `git clone` can load `nba_modell.pkl` without an ImportError"
    - `git show --stat HEAD` lists exactly two paths: `modell_utils.py` and `.gitignore`
    - `git show --stat HEAD` does NOT list `_test.bin`, `_wheels`, `_pip_tmp`, `_linux_pkgs`, `04_value_detector.py`, `05_skadefilter.py`, `06_bot.py` or `03_tren_modell.py`
    - `git diff HEAD~1 HEAD -- modell_utils.py | grep -c '^-[^-]'` returns 0 (file added verbatim, no content altered)
  </acceptance_criteria>
  <done>`modell_utils.py` is tracked and committed alongside the `.gitignore` hardening; a temp clone of HEAD imports `KalibrertModell` successfully; the commit contains exactly the two intended files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git history (public repo) | This plan performs the phase's first `git commit`. Anything staged becomes permanently public. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure | `git commit` in a tree still containing the hardcoded key at `04_value_detector.py:30` | mitigate | Explicit two-path staging (`git add modell_utils.py .gitignore`); mandatory `git status --short` inspection between staging and commit; acceptance criteria assert `git show --stat HEAD` does not list `04_value_detector.py` |
| T-01-04 | Information Disclosure | 100MB `_test.bin` and pip-scratch dirs pushed to a public repo | mitigate | `.gitignore` patterns added in Task 1 *before* the first commit in Task 2, so the artifacts are ignored rather than merely untracked at commit time |
| T-01-05 | Tampering | `modell_utils.py` content silently altered while being staged | mitigate | Acceptance criterion asserts zero removed lines in the commit diff for the file (`grep -c '^-[^-]'` returns 0) |
</threat_model>

<verification>
- All six scratch patterns present in `.gitignore` as exact non-comment lines
- `git status --porcelain` shows no scratch artifacts
- `git ls-files --error-unmatch modell_utils.py` exits 0
- Temp-clone import prints `IMPORT OK: KalibrertModell`
- `git show --stat HEAD` lists exactly `modell_utils.py` and `.gitignore`
</verification>

<success_criteria>
HYG-02 is closed: a fresh `git clone` of this repo contains `modell_utils.py`, and `from modell_utils import KalibrertModell` succeeds inside that clone — proven, not assumed. The local scratch artifacts are excluded from git and can no longer contaminate any subsequent commit in this phase.
</success_criteria>

<output>
Create `.planning/phases/01-repo-hygiene-config-remediation/01-02-SUMMARY.md` when done. Record the temp-clone import output verbatim as HYG-02 evidence, and the scratch-artifact disposition that was actually executed.
</output>
