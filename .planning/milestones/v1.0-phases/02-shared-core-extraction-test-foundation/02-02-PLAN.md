---
phase: 02-shared-core-extraction-test-foundation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - requirements-dev.txt
  - pytest.ini
  - tests/conftest.py
  - tests/test_oppsett.py
  - tests/test_strategy.py
  - config.py
  - 04_value_detector.py
  - 06_bot.py
autonomous: true
requirements: [CORE-02, CORE-03]
user_setup: []

must_haves:
  truths:
    - "D-10: pytest is installed and a tests/ directory at repo root is collected and executed by pytest with a non-zero test count"
    - "D-01: config.py is created as a flat module at the repo root, not inside a new nba_betting/ package directory"
    - "D-02: modell_utils.py is not touched, renamed or re-exported by this plan"
    - "D-07: MIN_VALUE_TERSKEL, MIN_ODDS, MAX_ODDS, KELLY_FRAKSJON, MAX_INNSATS, MIN_INNSATS and STARTKAPITAL keep their exact current values (0.05 / 1.50 / 4.00 / 0.5 / 150.0 / 20.0 / 1000.0) — extraction only, no value changes"
    - "There is exactly one place in the repo where each strategy constant is assigned a literal value, and both 04_value_detector.py and 06_bot.py import it from there"
    - "The Odds API key stays in 04_value_detector.py's env-var load and is NOT moved into config.py"
  artifacts:
    - path: "pytest.ini"
      provides: "pytest discovery config enabling repo-root imports from tests/"
      contains: "pythonpath"
    - path: "requirements-dev.txt"
      provides: "pytest declared as a dev-only dependency, separate from runtime requirements.txt"
      contains: "pytest"
    - path: "tests/conftest.py"
      provides: "Shared synthetic raw-games DataFrame fixtures used by plan 06's feature and parity tests"
      min_lines: 40
    - path: "config.py"
      provides: "Single source of truth for strategy constants"
      contains: "MIN_VALUE_TERSKEL"
  key_links:
    - from: "04_value_detector.py"
      to: "config.py"
      via: "from config import MIN_VALUE_TERSKEL, MIN_ODDS, MAX_ODDS"
      pattern: "from config import"
    - from: "06_bot.py"
      to: "config.py"
      via: "from config import KELLY_FRAKSJON, MAX_INNSATS, MIN_INNSATS, STARTKAPITAL"
      pattern: "from config import"
---

<objective>
Stand up the repo's first automated test harness and collapse the strategy constants into a single source of truth. Today `MIN_VALUE_TERSKEL`/`MIN_ODDS`/`MAX_ODDS` are literals in `04_value_detector.py:43-45` and `KELLY_FRAKSJON`/`MAX_INNSATS`/`MIN_INNSATS`/`STARTKAPITAL` are literals in `06_bot.py:33-36` — two places a future backtest could disagree with what is actually deployed (CORE-02, and the exact failure mode `KALIBRERING_RAPPORT.md` already caused once).

Purpose: every later plan in this phase verifies itself with `pytest`, so the harness must exist first; and `strategy.py` (plan 03) needs `config.py` to already be the single source of the values its call sites pass in.
Output: `pytest.ini`, `requirements-dev.txt`, `tests/` with a working conftest fixture set, `config.py`, and `04`/`06` importing their constants from it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-shared-core-extraction-test-foundation/02-CONTEXT.md
@.planning/phases/02-shared-core-extraction-test-foundation/02-RESEARCH.md
@.planning/phases/02-shared-core-extraction-test-foundation/02-VALIDATION.md
@.planning/phases/02-shared-core-extraction-test-foundation/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Exact current constant declarations the executor must relocate. Do not re-derive these — they are copied verbatim from the current source this plan was written against. -->

04_value_detector.py:43-45
  MIN_VALUE_TERSKEL = 0.05            # Flagg bets der vi er 5%+ over bookmaker
  MIN_ODDS = 1.50                     # Ikke bett på favoritter med veldig lave odds
  MAX_ODDS = 4.00                     # Ikke bett på store outsidere (over 4x = for usikkert)

06_bot.py:33-36
  STARTKAPITAL      = 1000.0   # kr
  KELLY_FRAKSJON    = 0.5      # Halvt Kelly (konservativt – reduserer varians)
  MAX_INNSATS       = 150.0    # Aldri mer enn 150 kr på ett bet
  MIN_INNSATS       = 20.0     # Aldri mindre enn 20 kr

Raw-games CSV column names (from 02_feature_engineering.py's kolonne_map, needed for the conftest fixture):
  GAME_ID, GAME_DATE_HJEMME, HJEMME_VANT,
  TEAM_ID_HJEMME, PTS_HJEMME, FG_PCT_HJEMME, FT_PCT_HJEMME, FG3_PCT_HJEMME,
  REB_HJEMME, AST_HJEMME, TOV_HJEMME, PLUS_MINUS_HJEMME,
  TEAM_ID_BORTE, PTS_BORTE, FG_PCT_BORTE, FT_PCT_BORTE, FG3_PCT_BORTE,
  REB_BORTE, AST_BORTE, TOV_BORTE, PLUS_MINUS_BORTE
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install pytest and create the test harness (pytest.ini, requirements-dev.txt, tests/ with shared fixtures)</name>
  <files>requirements-dev.txt, pytest.ini, tests/conftest.py, tests/test_oppsett.py</files>
  <read_first>
    - `.planning/phases/02-shared-core-extraction-test-foundation/02-RESEARCH.md` sections "Package Legitimacy Audit", "Pattern 5: pytest discovery for a flat, package-less repo", "Pitfall 2"
    - `requirements.txt` (to confirm it stays untouched — runtime deps only)
    - `.gitignore` (to confirm nothing in `tests/` is accidentally ignored)
    - `02_feature_engineering.py` lines 35-97 (the raw column names the conftest fixture must reproduce)
  </read_first>
  <action>
    Install and configure pytest, then create the shared fixture module.

    1. Create `requirements-dev.txt` at repo root containing a single dependency line `pytest>=9.1.1` plus a one-line Norwegian comment header explaining that this file holds dev-only dependencies and that `requirements.txt` stays runtime-only. Do NOT add pytest to `requirements.txt`.

    2. Install it with `./venv/bin/python -m pip install -r requirements-dev.txt`. Use `./venv/bin/python -m pip`, not a bare `pip` — bare `pip` is not reliably on PATH in this environment. pytest is disposition "Approved" in RESEARCH.md's Package Legitimacy Audit (PyPI, ~20 yrs, source `github.com/pytest-dev/pytest`, slopcheck `[OK]`), so no human legitimacy checkpoint is required.

    3. Create `pytest.ini` at repo root with section `[pytest]` and exactly two options: `pythonpath = .` and `testpaths = tests`. This is what lets `tests/test_strategy.py` do `from strategy import ...` for a module that lives at repo root, in a repo with zero `__init__.py` files. Do NOT create a `pyproject.toml` (D-01 defers packaging) and do NOT add `sys.path.insert` hacks to any test file.

    4. Create `tests/conftest.py` with a Norwegian module docstring and three pytest fixtures. All values must be deterministic — no `random`, no `datetime.now()`:
       - `LAG_IDER` — module-level list of four fake team ids: 1610612737, 1610612738, 1610612739, 1610612740.
       - Fixture `kamper_df` — a pandas DataFrame of 10 synthetic raw games with exactly the 21 column names listed in the `<interfaces>` block above. Build it by iterating `i` from 0 to 9. `GAME_ID` is the string `f"00{22400000 + i}"`. `GAME_DATE_HJEMME` is `pd.Timestamp("2024-11-01") + pd.Timedelta(days=3 * i)` (so dates run Nov 1 through Nov 28, all strictly before the as_of used in tests). Home/away team indices come from the fixed pairing cycle `[(0,1), (2,3), (1,2), (3,0), (0,2), (1,3), (0,1), (2,3), (1,2), (3,0)]` indexed by `i`, mapped through `LAG_IDER` — this gives every team exactly 5 games, which is enough for the `min_periods=3` rolling window to produce non-NaN values after `shift(1)`. Stat values are simple deterministic formulas of `i` and the team index, e.g. `PTS = 100 + i + 2 * lag_indeks`, `REB = 40 + lag_indeks`, `AST = 20 + i % 4`, `TOV = 12 + lag_indeks`, `PLUS_MINUS = i - 5 + lag_indeks`, and the three percentage columns as `0.44 + 0.01 * lag_indeks`, `0.78 + 0.01 * lag_indeks`, `0.35 + 0.01 * lag_indeks`. `HJEMME_VANT` is `i % 2`. Return the frame sorted by `GAME_DATE_HJEMME` with a reset index, matching how `02_feature_engineering.py` sorts before calling.
       - Fixture `fremtidige_kamper_df` — a DataFrame with exactly 2 rows in the same 21-column schema, built the same deterministic way: one row dated exactly `pd.Timestamp("2024-12-01")` (the as_of boundary row, which must be excluded by a strictly-less-than filter) and one dated `pd.Timestamp("2024-12-04")` (strictly future). Use `GAME_ID` values `"0022400010"` and `"0022400011"` so they never collide with `kamper_df`. Pair them as `(0,1)` and `(2,3)`.
       - Fixture `as_of_dato` — returns the string `"2024-12-01"`.
       Plan 06's `test_features.py` and `test_parity.py` consume these; do not duplicate fixture construction there.

    5. Create `tests/test_oppsett.py` with one test, `test_repo_rot_er_importerbar`, that does `import modell_utils` inside the test body and asserts `hasattr(modell_utils, "KalibrertModell")`. This proves `pythonpath = .` actually works before any real test depends on it. It reads `modell_utils.py` only — it does not modify it (D-02).

    6. Run `./venv/bin/python -m pytest -v` and read the printed test count. Per Pitfall 2, a zero-collected run also exits 0; confirm the count is greater than zero by eye as well as by exit code.

    7. Stage exactly these paths and commit: `git add requirements-dev.txt pytest.ini tests/conftest.py tests/test_oppsett.py` then commit with message `test(02-02): legg til pytest-harness (pytest.ini, requirements-dev.txt, tests/)`. Explicit pathspecs only — `git add -A` and `git add .` are forbidden phase-wide.
  </action>
  <acceptance_criteria>
    - `./venv/bin/python -m pytest --collect-only -q 2>&1 | tail -1` reports at least 1 test collected (not "no tests ran")
    - `./venv/bin/python -m pytest -v` exits 0 with `test_repo_rot_er_importerbar PASSED` in the output
    - `grep -v '^#' pytest.ini | grep -c 'pythonpath = \.'` returns 1
    - `grep -c pytest requirements.txt` returns 0 (pytest did NOT leak into the runtime requirements file)
    - `./venv/bin/python -c "import tests.conftest"` is NOT required to work; instead `./venv/bin/python -m pytest --fixtures tests/ 2>&1 | grep -c -E 'kamper_df|fremtidige_kamper_df|as_of_dato'` returns 3
    - `git status --short modell_utils.py` is empty (D-02 — file untouched)
    - `git log -1 --stat` shows exactly 4 files: `requirements-dev.txt`, `pytest.ini`, `tests/conftest.py`, `tests/test_oppsett.py`
  </acceptance_criteria>
  <verify>
    <automated>./venv/bin/python -m pytest -v && ./venv/bin/python -m pytest --collect-only -q | tail -1 && grep -c pytest requirements.txt; git log -1 --stat</automated>
  </verify>
  <done>`pytest.ini`, `requirements-dev.txt`, `tests/conftest.py` and `tests/test_oppsett.py` exist and are committed; `pytest -v` collects and passes at least one test; `requirements.txt` and `modell_utils.py` are unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Create config.py and rewire 04_value_detector.py and 06_bot.py to import from it</name>
  <files>config.py, 04_value_detector.py, 06_bot.py</files>
  <read_first>
    - `04_value_detector.py` lines 18-46 (import block and the KONFIGURASJON section being emptied)
    - `06_bot.py` lines 20-50 (import block, constant block, and the `les_bankroll` use of STARTKAPITAL)
    - `06_bot.py` lines 196-320 (the `beregn_innsats` and `plasser_bets` uses of KELLY_FRAKSJON/MIN_INNSATS/MAX_INNSATS)
    - `06_bot.py` lines 1015-1030 (the ROI/PnL uses of STARTKAPITAL)
    - `.planning/phases/02-shared-core-extraction-test-foundation/02-RESEARCH.md` section "Pattern 4: config.py single source of truth"
  </read_first>
  <action>
    Create `config.py` at repo root (flat, per D-01 — not a package) and make it the only place these seven values are written as literals.

    1. `config.py` gets a Norwegian module docstring stating it is the single source of truth for strategy parameters, imported by the live bot today and by the Phase 5 backtest later, and that changing a value here changes what a backtest validates. Then seven module-level `UPPER_SNAKE_CASE` constants copied verbatim, values and trailing comments intact, from the `<interfaces>` block above: `MIN_VALUE_TERSKEL = 0.05`, `MIN_ODDS = 1.50`, `MAX_ODDS = 4.00`, `KELLY_FRAKSJON = 0.5`, `MAX_INNSATS = 150.0`, `MIN_INNSATS = 20.0`, `STARTKAPITAL = 1000.0`. No type hints (project convention). No functions, no dataclass, no I/O, no `os.environ` reads.

    2. Add a comment in `config.py` stating that the Odds API key deliberately does NOT live here — it stays as an environment-variable read in `04_value_detector.py` (Phase 1, `ODDS_API_NOKKEL`), because `config.py` is committed to git and secrets are not.

    3. In `04_value_detector.py`: delete the three literal assignments at lines 43-45 and add `from config import MIN_VALUE_TERSKEL, MIN_ODDS, MAX_ODDS` to the import block. Leave the `API_NØKKEL` env-var load and its `sys.exit(1)` fail-fast exactly as they are. The names `MIN_VALUE_TERSKEL`, `MIN_ODDS`, `MAX_ODDS` stay identical, so the existing uses at lines 238, 252 and 287 need no edit.

    4. In `06_bot.py`: delete the four literal assignments at lines 33-36 and add `from config import KELLY_FRAKSJON, MAX_INNSATS, MIN_INNSATS, STARTKAPITAL` to the import block. Names stay identical, so uses at lines 48-49, 220-221, 287, 329 and 1019-1026 need no edit.

    5. Per Pitfall 1 this is extraction only. The diff on `04_value_detector.py` and `06_bot.py` must consist of exactly: one added import line and the deleted constant lines. Any other change in those files is out of scope for this task — do not tidy, reformat or "improve" anything else while in there.

    6. Verify nothing broke at import time by byte-compiling both scripts (they execute top-level code on import, so do not import them — compile them instead): `./venv/bin/python -m py_compile 04_value_detector.py 06_bot.py config.py`.

    7. Stage and commit: `git add config.py 04_value_detector.py 06_bot.py` then commit with message `refactor(02-02): flytt strategikonstanter til config.py (CORE-02)`. Include in the commit body: whether the developer chose "include" in plan 01 Task 1, since `06_bot.py`'s pre-existing WIP rides along with this commit.
  </action>
  <acceptance_criteria>
    - `grep -c 'MIN_VALUE_TERSKEL = \|MIN_ODDS = \|MAX_ODDS = \|KELLY_FRAKSJON = \|MAX_INNSATS = \|MIN_INNSATS = \|STARTKAPITAL = ' config.py` returns 7
    - `grep -v '^#' 04_value_detector.py | grep -c -E '^(MIN_VALUE_TERSKEL|MIN_ODDS|MAX_ODDS) *='` returns 0
    - `grep -v '^#' 06_bot.py | grep -c -E '^(STARTKAPITAL|KELLY_FRAKSJON|MAX_INNSATS|MIN_INNSATS) *='` returns 0
    - `grep -c 'from config import' 04_value_detector.py` returns 1 and `grep -c 'from config import' 06_bot.py` returns 1
    - `grep -c 'ODDS_API_NOKKEL\|API_NØKKEL' config.py` returns 0 (no secret moved into the committed config module)
    - `./venv/bin/python -c "import config; assert (config.MIN_VALUE_TERSKEL, config.MIN_ODDS, config.MAX_ODDS, config.KELLY_FRAKSJON, config.MAX_INNSATS, config.MIN_INNSATS, config.STARTKAPITAL) == (0.05, 1.50, 4.00, 0.5, 150.0, 20.0, 1000.0)"` exits 0
    - `./venv/bin/python -m py_compile 04_value_detector.py 06_bot.py config.py` exits 0
    - `git diff HEAD~1 --numstat -- 04_value_detector.py` shows at most 1 insertion and 3 deletions
  </acceptance_criteria>
  <verify>
    <automated>./venv/bin/python -m py_compile config.py 04_value_detector.py 06_bot.py && ./venv/bin/python -c "import config; assert (config.MIN_VALUE_TERSKEL, config.MIN_ODDS, config.MAX_ODDS, config.KELLY_FRAKSJON, config.MAX_INNSATS, config.MIN_INNSATS, config.STARTKAPITAL) == (0.05, 1.50, 4.00, 0.5, 150.0, 20.0, 1000.0); print('config OK')"</automated>
  </verify>
  <done>`config.py` holds all seven constants at their exact current values; `04_value_detector.py` and `06_bot.py` import them and contain no literal assignment of any of the seven; both scripts still byte-compile; no secret was moved into `config.py`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock the config values behind a regression test (CORE-02)</name>
  <files>tests/test_strategy.py</files>
  <behavior>
    - `test_config_values`: `config.MIN_VALUE_TERSKEL == 0.05`, `MIN_ODDS == 1.50`, `MAX_ODDS == 4.00`, `KELLY_FRAKSJON == 0.5`, `MAX_INNSATS == 150.0`, `MIN_INNSATS == 20.0`, `STARTKAPITAL == 1000.0` — a deliberate tripwire so a Phase 5 threshold experiment cannot silently become the deployed config without a failing test forcing the change to be conscious
    - `test_config_har_ingen_hemmeligheter`: reading `config.py` as text, the source contains neither `ODDS_API` nor `apiKey` — config.py is committed, secrets are not
  </behavior>
  <read_first>
    - `config.py` (as written in Task 2)
    - `.planning/phases/02-shared-core-extraction-test-foundation/02-VALIDATION.md` (the per-task verification map names `tests/test_strategy.py::test_config_values` specifically — use that exact file and test name)
  </read_first>
  <action>
    Create `tests/test_strategy.py`. It will grow in plan 03 with the `beregn_innsats`, vig and dedup tests; this task creates it with only the two config tests above, plus a Norwegian module docstring noting that the file covers `config.py` and (from plan 03 onward) `strategy.py`.

    Write the two test functions named exactly `test_config_values` and `test_config_har_ingen_hemmeligheter`. Test function names are English per pytest convention; the comment above each assertion block explains in Norwegian *why* the assertion exists (per CLAUDE.md's "comments explain why, not what"), specifically: D-07 locks these values until Phase 5's backtest validates alternatives, and `KALIBRERING_RAPPORT.md` is the historical precedent for undeployed threshold changes causing confusion.

    For `test_config_har_ingen_hemmeligheter`, read `config.py` via `pathlib.Path(__file__).resolve().parent.parent / "config.py"` so the test does not depend on the process working directory.

    Do not import `strategy` in this file yet — it does not exist until plan 03, and an ImportError at collection time would break the whole suite.

    Stage and commit: `git add tests/test_strategy.py` then commit with message `test(02-02): lås strategikonstantene med regresjonstest (CORE-02)`.
  </action>
  <verify>
    <automated>./venv/bin/python -m pytest tests/test_strategy.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `./venv/bin/python -m pytest tests/test_strategy.py::test_config_values -v` exits 0 and prints `PASSED`
    - `./venv/bin/python -m pytest tests/test_strategy.py -v 2>&1 | grep -c '2 passed'` returns 1
    - `./venv/bin/python -m pytest -v` (whole suite) exits 0 with 3 tests passing
    - `grep -c 'import strategy\|from strategy' tests/test_strategy.py` returns 0 at this point in the phase
    - Deliberate-break check: temporarily editing `config.py`'s `MAX_ODDS` to `5.00` makes `pytest tests/test_strategy.py` fail; revert the edit and confirm it passes again. Record both outcomes in the SUMMARY.
  </acceptance_criteria>
  <done>`tests/test_strategy.py` exists with two passing config tests, the whole suite is green at 3 tests, and the deliberate-break check confirmed the tripwire actually fires.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → local venv | `pytest` executes locally with full user privileges once installed. |
| working tree → git history (public repo) | `config.py` is committed; anything written into it is public forever. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | `pip install -r requirements-dev.txt` (pytest) | mitigate | RESEARCH.md Package Legitimacy Audit: pytest is PyPI, ~20 yrs old, source `github.com/pytest-dev/pytest`, slopcheck `[OK]`, disposition Approved — not `[ASSUMED]`/`[SUS]`, so no blocking human checkpoint. Version floor pinned in `requirements-dev.txt`; install restricted to the repo venv via `./venv/bin/python -m pip`. |
| T-02-04 | Information Disclosure | consolidating "configuration" into `config.py` invites moving the Odds API key there next to the thresholds | mitigate | `config.py` carries an explicit comment that secrets stay in the env-var load; `test_config_har_ingen_hemmeligheter` asserts the file contains no `ODDS_API`/`apiKey` string, so a future move fails CI-equivalent verification (Task 3) |
| T-02-05 | Tampering | a silent change to a money-critical threshold (the `KALIBRERING_RAPPORT.md` precedent — never-deployed 0.20/2.50 thresholds documented as if live) | mitigate | `test_config_values` pins all seven values; changing one requires editing the test in the same commit, making the change conscious and reviewable |
| T-02-01 | Information Disclosure | `git add` sweeping `06_bot.py`'s ~1009 uninspected WIP lines into a public commit | mitigate | Disposition decided by the developer in plan 01 Task 1 and restated in this plan's commit body; explicit-pathspec staging only |
</threat_model>

<verification>
- `./venv/bin/python -m pytest -v` collects and passes 3 tests
- `./venv/bin/python -m py_compile 04_value_detector.py 06_bot.py config.py` exits 0
- `git log --oneline -3` shows three commits touching only the files listed in `files_modified`
- `git status --short 03_tren_modell.py` still shows ` M` (untouched by this plan, per D-09)
</verification>

<success_criteria>
- pytest runs from repo root and imports repo-root modules without any `sys.path` hack in test files
- Exactly one place in the repo assigns each of the seven strategy constants a literal value
- All seven values equal their pre-phase values (0.05 / 1.50 / 4.00 / 0.5 / 150.0 / 20.0 / 1000.0)
- No secret was relocated into `config.py`
- `requirements.txt` unchanged; `modell_utils.py` unchanged; `03_tren_modell.py` unchanged
</success_criteria>

<output>
Create `.planning/phases/02-shared-core-extraction-test-foundation/02-02-SUMMARY.md` when done. Record: the installed pytest version, the collected test count, and the deliberate-break check result from Task 3.
</output>
