---
phase: 02-shared-core-extraction-test-foundation
plan: 06
type: execute
wave: 6
depends_on: ["02-05"]
files_modified:
  - tests/test_parity.py
  - .planning/phases/02-shared-core-extraction-test-foundation/02-VALIDATION.md
autonomous: true
requirements: [CORE-04]
user_setup: []

must_haves:
  truths:
    - "D-12: CORE-04 is satisfied as a determinism/referential-transparency test on the shared core, not a live-vs-backtest integration test — the backtest engine does not exist until Phase 5, and this scoping is stated in the test module docstring"
    - "Calling the shared functions twice with identical inputs (including a fixed as_of) returns identical results — the property that makes it safe for two different call sites to share them"
    - "Appending rows dated on or after as_of to the input frame does not change any feature value computed for dates before as_of — the leakage regression Pitfall 3 names"
    - "A full simulated bet decision (features to vig removal to value/EV to threshold to stake) is reproduced identically across two independent invocations from the same inputs"
    - "The repo-wide de-duplication audit shows zero surviving duplicate implementations of team lookup, feature engineering, strategy math or strategy constants in tracked source"
  artifacts:
    - path: "tests/test_parity.py"
      provides: "CORE-04 determinism and leakage-regression tests over features.py and strategy.py"
      contains: "assert_frame_equal"
      min_lines: 80
  key_links:
    - from: "tests/test_parity.py"
      to: "features.py"
      via: "from features import beregn_lag_form"
      pattern: "from features import"
    - from: "tests/test_parity.py"
      to: "strategy.py"
      via: "from strategy import fjern_vigorish, beregn_value_og_ev, beregn_innsats"
      pattern: "from strategy import"
---

<objective>
Close CORE-04 and gate the phase. The requirement text says "a parity/leakage regression test confirms the live path and backtest path produce an identical decision for the same historical date/game" — but the backtest path does not exist until Phase 5. CONTEXT.md D-12 scopes this down explicitly: prove the shared functions are referentially transparent and leakage-safe, which is the property that makes a second call site safe to add later. This plan implements that scoping and states it in the test file itself, so a Phase 5 reader knows why this test is shaped the way it is and what still has to be added once the real backtest exists.

Purpose: the extractions in plans 03-05 are only worth anything if the shared functions genuinely produce the same answer regardless of who calls them and what else is in the input frame. That is a testable property today.
Output: `tests/test_parity.py`, an updated `02-VALIDATION.md` with real plan/task IDs and statuses, and a recorded repo-wide de-duplication audit proving ROADMAP Phase 2 success criterion 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-shared-core-extraction-test-foundation/02-CONTEXT.md
@.planning/phases/02-shared-core-extraction-test-foundation/02-RESEARCH.md
@.planning/phases/02-shared-core-extraction-test-foundation/02-VALIDATION.md
@.planning/phases/02-shared-core-extraction-test-foundation/02-03-SUMMARY.md
@.planning/phases/02-shared-core-extraction-test-foundation/02-04-SUMMARY.md
@.planning/phases/02-shared-core-extraction-test-foundation/02-05-SUMMARY.md
</context>

<phase_ordering_note>
Binding: explicit-pathspec staging only; `03_tren_modell.py` never staged (D-09). This plan modifies no production source — only a new test file and the phase's own VALIDATION.md.
</phase_ordering_note>

<interfaces>
<!-- Shared-core signatures as built by plans 02-03 through 02-05. Confirm against the SUMMARYs before writing tests. -->

features.py:
  STATS_KOLONNER   # 9 stats
  DIFF_STATS       # 7 stats (batch subset)
  RULLENDE_VINDU   # 10
  beregn_lag_form(df_raw, vindu=RULLENDE_VINDU, as_of=None)   # -> DataFrame, 2 rows per game
  snitt_fra_kamplogg(df_logg)                                  # -> dict keyed by STATS_KOLONNER
  bygg_feature_rad(hjemme_stats, borte_stats)                  # -> dict, 27 keys

strategy.py:
  fjern_vigorish(odds_hjemme, odds_borte)                                        # -> (p_h, p_b)
  beregn_value_og_ev(modell_prob, odds, impl_prob)                               # -> (value, ev)
  beregn_innsats(saldo, modell_prob, odds, kelly_fraksjon, min_innsats, max_innsats)
  finn_bet_nokkel(kamp, bet, kamp_dato)
  bygg_bet_nokler(bets)
  er_duplikat(nokkel, eksisterende_nokler)

config.py:
  MIN_VALUE_TERSKEL=0.05  MIN_ODDS=1.50  MAX_ODDS=4.00
  KELLY_FRAKSJON=0.5  MAX_INNSATS=150.0  MIN_INNSATS=20.0  STARTKAPITAL=1000.0

tests/conftest.py fixtures (plan 02-02):
  kamper_df            # 10 synthetic games, 21 raw columns, dates 2024-11-01..2024-11-28
  fremtidige_kamper_df # 2 rows: one dated exactly 2024-12-01, one 2024-12-04
  as_of_dato           # "2024-12-01"
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the CORE-04 determinism and leakage-regression tests</name>
  <files>tests/test_parity.py</files>
  <behavior>
    - `test_beregn_lag_form_er_deterministisk`: two calls with the same frame and the same `as_of` produce frames that satisfy `pd.testing.assert_frame_equal` exactly — referential transparency, the property that lets two call sites share the function
    - `test_fremtidige_rader_endrer_ikke_tidligere_features`: `beregn_lag_form(kamper_df, as_of=as_of_dato)` and `beregn_lag_form(pd.concat([kamper_df, fremtidige_kamper_df]), as_of=as_of_dato)` produce identical frames — appending games dated on or after the cutoff cannot change any feature computed before it. This is the leakage regression: a Phase 5 walk-forward loop will pass the full multi-season frame on every iteration, and this asserts that is safe
    - `test_grenserad_paa_as_of_er_ekskludert`: the row dated exactly `2024-12-01` from `fremtidige_kamper_df` contributes no row to the output when `as_of="2024-12-01"` — assert on GAME_ID membership; strict `<`, not `<=` (Pitfall 3)
    - `test_rekkefolge_i_input_endrer_ikke_output`: shuffling the input rows with a fixed permutation (do NOT use `random`; reverse the frame with `.iloc[::-1]`) and re-running produces the same result after sorting both outputs by `["TEAM_ID", "DATO"]` and resetting the index — the function must not depend on incoming row order, since a backtest will feed rows in a different order than the CSV does
    - `test_strategy_er_deterministisk`: `fjern_vigorish`, `beregn_value_og_ev` and `beregn_innsats` each return exactly equal values across two invocations with identical arguments
    - `test_identisk_bet_beslutning_fra_to_kallsteder`: a helper defined inside the test file, `simuler_bet_beslutning(modell_prob, odds_hjemme, odds_borte, saldo)`, chains vig removal to value/EV to the `MIN_VALUE_TERSKEL`/`MIN_ODDS`/`MAX_ODDS` gate to `beregn_innsats`, using `config` values — the same chain `04_value_detector.py` and `06_bot.py` perform across a process boundary today, and the shape Phase 5's backtest will perform in-process. Two independent invocations with identical inputs must return an identical `(bet_flagget, value, ev, innsats)` tuple. Cover three input sets: one clear value bet, one below the value threshold, one above `MAX_ODDS`
    - `test_bet_beslutning_respekterer_odds_grensene`: the `MAX_ODDS` case returns `bet_flagget is False` even when value is positive — proving the odds gate is part of the shared decision, not an incidental filter
  </behavior>
  <read_first>
    - `features.py`, `strategy.py`, `config.py` (the actual built signatures — reconcile against the `<interfaces>` block and use what is really there)
    - `tests/conftest.py` (the three fixtures)
    - `.planning/phases/02-shared-core-extraction-test-foundation/02-CONTEXT.md` decision D-12 (the exact scoping wording that must be reproduced in the module docstring)
    - `.planning/phases/02-shared-core-extraction-test-foundation/02-RESEARCH.md` "Pitfall 3", "Example determinism test shape for CORE-04"
    - `04_value_detector.py` lines 204-265 (the real decision chain `simuler_bet_beslutning` must mirror: vig removal, value, EV, then `value > MIN_VALUE_TERSKEL and MIN_ODDS <= odds <= MAX_ODDS`)
  </read_first>
  <action>
    Create `tests/test_parity.py`.

    The module docstring is load-bearing and must state, in Norwegian: that CORE-04's literal wording asks for a live-vs-backtest parity test; that no backtest engine exists until Phase 5; that per CONTEXT.md D-12 this is scoped to a determinism and leakage-regression test on the shared core, which proves the functions are safe to call identically from two call sites without the second call site existing yet; and — explicitly — what Phase 5 must ADD once the backtest exists, namely a test asserting the backtest replay and the live path produce the same bet decision for one fixed historical date and game. Write that last sentence as a concrete instruction, because it is the only record of the deferred half of this requirement.

    Import `pandas as pd`, `pytest`, `from features import beregn_lag_form`, `from strategy import fjern_vigorish, beregn_value_og_ev, beregn_innsats`, and `import config`.

    Write the seven tests named in `<behavior>`, plus the `simuler_bet_beslutning` helper. The helper must read its thresholds from `config` (not from literals), so a future threshold change is reflected here automatically rather than being pinned twice — `test_config_values` in `tests/test_strategy.py` is what pins the values themselves.

    Concrete inputs for `test_identisk_bet_beslutning_fra_to_kallsteder`:
      - clear value bet: `modell_prob=0.60, odds_hjemme=2.00, odds_borte=2.00, saldo=1000.0` → vig-free implied 0.50, value +0.10 (above the 0.05 threshold), odds 2.00 inside [1.50, 4.00] → flagged, stake 100.0
      - below threshold: `modell_prob=0.52, odds_hjemme=2.00, odds_borte=2.00, saldo=1000.0` → value +0.02, below 0.05 → not flagged
      - above MAX_ODDS: `modell_prob=0.40, odds_hjemme=5.00, odds_borte=1.20, saldo=1000.0` → value is positive but odds 5.00 exceeds `MAX_ODDS=4.00` → not flagged
    Assert the expected outcome for each, then assert the two invocations are equal to each other. Use `pytest.approx` for the float components and exact equality for `bet_flagget` and `innsats`.

    Every value in this file must be deterministic. No `random`, no `datetime.now()`, no `date.today()`, no network. The `as_of` value comes from the `as_of_dato` fixture, never from the clock — a test that reads the wall clock is not a determinism test.

    Run `./venv/bin/python -m pytest tests/test_parity.py -v` and confirm 7 tests pass.

    Stage and commit: `git add tests/test_parity.py` then commit with message `test(02-06): determinisme- og leakage-regresjonstest for delt kjerne (CORE-04)`.
  </action>
  <acceptance_criteria>
    - `./venv/bin/python -m pytest tests/test_parity.py -v` exits 0 and reports `7 passed`
    - `./venv/bin/python -m pytest -v` (whole suite) exits 0 with 37 tests passing
    - `grep -c 'random\|datetime.now\|date.today' tests/test_parity.py` returns 0
    - `grep -c 'assert_frame_equal' tests/test_parity.py` returns at least 3
    - `grep -c 'config\.MIN_VALUE_TERSKEL\|config\.MAX_ODDS\|config\.MIN_ODDS' tests/test_parity.py` returns at least 3 (thresholds come from `config`, not re-typed literals)
    - `grep -c 'D-12' tests/test_parity.py` returns 1 (the scoping decision is cited in the file, not just in planning docs)
    - `grep -ci 'phase 5\|fase 5' tests/test_parity.py` returns at least 1 (the deferred half of CORE-04 is recorded in the test file)
    - Deliberate-break check: temporarily change `features.beregn_lag_form` to drop its `as_of` filter entirely, confirm `test_fremtidige_rader_endrer_ikke_tidligere_features` FAILS, revert, confirm it passes. Record both outcomes in the SUMMARY — this proves the leakage assertion is not vacuous.
  </acceptance_criteria>
  <verify>
    <automated>./venv/bin/python -m pytest tests/test_parity.py -v && ./venv/bin/python -m pytest -v</automated>
  </verify>
  <done>`tests/test_parity.py` passes 7 tests covering feature determinism, future-row invariance, the strict `as_of` boundary, input-order independence, strategy determinism, and the full simulated bet decision across three input sets; the module docstring records the D-12 scoping and what Phase 5 must add; the deliberate-break check proved the leakage assertion fires.</done>
</task>

<task type="auto">
  <name>Task 2: Run the phase de-duplication audit and close out VALIDATION.md</name>
  <files>.planning/phases/02-shared-core-extraction-test-foundation/02-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/02-shared-core-extraction-test-foundation/02-VALIDATION.md` (the Per-Task Verification Map with `TBD` plan/wave IDs and `⬜ pending` statuses to fill in)
    - `.planning/ROADMAP.md` Phase 2 section (the four success criteria this audit must evidence)
    - `.planning/phases/02-shared-core-extraction-test-foundation/02-01-SUMMARY.md` (the `debug_kamp.py` option id — determines whether the audit expects an exception)
    - `02-03-SUMMARY.md`, `02-04-SUMMARY.md`, `02-05-SUMMARY.md` (the recorded grep outputs and intentional behavior changes to consolidate)
  </read_first>
  <action>
    Prove the phase's central claim mechanically, then record it.

    1. Run the four de-duplication greps and capture the exact output of each into the SUMMARY under a "De-duplication audit" heading. All four exclude `./venv/`:

       a. Team lookup — `grep -rn "get_teams()" --include="*.py" . | grep -v "^./venv/" | grep -v "^./tests/"`
          Expected: 1 hit, in `teams.py`. If `02-01-SUMMARY.md` recorded `leave-untracked`, `debug_kamp.py` is also expected — record it as the declared exception rather than a failure.

       b. Stat list — `grep -rn '"PTS", *"FG_PCT", *"FT_PCT"' --include="*.py" . | grep -v "^./venv/"`
          Expected: 1 hit, the `STATS_KOLONNER` definition in `features.py`.

       c. Strategy constants — `grep -rn -E "^(MIN_VALUE_TERSKEL|MIN_ODDS|MAX_ODDS|KELLY_FRAKSJON|MAX_INNSATS|MIN_INNSATS|STARTKAPITAL) *=" --include="*.py" . | grep -v "^./venv/"`
          Expected: 7 hits, all in `config.py`.

       d. Money math — `grep -rn -E "^def (beregn_innsats|beregn_lag_form|finn_lag|fjern_vigorish)" --include="*.py" . | grep -v "^./venv/"`
          Expected: 4 hits total, one each in `strategy.py`, `features.py`, `teams.py`, `strategy.py` — and none in any numbered pipeline script.

       If any grep returns more than expected, STOP and report which file still holds a duplicate. Do not adjust the expectation to match reality — the surviving duplicate is the finding.

    2. Run the full suite once more and record the exact count: `./venv/bin/python -m pytest -v 2>&1 | tail -3`. Expected: 37 passed.

    3. Update `02-VALIDATION.md`:
       - In the Per-Task Verification Map, replace each `TBD` Plan/Wave and each `02-XX-XX` Task ID with the real values: the `teams.finn_lag` row is plan 02-04 wave 4 Task 2; the `features.beregn_lag_form` row is plan 02-05 wave 5 Task 2; the `strategy.fjern_vigorish` row is plan 02-03 wave 3 Task 2; the `config.py` row is plan 02-02 wave 2 Task 3; both CORE-03 rows are plan 02-03 wave 3 Task 2; the CORE-04 row is plan 02-06 wave 6 Task 1.
       - Change every `❌ Wave 0` in the File Exists column to `✅` and every `⬜ pending` status to `✅ green`, but ONLY for rows whose named command you actually ran and observed passing in this task. Any row you did not personally verify stays as it is, with a note.
       - Add a `Threat Ref` value where one exists: the `features.beregn_lag_form` `as_of` row maps to `T-02-14`, the `config.py` row to `T-02-05`, the `beregn_innsats` row to `T-02-06`, the dedup row to `T-02-07`, the `teams.finn_lag` row to `T-02-09`.
       - Set `status: complete` and `wave_0_complete: true` in the frontmatter.
       - Tick all six Validation Sign-Off checkboxes only if each is genuinely true; leave any unmet box unticked and say why.

    4. Do not modify any production source file in this task. `git status --short` must show only `02-VALIDATION.md` as newly modified beyond the pre-existing WIP.

    Stage and commit: `git add .planning/phases/02-shared-core-extraction-test-foundation/02-VALIDATION.md` then commit with message `docs(02-06): lukk valideringskontrakten for fase 2`.
  </action>
  <acceptance_criteria>
    - `grep -rn "get_teams()" --include="*.py" . | grep -v "^./venv/" | grep -v "^./tests/" | grep -vc "^./debug_kamp.py"` returns 1
    - `grep -rn '"PTS", *"FG_PCT", *"FT_PCT"' --include="*.py" . | grep -v "^./venv/" | wc -l` returns 1
    - `grep -rn -E "^(MIN_VALUE_TERSKEL|MIN_ODDS|MAX_ODDS|KELLY_FRAKSJON|MAX_INNSATS|MIN_INNSATS|STARTKAPITAL) *=" --include="*.py" . | grep -v "^./venv/" | grep -vc "^./config.py"` returns 0
    - `grep -rn -E "^def (beregn_innsats|beregn_lag_form|fjern_vigorish)" --include="*.py" . | grep -v "^./venv/" | grep -vc -E "^./(strategy|features).py"` returns 0
    - `./venv/bin/python -m pytest -v` exits 0 and reports `37 passed`
    - `grep -c 'TBD' .planning/phases/02-shared-core-extraction-test-foundation/02-VALIDATION.md` returns 0
    - `grep -c '02-XX-XX' .planning/phases/02-shared-core-extraction-test-foundation/02-VALIDATION.md` returns 0
    - `grep -c 'status: complete' .planning/phases/02-shared-core-extraction-test-foundation/02-VALIDATION.md` returns 1
    - `git log -1 --stat` shows exactly 1 file: `02-VALIDATION.md`
    - `git status --short 03_tren_modell.py` still shows ` M`
  </acceptance_criteria>
  <verify>
    <automated>./venv/bin/python -m pytest -v 2>&1 | tail -3 && grep -rn -E "^(MIN_VALUE_TERSKEL|MIN_ODDS|MAX_ODDS|KELLY_FRAKSJON|MAX_INNSATS|MIN_INNSATS|STARTKAPITAL) *=" --include="*.py" . | grep -v "^./venv/" && grep -rn "get_teams()" --include="*.py" . | grep -v "^./venv/" | grep -v "^./tests/"</automated>
  </verify>
  <done>All four de-duplication greps return their expected counts (with any declared `debug_kamp.py` exception recorded), the full suite is green at 37 tests, and `02-VALIDATION.md` has real plan/task IDs, real statuses, threat references and `status: complete`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared core → future Phase 5 backtest | This plan's tests are the only contract guaranteeing the shared functions behave identically for a caller that does not exist yet. |
| planning artifacts → phase verification | `02-VALIDATION.md` is what `/gsd:verify-work` reads; a status marked green without a run behind it launders an unverified claim into the phase record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Repudiation (false assurance) | a determinism test that passes vacuously — e.g. comparing a function to itself in a way no real regression could break — letting CORE-04 be marked satisfied without evidence | mitigate | The deliberate-break check in Task 1 removes the `as_of` filter and requires the leakage test to actually FAIL before it is accepted; the result is recorded in the SUMMARY |
| T-02-18 | Repudiation | `02-VALIDATION.md` rows marked `✅ green` for commands that were never run | mitigate | Task 2 permits flipping a status only for rows whose named command was run and observed in that task; unverified rows must stay pending with a stated reason |
| T-02-14 | Tampering (data leakage) | a future edit reintroducing `<=` or dropping the `as_of` filter, silently inflating Phase 5 backtest ROI | mitigate | `test_grenserad_paa_as_of_er_ekskludert` and `test_fremtidige_rader_endrer_ikke_tidligere_features` both fail on that edit; both are in the default `pytest -v` run |
| T-02-19 | Information Disclosure | a surviving duplicate implementation going unrecorded, so Phase 4/5 builds on the false premise that the core is single-sourced | mitigate | Four mechanical greps with pre-stated expected counts; any excess is reported as a finding rather than absorbed into a revised expectation |
| T-02-SC | Tampering | package installs | accept | This plan installs nothing. `pytest` was audited and installed in plan 02-02 (`[OK]`, Approved). No new dependency is introduced. |
</threat_model>

<verification>
- `./venv/bin/python -m pytest -v` exits 0 with 37 tests passing
- `tests/test_parity.py` contains no clock, no `random`, no network access
- All four de-duplication greps return their expected counts, with any `debug_kamp.py` exception explicitly declared
- `02-VALIDATION.md` has zero `TBD` and zero `02-XX-XX` placeholders and is marked `status: complete`
- No production source file was modified by this plan
</verification>

<success_criteria>
- CORE-04 is satisfied as scoped by D-12, with the scoping and the Phase 5 follow-up recorded inside the test file itself
- The shared core is proven referentially transparent, order-independent and leakage-safe at the `as_of` boundary
- ROADMAP Phase 2 success criteria 1-4 all have mechanical evidence: the four greps (criterion 1), the `config.py` grep (criterion 2), the stake and dedup tests (criterion 3), and `tests/test_parity.py` (criterion 4)
- The phase validation contract is closed with real IDs and honestly-earned statuses
</success_criteria>

<output>
Create `.planning/phases/02-shared-core-extraction-test-foundation/02-06-SUMMARY.md` when done. Record: the final test count, the deliberate-break check result from Task 1, all four de-duplication grep outputs verbatim under a "De-duplication audit" heading, any declared exception (untracked `debug_kamp.py`), and a "Deferred to Phase 5" section stating that the live-vs-backtest half of CORE-04's literal wording is still outstanding and where the instruction for it lives.
</output>
