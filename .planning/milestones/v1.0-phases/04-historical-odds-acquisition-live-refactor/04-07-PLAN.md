---
phase: 04-historical-odds-acquisition-live-refactor
plan: 07
type: execute
wave: 5
depends_on: [04-05]
files_modified:
  - .planning/phases/04-historical-odds-acquisition-live-refactor/04-SMOKETEST.md
  - odds_arkiv.db
autonomous: false
requirements: [ODDS-01]

user_setup:
  - service: the-odds-api
    why: "This plan makes the first real, credit-spending calls of the project's paid month"
    env_vars:
      - name: ODDS_API_NOKKEL
        source: "the-odds-api.com account page — already in the local, git-ignored .env since Phase 1"

must_haves:
  truths:
    - "The real, observed credit cost of a sport-wide snapshot call is known, not estimated"
    - "The number of tipoff clusters per real NBA date is measured, not assumed"
    - "Bookmaker coverage at the start of the date range is compared against the end of the range"
    - "Re-running an already-archived date against the real API costs zero credits, proven empirically"
    - "The developer has seen the projected full-run cost and explicitly approved or aborted it"
  artifacts:
    - path: ".planning/phases/04-historical-odds-acquisition-live-refactor/04-SMOKETEST.md"
      provides: "Recorded evidence: per-call credit cost, remaining balance, cluster counts, coverage comparison, projected full-run cost"
      contains: "x-requests-last"
  key_links:
    - from: "07_hent_historisk_odds.py --utfor"
      to: "api.the-odds-api.com"
      via: "first real credit-spending calls, bounded by --datoer and --maks-kreditt"
      pattern: "--maks-kreditt"
    - from: "04-SMOKETEST.md"
      to: "plan 04-09's --maks-kreditt argument"
      via: "measured cost drives the approved ceiling for the full run"
      pattern: "Projisert"
---

<objective>
Spend a small, hard-capped number of real credits to replace every remaining estimate in this phase with a measurement, then stop and ask. Nothing in this plan runs unattended past its own ceiling, and the full 480-date backfill is deliberately NOT part of it.

Purpose: 04-RESEARCH.md leaves three things unmeasured — the actual per-call cost, the real tipoff-cluster distribution (Open Question 2, assumption A2), and whether `eu` bookmaker coverage reaches back to 2022-10-24 (Open Question 3, assumption A4). All three change the full run's cost or its data quality. Guessing them and then spending 15,000 credits is the one irreversible mistake available in this phase.
Output: `04-SMOKETEST.md` with recorded evidence, a partially populated `odds_arkiv.db`, and an explicit human go/no-go on the full backfill.

Budget for this entire plan: at most 130 credits of the 20,000-credit month (~0.65%).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-historical-odds-acquisition-live-refactor/04-CONTEXT.md
@.planning/phases/04-historical-odds-acquisition-live-refactor/04-RESEARCH.md
@.planning/phases/04-historical-odds-acquisition-live-refactor/04-VALIDATION.md
@.planning/phases/04-historical-odds-acquisition-live-refactor/04-05-SUMMARY.md
@odds.py
@07_hent_historisk_odds.py

<interfaces>
CLI contract from plan 04-05 (all flags ASCII):
```
07_hent_historisk_odds.py --snapshot-type {bet_time,closing} --maks-kreditt N
                          [--utfor] [--fra YYYY-MM-DD] [--til YYYY-MM-DD]
                          [--datoer N] [--features-fil PATH] [--arkiv PATH]
```
Without `--utfor` the run is a dry run and makes zero calls. Exit code 1 means the run
stopped on the credit ceiling; exit code 0 means it completed.

Archive tables from plan 04-01:
```sql
odds_arkiv(id, sport, event_id, kamp_dato, hjemmelag, bortelag, hjemme_lag_id,
           borte_lag_id, commence_time, snapshot_type, snapshot_timestamp,
           bookmaker, marked, utfall_navn, odds, hentet_tidspunkt)
kreditt_logg(id, tidspunkt, endepunkt, forespurt_dato, kreditt_brukt,
             kreditt_igjen, antall_rader)
```

Verified-on-paper cost model to be confirmed against reality by this plan:
- sport-wide historical odds call: 10 credits (1 market x 1 region)
- historical events discovery call: 1 credit (0 if the result is empty)
- Estimated full run: bet_time 480 x 10 = 4,800; closing 480 x (1 + clusters x 10)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Røyktest mot ekte API — mål kostnad, klynger, dekning og gratis gjenkjøring</name>
  <files>.planning/phases/04-historical-odds-acquisition-live-refactor/04-SMOKETEST.md, odds_arkiv.db</files>
  <read_first>
    - 07_hent_historisk_odds.py (the exact flag names and the exit-code contract)
    - odds.py (kjor_backfill's returned counter dict and what it prints)
    - .planning/phases/04-historical-odds-acquisition-live-refactor/04-RESEARCH.md § "CRITICAL FINDING" (cost formula), § "Pitfall 3" (early-range bookmaker coverage), § "Open Questions" 2 and 3, § "Assumptions Log" A2/A3/A4
    - .planning/phases/04-historical-odds-acquisition-live-refactor/04-05-SUMMARY.md (whether `tenacity` was approved, and the measured 480-date list)
  </read_first>
  <action>
    Run the steps below in order and record every observed number in `04-SMOKETEST.md` as you go. Do not batch the runs and reconstruct the numbers afterwards — the response headers are the evidence and they are only visible per call.

    Hard rule for this entire task: no command may omit `--maks-kreditt`, and no `--maks-kreditt` value in this task may exceed `60`. If any single command reports a `kreditt_brukt` above its ceiling, stop immediately, record what happened, and do not run the remaining steps — that would mean the ceiling logic from plan 04-05 does not work and the full backfill must not proceed.

    Step 1 — Re-verify the cost model with zero credits. `WebFetch` `https://the-odds-api.com/liveapi/guides/v4/` and locate the "Usage Quota Costs" text for `GET historical odds` and `GET historical events`. 04-RESEARCH.md's metadata explicitly says to re-check this immediately before spending, because the milestone research was found wrong four days after it was written. Record both quoted cost formulas verbatim in `04-SMOKETEST.md`. If the formula for `GET historical odds` is no longer `10 x [markets] x [regions]` per call, STOP the whole task, record the discrepancy prominently, and let Task 2's checkpoint surface it — do not proceed to spend.

    Step 2 — Bet-time smoke, early end of range (2 dates, ceiling 30):
    `venv/bin/python 07_hent_historisk_odds.py --snapshot-type bet_time --fra 2022-10-24 --til 2022-10-25 --maks-kreditt 30 --utfor`
    Record: exit code, the printed counter dict, and for each call the `kreditt_brukt` and `kreditt_igjen` values now in `kreditt_logg`.

    Step 3 — Bet-time smoke, late end of range (2 dates, ceiling 30):
    Same command with `--fra 2025-04-12 --til 2025-04-13`. Record the same fields.

    Step 4 — Free-resume proof against the real API. Re-run the exact Step 2 command verbatim. It must report `hoppet_over` equal to the number of dates, `kall` equal to 0 and `kreditt_brukt` equal to 0, and `SELECT MAX(kreditt_igjen) FROM kreditt_logg` must be unchanged from Step 3. This is ODDS-01's central claim measured against the real API rather than a mock, so record the before/after remaining-credit numbers side by side.

    Step 5 — Closing-line smoke on one late date (ceiling 60):
    `venv/bin/python 07_hent_historisk_odds.py --snapshot-type closing --fra 2025-04-13 --til 2025-04-13 --maks-kreditt 60 --utfor`
    Record: how many clusters `grupper_commence_tider` produced, the `lukketidspunkt` requested for each, the `snapshot_timestamp` the API actually returned for each, and the total credits spent. Then check Pitfall 4's warning sign with SQL: no archived `closing` row may have a `snapshot_timestamp` later than its own `commence_time`. Report the count from
    `SELECT COUNT(*) FROM odds_arkiv WHERE snapshot_type='closing' AND snapshot_timestamp > commence_time;`
    — it must be `0`.

    Step 6 — Coverage comparison (Pitfall 3 / assumption A4). Run and record:
    `SELECT kamp_dato, snapshot_type, COUNT(DISTINCT event_id) AS kamper, COUNT(DISTINCT bookmaker) AS bookmakere, COUNT(*) AS rader FROM odds_arkiv GROUP BY kamp_dato, snapshot_type ORDER BY kamp_dato;`
    State plainly whether the 2022-10 dates returned meaningfully fewer distinct bookmakers than the 2025-04 dates. If the early dates return zero or one bookmaker per game while the late dates return several, say so as a finding — do not quietly average it away. Also record how many games each date's snapshot contained versus how many survived `parse_snapshot_til_rader`'s date-window filter; a large gap there means the timezone convention is wrong (Pitfall 2).

    Step 7 — Sanity-check the archive is queryable for its actual purpose (ROADMAP success criterion 3, "reconstruct odds as known on date D"). Run and record the output of:
    `SELECT hjemmelag, bortelag, bookmaker, utfall_navn, odds, snapshot_timestamp FROM odds_arkiv WHERE kamp_dato='2025-04-13' AND snapshot_type='bet_time' LIMIT 10;`

    Step 8 — Project the full run from measured numbers, not from A2's guessed three clusters per date:
    - bet_time total = 480 x (measured credits per sport-wide call)
    - closing total = 480 x (1 + measured clusters per date x measured credits per call)
    Show the arithmetic. Give a realistic figure and a worst case, compare both against the `kreditt_igjen` value observed in Step 5, and state clearly whether the full run fits in the remaining balance. If it does not fit, propose which subset to fetch (for example bet_time across all 480 dates first, since Phase 5's BT-01 needs it and BT-06's CLV need is what closing serves) rather than proposing to shrink the date range.

    Write all of the above into `04-SMOKETEST.md` with a Norwegian summary paragraph at the top and the raw numbers in tables below. This file is the input to Task 2 and to plan 04-09's ceiling.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-historical-odds-acquisition-live-refactor/04-SMOKETEST.md &amp;&amp; grep -q "x-requests-last" .planning/phases/04-historical-odds-acquisition-live-refactor/04-SMOKETEST.md &amp;&amp; venv/bin/python -c "
import sqlite3
con = sqlite3.connect('odds_arkiv.db')
rader = con.execute('SELECT COUNT(*) FROM odds_arkiv').fetchone()[0]
kall = con.execute('SELECT COUNT(*) FROM kreditt_logg').fetchone()[0]
brukt = con.execute('SELECT COALESCE(SUM(kreditt_brukt),0) FROM kreditt_logg').fetchone()[0]
sent = con.execute(\"SELECT COUNT(*) FROM odds_arkiv WHERE snapshot_type='closing' AND snapshot_timestamp > commence_time\").fetchone()[0]
assert rader > 0, 'arkivet er tomt'
assert kall > 0, 'ingen kall logget'
assert brukt <= 130, f'for mye kreditt brukt: {brukt}'
assert sent == 0, f'{sent} closing-rader er tatt etter tipoff'
print(f'GRØNN rader={rader} kall={kall} kreditt={brukt}')
"</automated>
  </verify>
  <acceptance_criteria>
    - `04-SMOKETEST.md` exists and records, as literal numbers: the `x-requests-last` value observed for a sport-wide odds call, the `x-requests-last` for a discovery call, and the `x-requests-remaining` before and after the smoke test
    - `SELECT COALESCE(SUM(kreditt_brukt),0) FROM kreditt_logg` is greater than `0` and at most `130`
    - The Step 4 re-run is recorded as `kall=0`, `kreditt_brukt=0`, with the remaining-credit balance unchanged — quoted from the actual output, not asserted in prose
    - `SELECT COUNT(*) FROM odds_arkiv WHERE snapshot_type='closing' AND snapshot_timestamp > commence_time` returns `0`
    - `04-SMOKETEST.md` states the measured number of tipoff clusters for 2025-04-13 and uses it (not the assumed 3) in the projection
    - `04-SMOKETEST.md` states the distinct-bookmaker count for at least one 2022-10 date and one 2025-04 date, and gives an explicit verdict on whether early coverage is thinner
    - `04-SMOKETEST.md` contains a projected full-run credit total with the arithmetic shown, and a yes/no on whether it fits the remaining balance
    - `git status --short` shows no `odds_arkiv.db` (the `.gitignore` rule from plan 04-01 holds)
  </acceptance_criteria>
  <done>Every estimate this phase was carrying is now a measured number recorded in `04-SMOKETEST.md`, the archive holds real rows for four dates, and the free-resume property is proven against the live API. At most 130 credits spent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Godkjenn eller avbryt full backfill basert på målte tall</name>
  <read_first>
    - .planning/phases/04-historical-odds-acquisition-live-refactor/04-SMOKETEST.md (produced by Task 1 — this checkpoint exists to put its numbers in front of you)
  </read_first>
  <what-built>
    Task 1 spent a capped amount of real credits on four game dates (two at each end of the 2022-10-24 → 2025-04-13 range) plus one closing-line date, and recorded every observed number in `04-SMOKETEST.md`. Nothing else has been fetched. Plan 04-09 is the full 480-date run and does not start without your answer here.
  </what-built>
  <action>
    Halt execution and present the five checks below, quoting the actual numbers out of `04-SMOKETEST.md` inline in your message so the developer does not have to open the file to answer. Do not start plan 04-09, do not run any further credit-spending command, and do not infer a ceiling from the projection. When the answer arrives, record both integer ceilings and the early-range coverage decision verbatim in `04-07-SUMMARY.md`.
  </action>
  <how-to-verify>
    Open `.planning/phases/04-historical-odds-acquisition-live-refactor/04-SMOKETEST.md` and check five things:

    1. **Cost per call.** Does the observed `x-requests-last` for a sport-wide odds call equal 10? If it is higher, the whole budget model shifts and the projection below is what matters, not the plan's original arithmetic.
    2. **Projected total versus remaining balance.** Does the projected full run fit inside the `x-requests-remaining` figure recorded in the smoke test, with headroom for retries? If it does not fit, Task 1 proposed a subset — decide whether you accept that subset or want to buy more credits.
    3. **Free resume.** Does Step 4 show `kall=0`, `kreditt_brukt=0` and an unchanged remaining balance? This is ODDS-01's core promise. If it does not hold, do not approve the full run — an interrupted 480-date run would re-spend from the start.
    4. **Early-range coverage.** Compare the distinct-bookmaker counts for the 2022-10 dates against the 2025-04 dates. If early coverage is materially thinner, decide now whether that is acceptable as a documented, date-dependent data-quality caveat that Phase 5's backtest must account for, or whether you want the archive to also fetch the `us` region for early dates (which roughly doubles the bet-time cost for those dates).
    5. **Closing-line timing.** Confirm the reported count of closing rows taken after tipoff is 0, and that the measured cluster count for 2025-04-13 looks plausible for an NBA slate.

    Then answer with a specific credit ceiling for the full run. Plan 04-09 passes whatever number you give straight into `--maks-kreditt`, and the run stops there rather than continuing.
  </how-to-verify>
  <resume-signal>Reply with either `godkjent: bet_time maks-kreditt=N, closing maks-kreditt=M` (two explicit ceilings) or `avbryt` with the reason</resume-signal>
  <acceptance_criteria>
    - The developer has replied with either two explicit integer ceilings or an abort — no implicit approval, and no ceiling inferred by the executor from the projection
    - The approved ceilings and the decision on early-range bookmaker coverage are both recorded verbatim in `04-07-SUMMARY.md`
    - If the answer is `avbryt`, `04-07-SUMMARY.md` records the reason and plan 04-09 is not executed
  </acceptance_criteria>
  <done>An explicit human decision exists, with two named credit ceilings, based on measured numbers rather than the plan's original estimates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → real, finite, already-paid-for credit budget | The first irreversible spend of the phase happens here |
| The Odds API docs → this project's cost model | A silently changed pricing page would invalidate every budget number in the phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-31 | Denial of service | Smoke test itself overspending | mitigate | Every command carries `--maks-kreditt` at or below 60 and an explicit `--fra`/`--til` range; the automated verify asserts total spend at or below 130 |
| T-04-32 | Repudiation | Approving a 15,000-credit run on estimates rather than measurements | mitigate | Blocking checkpoint presents measured cost, measured cluster count and measured coverage; the developer supplies the ceiling, the executor does not infer it |
| T-04-33 | Tampering | Stale pricing model from a four-day-old research doc | mitigate | Step 1 re-verifies the official docs with zero credits and halts the task on any discrepancy |
| T-04-34 | Tampering | Closing-line snapshots taken after tipoff, silently corrupting future CLV numbers | mitigate | SQL assertion that no closing row has `snapshot_timestamp > commence_time`, enforced in the automated verify |
| T-04-35 | Information disclosure | API key in the recorded evidence file | mitigate | `04-SMOKETEST.md` records only header values, counts and SQL output; never paste a full request URL containing `apiKey` |
</threat_model>

<verification>
- `venv/bin/python -m pytest -q` — full suite still green (no source files changed by this plan)
- `04-SMOKETEST.md` exists and is committed
- `git status --short` shows no `odds_arkiv.db`
- Total credits consumed by this plan, read from `kreditt_logg`, is at most 130
</verification>

<success_criteria>
- Assumptions A2 (cluster count), A4 (region coverage) and the cost formula are replaced by measurements
- ODDS-01's zero-cost-resume promise is demonstrated against the live API, not just against mocks
- The developer holds the go/no-go and sets the ceiling for the full run
</success_criteria>

<output>
Create `.planning/phases/04-historical-odds-acquisition-live-refactor/04-07-SUMMARY.md` when done
</output>
