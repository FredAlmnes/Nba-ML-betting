---
phase: 04-historical-odds-acquisition-live-refactor
plan: 09
type: execute
wave: 6
depends_on: [04-07]
files_modified:
  - odds_arkiv.db
  - .planning/phases/04-historical-odds-acquisition-live-refactor/04-ARKIV-RAPPORT.md
autonomous: false
requirements: [ODDS-01]

user_setup:
  - service: the-odds-api
    why: "The full historical backfill — the spend this phase's paid month was purchased for"
    env_vars:
      - name: ODDS_API_NOKKEL
        source: "the-odds-api.com account page — already in the local, git-ignored .env since Phase 1"

must_haves:
  truths:
    - "Bet-time odds are archived for the game dates covered by the approved budget"
    - "Closing-line odds are archived for the dates covered by the approved budget"
    - "The archive can reconstruct 'odds as known on the morning of date D' for any archived date, with no live API access"
    - "Every credit spent is recorded and reconcilable against the API's own remaining-balance figure"
    - "Any date the archive does not cover is named explicitly rather than silently missing"
  artifacts:
    - path: "odds_arkiv.db"
      provides: "Permanent SQLite odds archive for Phase 5's backtest"
    - path: ".planning/phases/04-historical-odds-acquisition-live-refactor/04-ARKIV-RAPPORT.md"
      provides: "Coverage, cost and gap report for the completed archive"
      contains: "kamp_dato"
  key_links:
    - from: "odds_arkiv.db"
      to: "nba_features.csv GAME_DATE_HJEMME"
      via: "kamp_dato join key for Phase 5's backtest"
      pattern: "kamp_dato"
    - from: "kreditt_logg"
      to: "x-requests-remaining"
      via: "spend reconciliation"
      pattern: "kreditt_igjen"
---

<objective>
Run the historical backfill this phase exists for, inside the two credit ceilings the developer approved in plan 04-07, and produce an honest coverage report naming every date the archive does and does not cover.

Purpose: ODDS-01. After this plan, Phase 5's backtest can iterate on strategy parameters as many times as it likes without spending another credit — which is the entire reason the archive is permanent and append-only (D-04).
Output: A populated `odds_arkiv.db` and `04-ARKIV-RAPPORT.md`.

Prerequisite: plan 04-07's checkpoint must have returned `godkjent` with two explicit integer ceilings. If it returned `avbryt`, do not execute this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-historical-odds-acquisition-live-refactor/04-CONTEXT.md
@.planning/phases/04-historical-odds-acquisition-live-refactor/04-RESEARCH.md
@.planning/phases/04-historical-odds-acquisition-live-refactor/04-SMOKETEST.md
@.planning/phases/04-historical-odds-acquisition-live-refactor/04-07-SUMMARY.md
@odds.py
@07_hent_historisk_odds.py

<interfaces>
CLI contract from plan 04-05:
```
07_hent_historisk_odds.py --snapshot-type {bet_time,closing} --maks-kreditt N
                          [--utfor] [--fra YYYY-MM-DD] [--til YYYY-MM-DD]
                          [--datoer N] [--features-fil PATH] [--arkiv PATH]
```
Exit 0 = ran to completion. Exit 1 = stopped on the credit ceiling (resume by re-running
the identical command; already-archived dates cost nothing).

Archive tables from plan 04-01:
```sql
odds_arkiv(id, sport, event_id, kamp_dato, hjemmelag, bortelag, hjemme_lag_id,
           borte_lag_id, commence_time, snapshot_type, snapshot_timestamp,
           bookmaker, marked, utfall_navn, odds, hentet_tidspunkt)
kreditt_logg(id, tidspunkt, endepunkt, forespurt_dato, kreditt_brukt,
             kreditt_igjen, antall_rader)
```

Target coverage: 480 unique game dates, 2022-10-24 through 2025-04-13, 3,638 games
(verified against `nba_features.csv`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full bet_time-backfill innenfor godkjent kredittak</name>
  <files>odds_arkiv.db</files>
  <read_first>
    - .planning/phases/04-historical-odds-acquisition-live-refactor/04-07-SUMMARY.md (the two approved ceilings — use the `bet_time` number verbatim, do not round it, do not raise it)
    - .planning/phases/04-historical-odds-acquisition-live-refactor/04-SMOKETEST.md (measured cost per call, and the early-range coverage verdict)
    - 07_hent_historisk_odds.py (flags and exit codes)
  </read_first>
  <action>
    Read the approved `bet_time` ceiling out of `04-07-SUMMARY.md`. If that file does not contain an explicit integer ceiling from the developer, STOP and report — this plan has no authority to pick one.

    Run a dry run first to confirm what is left to do after the smoke test's four dates:
    `venv/bin/python 07_hent_historisk_odds.py --snapshot-type bet_time --maks-kreditt <godkjent> `
    (no `--utfor`). Record the reported date count and skip count.

    Then run for real:
    `venv/bin/python 07_hent_historisk_odds.py --snapshot-type bet_time --maks-kreditt <godkjent> --utfor`

    This is a long-running loop over roughly 476 remaining dates. Expect it to take tens of minutes at ~0.2s of courtesy sleep plus request latency per call. Let it run to completion; do not add parallelism, do not raise the sleep to zero, and do not restart it from scratch if it is slow — every completed date is already committed and a restart would skip them for free anyway.

    Handle the two possible endings:
    - Exit 0: the loop covered every date. Record the final counter dict.
    - Exit 1 with `avbrutt_grunn == "kredittgrense"`: the approved ceiling was reached. This is a correct, designed outcome, not a failure. Record exactly which date it stopped on and how many dates remain, then STOP — do not re-run with a higher ceiling. Raising the ceiling is the developer's decision and belongs in Task 2's checkpoint.

    If the run aborts for any other reason (network outage, `SystemExit` from a fatal API error, keyboard interrupt), re-run the identical command once. Already-archived dates are skipped without spend, so a resume is free. If it fails the same way twice, stop and record the error rather than looping.

    After the run, record in your working notes (Task 3 turns these into the report):
    - `SELECT COUNT(DISTINCT kamp_dato) FROM odds_arkiv WHERE snapshot_type='bet_time';`
    - `SELECT COALESCE(SUM(kreditt_brukt),0) FROM kreditt_logg;`
    - `SELECT kreditt_igjen FROM kreditt_logg ORDER BY id DESC LIMIT 1;`
  </action>
  <verify>
    <automated>venv/bin/python -c "
import sqlite3
con = sqlite3.connect('odds_arkiv.db')
datoer = con.execute(\"SELECT COUNT(DISTINCT kamp_dato) FROM odds_arkiv WHERE snapshot_type='bet_time'\").fetchone()[0]
rader = con.execute(\"SELECT COUNT(*) FROM odds_arkiv WHERE snapshot_type='bet_time'\").fetchone()[0]
brukt = con.execute('SELECT COALESCE(SUM(kreditt_brukt),0) FROM kreditt_logg').fetchone()[0]
igjen = con.execute('SELECT kreditt_igjen FROM kreditt_logg ORDER BY id DESC LIMIT 1').fetchone()[0]
assert datoer >= 100, f'bare {datoer} datoer arkivert - kjoeringen kom knapt i gang'
assert rader > 0
print(f'GRØNN datoer={datoer}/480 rader={rader} kreditt_brukt={brukt} kreditt_igjen={igjen}')
"</automated>
  </verify>
  <acceptance_criteria>
    - The `--maks-kreditt` value used matches the integer in `04-07-SUMMARY.md` character for character
    - `SELECT COUNT(DISTINCT kamp_dato) FROM odds_arkiv WHERE snapshot_type='bet_time'` is recorded, together with how it compares to 480
    - `SELECT COALESCE(SUM(kreditt_brukt),0) FROM kreditt_logg` is recorded and does not exceed the approved ceiling plus the smoke test's spend
    - The final `kreditt_igjen` reading is recorded, and the difference between successive `kreditt_igjen` values is consistent with `kreditt_brukt` — a mismatch means the API is charging differently than it reports and must be flagged
    - If the run exited 1 on the ceiling, the stopping date and the remaining date count are recorded, and no higher-ceiling re-run was attempted
    - `git status --short` shows no `odds_arkiv.db`
  </acceptance_criteria>
  <done>Bet-time odds are archived for every date the approved budget reached, the spend is reconciled against the API's own balance, and any shortfall is named with the exact date it stopped at.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Godkjenn lukkelinje-passet mot faktisk gjenstående saldo</name>
  <read_first>
    - Task 1's recorded numbers (dates covered, credits spent, credits remaining)
    - .planning/phases/04-historical-odds-acquisition-live-refactor/04-07-SUMMARY.md (the approved `closing` ceiling — this checkpoint confirms or revises it against reality)
  </read_first>
  <what-built>
    The bet-time pass is done (or stopped at its ceiling). Task 1 recorded how many of the 480 dates are archived, how many credits went, and what the API says is left. The closing-line pass is the more expensive half — one discovery call plus one odds call per tipoff cluster per date — and it serves Phase 5's CLV requirement (BT-06), not the core backtest (BT-01), which only needs bet-time odds.
  </what-built>
  <action>
    Halt execution and present Task 1's recorded figures (dates covered, credits spent, credits remaining) together with the re-projection described below. Do not start the closing-line pass, do not raise the bet-time ceiling, and do not re-use plan 04-07's approved number without this fresh confirmation against the balance that actually remains. When the answer arrives, record the decision and its rationale verbatim in `04-09-SUMMARY.md`, then run Task 3.
  </action>
  <how-to-verify>
    1. Read Task 1's reported `datoer`, `kreditt_brukt` and `kreditt_igjen` figures.
    2. If bet-time coverage is short of 480 dates, decide first: finish bet-time with a raised ceiling, or accept partial coverage. Bet-time is the higher-value data — BT-01's whole replay depends on it, while closing lines only feed the CLV metric. If the remaining balance cannot cover both, prefer completing bet-time.
    3. Multiply the measured clusters-per-date figure from `04-SMOKETEST.md` by the remaining date count and the measured per-call cost to re-project the closing pass against the balance that actually remains right now, not against the projection made before the bet-time run.
    4. Decide one of three outcomes and reply with it.
  </how-to-verify>
  <resume-signal>Reply with `fullfør bet_time: maks-kreditt=N`, or `kjør closing: maks-kreditt=M`, or `stopp` (archive as-is, defer closing lines to a future paid month)</resume-signal>
  <acceptance_criteria>
    - The developer has replied with one of the three explicit outcomes; the executor did not infer a ceiling or proceed on the plan 04-07 number without this re-confirmation
    - The decision and its rationale are recorded verbatim in `04-09-SUMMARY.md`
    - If the answer is `stopp`, Task 3 still runs (the report is produced for whatever is archived) and the missing closing-line coverage is recorded as a named gap for Phase 5
  </acceptance_criteria>
  <done>An explicit human decision exists about the closing-line pass, made against the real remaining balance rather than a pre-run estimate.</done>
</task>

<task type="auto">
  <name>Task 3: Utfør godkjent pass og skriv arkivrapport med eksplisitte hull</name>
  <files>odds_arkiv.db, .planning/phases/04-historical-odds-acquisition-live-refactor/04-ARKIV-RAPPORT.md</files>
  <read_first>
    - Task 2's recorded decision
    - .planning/phases/04-historical-odds-acquisition-live-refactor/04-SMOKETEST.md (the early-range coverage verdict, which belongs in the report as a data-quality caveat)
    - .planning/research/ARCHITECTURE.md § "Common Pitfalls" #6 (treat missing historical odds honestly — skip, never substitute a nearby snapshot)
  </read_first>
  <action>
    Execute exactly the pass Task 2 approved, using its ceiling verbatim:
    - `fullfør bet_time` -> re-run the bet-time command with the new ceiling
    - `kjør closing` -> `venv/bin/python 07_hent_historisk_odds.py --snapshot-type closing --maks-kreditt <godkjent> --utfor`
    - `stopp` -> skip straight to the report

    Then write `.planning/phases/04-historical-odds-acquisition-live-refactor/04-ARKIV-RAPPORT.md`, in Norwegian with the raw numbers in tables. It is the handover document Phase 5's planner reads, so it must be honest about what is missing, not just about what is present.

    Content, in this order:

    1. **Sammendrag** — one paragraph: what the archive now contains, total credits spent this phase, credits remaining, and whether ODDS-01 is fully or partially satisfied.

    2. **Dekning per snapshot-type** — from
       `SELECT snapshot_type, COUNT(DISTINCT kamp_dato) AS datoer, COUNT(DISTINCT event_id) AS kamper, COUNT(*) AS rader FROM odds_arkiv GROUP BY snapshot_type;`
       compared against the target of 480 dates and 3,638 games.

    3. **Manglende datoer** — the explicit gap list. Compute the set difference between `odds.hent_unike_kampdatoer()` and the archived `bet_time` dates, and again for `closing`. List every missing date (or, if the list runs past 30 entries, give the count plus the contiguous ranges). Do not summarise this as "mostly complete" — Phase 5's backtest must be able to exclude these dates deliberately rather than silently produce thinner numbers for them.

    4. **Kamper uten arkiverte odds** — how many `nba_features.csv` games fall on an archived date but have no matching `event_id` in the archive. Cross-reference by `kamp_dato` and the two team IDs (`hjemme_lag_id`/`borte_lag_id`, populated via `teams.finn_lag_id`). Games where either lag_id is `NULL` are a separate, named subcategory — report both counts.

    5. **Bookmaker-dekning over tid** — from
       `SELECT substr(kamp_dato,1,7) AS maaned, COUNT(DISTINCT bookmaker) AS bookmakere, COUNT(DISTINCT event_id) AS kamper FROM odds_arkiv WHERE snapshot_type='bet_time' GROUP BY maaned ORDER BY maaned;`
       State plainly whether early-range coverage is thinner, carrying forward the smoke test's verdict. This is a real bias risk for a backtest that treats all dates as equally reliable (Pitfall 3, assumption A4).

    6. **Kredittregnskap** — total spent, split by endpoint from `SELECT endepunkt, COUNT(*), SUM(kreditt_brukt) FROM kreditt_logg GROUP BY endepunkt;`, plus the final `kreditt_igjen`.

    7. **Rekonstruksjonsbevis** — ROADMAP success criterion 3. Pick one archived date at random, and show the query and its output that reconstructs "odds as known on the morning of that date" with no network access:
       `SELECT hjemmelag, bortelag, bookmaker, utfall_navn, odds, snapshot_timestamp FROM odds_arkiv WHERE kamp_dato = ? AND snapshot_type = 'bet_time' ORDER BY hjemmelag, bookmaker;`

    8. **Kjente forbehold for Fase 5** — a bulleted handover list: the 13:00 UTC bet-time convention and the fact that it must match whatever time the live bot actually runs (assumption A3, D-01); any date-dependent bookmaker-coverage bias; missing dates; and whether closing lines exist for BT-06's CLV metric or were deferred.

    Do not fabricate or interpolate any number in this report. Every figure must come from a query you actually ran, and the queries themselves belong in the report next to their results.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-historical-odds-acquisition-live-refactor/04-ARKIV-RAPPORT.md &amp;&amp; venv/bin/python -c "
import sqlite3, odds
con = sqlite3.connect('odds_arkiv.db')
maal = set(odds.hent_unike_kampdatoer())
arkivert = {r[0] for r in con.execute(\"SELECT DISTINCT kamp_dato FROM odds_arkiv WHERE snapshot_type='bet_time'\")}
mangler = sorted(maal - arkivert)
sent = con.execute(\"SELECT COUNT(*) FROM odds_arkiv WHERE snapshot_type='closing' AND snapshot_timestamp > commence_time\").fetchone()[0]
rapport = open('.planning/phases/04-historical-odds-acquisition-live-refactor/04-ARKIV-RAPPORT.md').read()
assert sent == 0, f'{sent} closing-rader er tatt etter tipoff'
assert 'Manglende datoer' in rapport, 'rapporten mangler hull-seksjonen'
assert str(len(mangler)) in rapport, f'rapporten oppgir ikke antall manglende datoer ({len(mangler)})'
print(f'GRØNN arkiverte_datoer={len(arkivert)}/{len(maal)} manglende={len(mangler)}')
"</automated>
  </verify>
  <acceptance_criteria>
    - `04-ARKIV-RAPPORT.md` exists and contains all eight numbered sections
    - The count of missing `bet_time` dates in the report matches the set difference computed against `odds.hent_unike_kampdatoer()` exactly
    - `SELECT COUNT(*) FROM odds_arkiv WHERE snapshot_type='closing' AND snapshot_timestamp > commence_time` returns `0`
    - Section 7 contains a real query and its real output for a specific archived date
    - Section 6's total matches `SELECT SUM(kreditt_brukt) FROM kreditt_logg`
    - The report nowhere claims complete coverage unless the missing-date count is `0`
    - `venv/bin/python -m pytest -q` (full suite) exits 0 — no source file was changed by this plan
    - `git status --short` shows no `odds_arkiv.db`
  </acceptance_criteria>
  <done>The archive is populated to the extent the approved budget allowed, and `04-ARKIV-RAPPORT.md` tells Phase 5's planner exactly what is in it, what is missing, what it cost, and which caveats the backtest has to carry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → the remaining credit balance | The largest single spend of the project happens in this plan |
| `odds_arkiv.db` → Phase 5's backtest | Everything the backtest concludes about ROI rests on this file being honest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-42 | Denial of service | The full run exhausting the month's remaining credits | mitigate | Both passes bounded by developer-supplied ceilings; a ceiling hit stops the run and requires a fresh human decision before any raise |
| T-04-43 | Repudiation | An incomplete archive being presented as complete to Phase 5 | mitigate | Section 3's explicit missing-date list, enforced by an automated check that the report's count matches the computed set difference |
| T-04-44 | Tampering | Substituting a nearby snapshot for a missing one to fill a gap | mitigate | `parse_snapshot_til_rader` drops out-of-window games by construction (plan 04-03); the report names gaps instead of filling them (ARCHITECTURE.md Pitfall 6) |
| T-04-45 | Tampering | Date-dependent bookmaker coverage silently biasing early-season backtest results | mitigate | Section 5's month-by-month coverage table and an explicit verdict carried into the Phase 5 caveat list |
| T-04-46 | Repudiation | Spend not reconcilable after the fact | mitigate | `kreditt_logg` per call; Task 1 cross-checks successive `kreditt_igjen` deltas against reported `kreditt_brukt` |
| T-04-47 | Information disclosure | The archive or the report leaking the API key | mitigate | Neither table stores request URLs or params; the report contains only aggregate SQL output |
</threat_model>

<verification>
- `venv/bin/python -m pytest -q` — full suite green
- `04-ARKIV-RAPPORT.md` exists, is committed, and its missing-date count matches the live set difference
- Re-running the bet-time command a final time reports `kall=0` and `kreditt_brukt=0` for every already-archived date — the closing proof that further backtest iteration is free
- `git status --short` shows no `odds_arkiv.db`
</verification>

<success_criteria>
- ODDS-01 is satisfied for the coverage the approved budget allowed, with any shortfall named date by date
- Phase 5 can query "odds as known on the morning of date D" entirely offline
- Re-running the fetch costs zero credits, demonstrated one final time against the live API
- Total spend is reconciled against the API's own remaining-balance figure
</success_criteria>

<output>
Create `.planning/phases/04-historical-odds-acquisition-live-refactor/04-09-SUMMARY.md` when done
</output>
