---
phase: 05-walk-forward-backtest-engine
plan: 05
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - spillerlogg.py
  - tests/test_spillerlogg.py
  - nba_spillerlogg_raw.csv
autonomous: true
requirements: [BT-01, BT-02]

must_haves:
  truths:
    - "spillerlogg.py can be imported without making a single nba_api network call"
    - "One failing season fetch is skipped and logged; the other two seasons still land"
    - "Re-running the fetch with an existing nba_spillerlogg_raw.csv only fetches the seasons missing from it"
    - "nba_spillerlogg_raw.csv exists on disk with player-level rows for 2022-23, 2023-24 and 2024-25"
    - "GAME_DATE is a YYYY-MM-DD string, so a plain string comparison against an as_of date is a correct as-of filter (same convention as nba_features.csv)"
    - "A renamed or missing required column from nba_api fails loudly instead of producing a silently-empty injury signal"
    - "An all-seasons-failed run refuses to write an empty CSV and exits non-zero"
  artifacts:
    - path: "spillerlogg.py"
      provides: "Historical player-game-log acquisition, normalization and read/write helpers for the as-of injury filter"
      contains: "def hent_spillerlogg"
      min_lines: 90
    - path: "tests/test_spillerlogg.py"
      provides: "No-network schema, parse, skip-and-log and resume assertions on injected fixture DataFrames"
      contains: "def test_hent_spillerlogg_hopper_over_feilet_sesong"
    - path: "nba_spillerlogg_raw.csv"
      provides: "The player-game-log archive plan 05-06's sjekk_lag_helse_som_of() reads"
      contains: "PLAYER_ID"
  key_links:
    - from: "spillerlogg.py"
      to: "nba_api.stats.endpoints.leaguegamelog"
      via: "LeagueGameLog(player_or_team_abbreviation='P', ...) inside a function, never at module level"
      pattern: "player_or_team_abbreviation="
    - from: "tests/test_spillerlogg.py"
      to: "spillerlogg.hent_spillerlogg"
      via: "injected hent_fn keyword, mirroring skadefilter's siste3/sesong_snitt injection"
      pattern: "hent_fn="
    - from: "nba_spillerlogg_raw.csv"
      to: "spillerlogg.les_spillerlogg"
      via: "CSV read with GAME_DATE forced to str"
      pattern: "def les_spillerlogg"
---

<objective>
Close the Pitfall 1 data gap: build `spillerlogg.py` and produce `nba_spillerlogg_raw.csv`,
the player-game-log archive that makes the injury filter backtestable at all.

`05-RESEARCH.md` verified — by reading both files directly — that `nba_kamper_raw.csv` and
`nba_features.csv` are team-level only: no `PLAYER_ID`, no per-player `MIN`. The live
injury filter (`skadefilter.py`) therefore cannot be made as-of-aware from existing data.
`05-CONTEXT.md`'s Post-Research Resolution put the fix in scope rather than disabling the
filter, because BT-01 names the injury filter as part of the replayed decision pipeline and
disabling it would bias every reported ROI upward by an unknown amount (games where a key
player was actually out would be scored as if they weren't).

Research verified live that `leaguegamelog.LeagueGameLog(player_or_team_abbreviation="P",
season=...)` returns a full season of player-game rows in **one free call** (25,895 rows for
2022-23), with `PLAYER_ID`, `PLAYER_NAME`, `TEAM_ID`, `GAME_DATE` and numeric `MIN` — the
same season-loop-to-CSV shape `01_hent_data.py` already uses for team logs. Three seasons =
three calls. No Odds API credit is spent anywhere in this plan.

Scope boundary: this plan is **data acquisition only**. The as-of filter logic
(`sesong_grenser_for_dato`, `sjekk_lag_helse_som_of`) is plan 05-06's job and must not be
written here.

Purpose: without this file, plan 05-06 has nothing to filter and BT-01's "full decision
pipeline" claim is unbackable.
Output: `spillerlogg.py`, `tests/test_spillerlogg.py`, and a populated (git-ignored)
`nba_spillerlogg_raw.csv`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-walk-forward-backtest-engine/05-CONTEXT.md
@.planning/phases/05-walk-forward-backtest-engine/05-RESEARCH.md
@.planning/phases/05-walk-forward-backtest-engine/05-PATTERNS.md
@01_hent_data.py

<interfaces>
<!-- Everything the executor needs. No codebase exploration required. -->

nba_api endpoint signature (`venv/lib/python3.14/site-packages/nba_api/stats/endpoints/leaguegamelog.py`),
relevant parameters only:

```python
class LeagueGameLog(Endpoint):
    def __init__(self, counter=0, direction=Direction.default, league_id=LeagueID.default,
                 player_or_team_abbreviation=PlayerOrTeamAbbreviation.default,
                 season=Season.default, season_type_all_star=SeasonTypeAllStar.default,
                 sorter=Sorter.default, date_from_nullable="", date_to_nullable="", ...)
```

Constant values (`stats/library/parameters.py`): `PlayerOrTeamAbbreviation.player == "P"`
— the class default is `"T"` (team), so `"P"` MUST be passed explicitly. `LeagueID.nba == "00"`
is already the default; pass it explicitly anyway to match `01_hent_data.py`'s explicit NBA
scoping. Rows come from `.get_data_frames()[0]`, same as `01_hent_data.py:42`.

NOTE: the class-level `expected_data` dict in that file lists the **team**-mode columns and
does not mention `PLAYER_ID`/`PLAYER_NAME`. It is not a contract for player mode. Research
verified the real player-mode response live: `PLAYER_ID`, `PLAYER_NAME`, `TEAM_ID`,
`TEAM_ABBREVIATION`, `GAME_ID`, `GAME_DATE`, `MATCHUP`, `WL`, `MIN`, plus box-score columns.

`skadefilter.py:54-66` — the nba_api wrapper convention to mirror (try/except, printed
error, empty DataFrame, sleep after the call):

```python
def hent_spillerdata(season_type, sesong, last_n=0):
    """Henter spillerdata for gitt season_type/sesong. Returnerer tom DataFrame ved feil."""
    try:
        df = leaguedashplayerstats.LeagueDashPlayerStats(
            season=sesong, season_type_all_star=season_type, last_n_games=last_n
        ).get_data_frames()[0]
        time.sleep(1.0)  # Rate limiting
        return df
    except Exception as e:
        print(f"  (Kunne ikke hente {season_type} data: {e})")
        return pd.DataFrame()
```

`01_hent_data.py:29-51` — the season-loop shape to mirror (same three seasons, same
`time.sleep()` spacing, same concat-then-`to_csv(index=False)` ending):

```python
sesonger = ["2022-23", "2023-24", "2024-25"]
alle_kamper = []
for sesong in sesonger:
    print(f"\nHenter kamper for sesong {sesong}...")
    kamper_df = gamefinder.get_data_frames()[0]
    alle_kamper.append(kamper_df)
    print(f"  Fant {len(kamper_df)} rader (en rad per lag per kamp)")
    time.sleep(1)   # Viktig: nba_api har rate limiting
df = pd.concat(alle_kamper, ignore_index=True)
```

Date format this plan MUST produce, taken from the real `nba_features.csv` header and first
row — `GAME_DATE_HJEMME` is a plain `YYYY-MM-DD` string, which is exactly why `features.py`'s
`df_raw[df_raw["GAME_DATE_HJEMME"] < as_of]` string comparison is a correct as-of filter:

```
GAME_ID,GAME_DATE_HJEMME,TEAM_ABBREVIATION_HJEMME,TEAM_ABBREVIATION_BORTE,HJEMME_VANT,...
22200045,2022-10-24,MIA,TOR,0,108.0,...
```

Columns plan 05-06 will consume (`05-RESEARCH.md` Pattern 7, lines 400-431). This plan's
output must supply exactly these, under exactly these names:

```python
spillerlogg_df[(spillerlogg_df["TEAM_ID"] == team_id) &
               (spillerlogg_df["GAME_DATE"] >= sesong_start) &
               (spillerlogg_df["GAME_DATE"] < as_of_dato)]
    .groupby("PLAYER_ID").agg(MIN=("MIN", "mean"), PLAYER_NAME=("PLAYER_NAME", "first"))
```

`tests/test_skadefilter.py:30-46` — the exact no-network monkeypatch pattern to copy:

```python
def test_import_skadefilter_gjor_ingen_nettverkskall(monkeypatch):
    from nba_api.stats.endpoints import leaguedashplayerstats
    def _sprengt(*args, **kwargs):
        raise AssertionError("LeagueDashPlayerStats skal IKKE kalles ved import")
    monkeypatch.setattr(leaguedashplayerstats, "LeagueDashPlayerStats", _sprengt)
    modul = importlib.reload(skadefilter)
    assert modul is not None
```

`odds.py:631-639` — the named-counter result-dict convention this plan's loop returns:

```python
resultat = {
    "datoer_totalt": len(datoer), "hoppet_over": 0, "ville_hentet": 0,
    "kall": 0, "kreditt_brukt": 0, "nye_rader": 0, "avbrutt_grunn": None,
}
```

`pytest.ini` — `pythonpath = .`, `testpaths = tests`. Run tests as
`python3 -m pytest tests/test_spillerlogg.py -q` from the repo root.

Upstream from plan 05-01 (wave 1, already committed): `.gitignore` contains the exact line
`nba_spillerlogg_raw.csv`, so this plan's output file is ignored before it is ever created.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: spillerlogg.py — player-game-log acquisition with skip-and-log, resume, and strict schema normalization</name>

  <read_first>
    - `01_hent_data.py` (104 lines, read in full) — the season-loop-to-CSV analog, its numbered `# --- N. ... ---` banner style, and its `time.sleep(1)` rate-limit comment at line 47
    - `skadefilter.py` lines 1-70 — module docstring framing ("gjør ingen nettverkskall bare ved import"), `hent_spillerdata`'s try/except-returns-empty convention, and the `UPPER_SNAKE_CASE` config block placement
    - `.planning/phases/05-walk-forward-backtest-engine/05-PATTERNS.md` §"`nba_spillerlogg_raw.csv` acquisition" and §"Shared Patterns" — the analog mapping and the no-type-hints / Norwegian-snake_case / `if __name__` rules
    - `.planning/phases/05-walk-forward-backtest-engine/05-RESEARCH.md` §"Common Pitfalls → Pitfall 1" and §"Architecture Patterns → Pattern 7" — why this file exists and which columns must survive
  </read_first>

  <files>spillerlogg.py</files>

  <action>
    Create `spillerlogg.py` at the repo root. Norwegian identifiers and comments, no type
    hints, numbered `# --- N. ... ---` section banners, an `if __name__ == "__main__": main()`
    guard, and **zero network calls at module level** — every nba_api call lives inside a
    function, exactly as `skadefilter.py`'s own docstring explains for its own refactor.

    Module docstring (Norwegian) must state: this module owns the historical player-game
    log; it exists because `nba_kamper_raw.csv` and `nba_features.csv` are team-level only
    and the live injury filter cannot be made as-of-aware without player-level `MIN`; its
    consumer is plan 05-06's `sjekk_lag_helse_som_of()` in `skadefilter.py`; and the fetch
    uses the free `nba_api`, never The Odds API, so re-running costs no credits.

    Module-level constants:
    - `SESONGER = ["2022-23", "2023-24", "2024-25"]` — the same three seasons
      `01_hent_data.py:29` uses. Trailing Norwegian comment noting they must stay in sync
      with `01_hent_data.py`, since a season present in one file and absent from the other
      makes the backtest's injury check silently vacuous for those dates.
    - `SPILLERLOGG_FIL = "nba_spillerlogg_raw.csv"`
    - `SESONGTYPE = "Regular Season"` — trailing comment: matches `01_hent_data.py`'s
      `season_type_nullable="Regular Season"`, so the player log covers exactly the games
      `nba_features.csv` contains and no playoff games it does not.
    - `PAUSE_SEKUNDER = 1.0` — trailing comment mirroring `01_hent_data.py:47`'s
      rate-limiting note.
    - `KILDEKOLONNER = ["PLAYER_ID", "PLAYER_NAME", "TEAM_ID", "GAME_ID", "GAME_DATE", "MIN"]`
      — the columns required from nba_api.
    - `KOLONNER = ["SESONG"] + KILDEKOLONNER` — the exact column order written to CSV.

    Write six functions:

    1. `hent_sesong_logg(sesong)` — makes the single nba_api call:
       `leaguegamelog.LeagueGameLog(league_id="00", player_or_team_abbreviation="P",
       season=sesong, season_type_all_star=SESONGTYPE).get_data_frames()[0]`, then
       `time.sleep(PAUSE_SEKUNDER)` after a successful call. Wrap in
       `try/except Exception as e`, print the error in Norwegian, return `pd.DataFrame()`
       on failure — verbatim the convention `skadefilter.hent_spillerdata` uses. Docstring
       must state that `player_or_team_abbreviation="P"` is mandatory because the parameter
       class default is `"T"` (team mode), which would return team-level rows with no
       `PLAYER_ID` at all — the precise failure this module exists to prevent.

    2. `normaliser_spillerlogg(df, sesong)` — pure, no network, no I/O:
       (a) Verify every name in `KILDEKOLONNER` is present in `df.columns`. If any are
       missing, `raise ValueError` whose message lists the missing names and states that a
       silently-missing column would make the as-of injury check pass every team
       unconditionally (Pitfall 1's failure mode). This is the one place in this module
       that raises rather than skips — say why in the docstring.
       (b) Keep only `KILDEKOLONNER`, insert a `SESONG` column set to the `sesong` argument,
       and order the result per `KOLONNER`.
       (c) `GAME_DATE` becomes `pd.to_datetime(...).dt.strftime("%Y-%m-%d")` so it is a
       `YYYY-MM-DD` **string** regardless of what dtype nba_api returned. Docstring must
       state this is what makes plan 05-06's plain `GAME_DATE < as_of_dato` string
       comparison a correct as-of filter, identical to `features.py`'s
       `GAME_DATE_HJEMME < as_of` convention, and that using strict `<` (never `<=`) is the
       caller's responsibility.
       (d) `MIN` becomes `pd.to_numeric(..., errors="coerce")` then `.fillna(0.0)` as float.
       Docstring: a null or unparseable `MIN` becomes 0.0 deliberately, because that biases
       the downstream check toward "player looks unavailable" (bet flagged uncertain) — the
       conservative direction. The opposite default would manufacture false confidence.
       (e) Drop rows where `PLAYER_ID` or `GAME_DATE` is null.
       (f) `sort_values(["GAME_DATE", "TEAM_ID", "PLAYER_ID"]).reset_index(drop=True)` so the
       written CSV is byte-stable across identical fetches.

    3. `hent_spillerlogg(sesonger=None, hent_fn=None, eksisterende_df=None)` — the loop.
       `sesonger` defaults to `SESONGER`, `hent_fn` defaults to `hent_sesong_logg`. The
       `hent_fn` keyword is this module's injection point and is the entire reason the tests
       need no network — mirror the rationale `skadefilter.hent_spillerstatistikk`'s
       docstring gives for its `siste3`/`sesong_snitt` keywords.
       - If `eksisterende_df` is a non-empty DataFrame, read its `SESONG` column and skip any
         season already present, counting them as `allerede_hentet`. This is the resume
         mechanism: a partial run can be re-run and will refetch only what is missing, which
         also keeps repeated runs off the free nba_api endpoint.
       - For each remaining season, call `hent_fn(sesong)`. If the result is empty, print a
         Norwegian warning, append the season name to a `hoppet_over` list, and `continue` —
         never raise, never abort the remaining seasons. Otherwise pass it through
         `normaliser_spillerlogg` and collect it.
       - Return the tuple `(spillerlogg_df, resultat)`, where `spillerlogg_df` is the
         concatenation of `eksisterende_df` (if given) and every newly fetched season,
         re-sorted per step (f), and `resultat` is a named-counter dict in the
         `odds.py::kjor_backfill` style with keys `sesonger_totalt`, `sesonger_hentet`,
         `allerede_hentet`, `hoppet_over` (a list of season strings) and `rader_totalt`.

    4. `lagre_spillerlogg(df, filnavn=SPILLERLOGG_FIL)` — `df.to_csv(filnavn, index=False)`
       plus a Norwegian confirmation print with the row count.

    5. `les_spillerlogg(filnavn=SPILLERLOGG_FIL)` — `pd.read_csv` with
       `dtype={"SESONG": str, "GAME_DATE": str, "PLAYER_NAME": str}` so `GAME_DATE` never
       comes back as a datetime and silently breaks the string as-of comparison. On
       `FileNotFoundError`, raise a `FileNotFoundError` whose message names the file and
       instructs the reader to run `python3 spillerlogg.py` first — the same
       user-facing-instruction convention `05_skadefilter.py` uses for a missing
       `value_bets_idag.csv`. Then run the frame through the same `KILDEKOLONNER` presence
       check, so a hand-edited or truncated CSV fails loudly too.

    6. `main()` — `"=" * 60` banner, calls `les_spillerlogg()` inside a
       `try/except FileNotFoundError` to obtain `eksisterende_df` (None when absent), calls
       `hent_spillerlogg(eksisterende_df=...)`, prints a `"=" * 60` summary block listing
       every key in `resultat`. Then:
       - If the resulting DataFrame is empty, do **NOT** write the CSV. Print an explicit
         Norwegian error saying that an empty player log would make the backtest's injury
         filter pass every team silently, and `sys.exit(1)`.
       - Otherwise call `lagre_spillerlogg(...)`, then `sys.exit(1)` if `hoppet_over` is
         non-empty (partial run — visible in the exit code and re-runnable), else
         `sys.exit(0)`. Carry the same explanatory comment `04_value_detector.py` has about
         why a bare `exit()` is wrong here: it returns 0 and hides the failure from callers.

    Do NOT write `sesong_grenser_for_dato`, `sjekk_lag_helse_som_of`, or any edit to
    `skadefilter.py` — that is plan 05-06's scope. Do NOT touch `odds.py`,
    `verdi_deteksjon.py` or `tests/test_odds.py`; plan 05-04 owns those in this same wave.
  </action>

  <acceptance_criteria>
    - `spillerlogg.py` exists at the repo root
    - `grep -v '^#' spillerlogg.py | grep -c 'player_or_team_abbreviation'` outputs `1` or more
    - `grep -v '^#' spillerlogg.py | grep -cE 'def (hent_sesong_logg|normaliser_spillerlogg|hent_spillerlogg|lagre_spillerlogg|les_spillerlogg|main)\('` outputs `6`
    - `grep -c 'if __name__ == "__main__":' spillerlogg.py` outputs `1`
    - `python3 -c "import ast; t=ast.parse(open('spillerlogg.py').read()); assert all(n.__class__.__name__ in ('Expr','Import','ImportFrom','Assign','FunctionDef','If') for n in t.body); print('ingen modulnivaa-kall')"` exits 0 — proves no network call can fire on import
    - `python3 -c "import spillerlogg; assert spillerlogg.SESONGER == ['2022-23','2023-24','2024-25']; assert spillerlogg.KOLONNER[0] == 'SESONG'"` exits 0
    - `python3 -c "import pandas as pd, spillerlogg; d=pd.DataFrame([{'PLAYER_ID':1,'PLAYER_NAME':'A','TEAM_ID':1610612748,'GAME_ID':'0022200045','GAME_DATE':'2022-10-24','MIN':30,'EKSTRA':9}]); n=spillerlogg.normaliser_spillerlogg(d,'2022-23'); assert list(n.columns)==spillerlogg.KOLONNER; assert n.loc[0,'GAME_DATE']=='2022-10-24'; assert n.loc[0,'SESONG']=='2022-23'"` exits 0
    - `nba_spillerlogg_raw.csv` does not yet exist — this task writes no data file
  </acceptance_criteria>

  <verify>
    <automated>python3 -c "import pandas as pd, spillerlogg; d=pd.DataFrame([{'PLAYER_ID':1,'PLAYER_NAME':'A','TEAM_ID':1610612748,'GAME_ID':'0022200045','GAME_DATE':'2022-10-24','MIN':30,'EKSTRA':9}]); n=spillerlogg.normaliser_spillerlogg(d,'2022-23'); assert list(n.columns)==spillerlogg.KOLONNER; assert n.loc[0,'GAME_DATE']=='2022-10-24'; print('OK')"</automated>
  </verify>

  <done>`spillerlogg.py` imports with no network activity, exposes the six named functions, normalizes an injected frame to the exact `KOLONNER` order with a `YYYY-MM-DD` string `GAME_DATE`, and contains no injury-filter logic.</done>
</task>

<task type="auto">
  <name>Task 2: tests/test_spillerlogg.py — no-network schema, skip-and-log, resume and round-trip assertions</name>

  <read_first>
    - `tests/test_skadefilter.py` (189 lines, read in full) — the injected-DataFrame, monkeypatch-the-endpoint-to-raise, no-network test style this file must mirror
    - `tests/conftest.py` (read in full) — the deterministic-fixture philosophy: no `random`, no `datetime.now()`, everything reproducible
    - `spillerlogg.py` (written in Task 1) — the exact function signatures under test
  </read_first>

  <files>tests/test_spillerlogg.py</files>

  <action>
    Create `tests/test_spillerlogg.py`. Module docstring in Norwegian stating: these tests
    make zero network calls; every nba_api response is an injected synthetic DataFrame via
    `hent_spillerlogg`'s `hent_fn` keyword; and the file exists to prove the three properties
    plan 05-06 depends on — correct column schema, `GAME_DATE` usable as a string as-of key,
    and skip-and-log rather than crash on a failed season.

    Add a module-level helper `_raa_sesong(sesong, antall=3, team_id=1610612748)` that builds
    a synthetic raw nba_api-shaped DataFrame containing all of `KILDEKOLONNER` plus at least
    two extra columns (for example `MATCHUP` and `WL`) to prove extra columns are dropped.
    Dates must be deterministic and inside the given season (for `"2022-23"`, use
    `2022-10-24`, `2022-10-26`, `2022-10-28`). No `random`, no `datetime.now()`.

    Write these tests:

    1. `test_import_spillerlogg_gjor_ingen_nettverkskall` — monkeypatch
       `nba_api.stats.endpoints.leaguegamelog.LeagueGameLog` to a function raising
       `AssertionError`, then `importlib.reload(spillerlogg)` and assert the module loads.
       Copy the shape of
       `tests/test_skadefilter.py::test_import_skadefilter_gjor_ingen_nettverkskall` exactly.

    2. `test_normaliser_gir_eksakt_kolonneskjema` — pass `_raa_sesong("2022-23")` through
       `normaliser_spillerlogg`; assert `list(df.columns) == spillerlogg.KOLONNER`, that
       `MATCHUP` and `WL` are gone, and that `SESONG` equals `"2022-23"` on every row.

    3. `test_normaliser_gir_iso_dato_streng_uansett_inndatatype` — build one frame whose
       `GAME_DATE` values are strings (`"2022-10-24"`) and one whose values are
       `pd.Timestamp` objects for the same days; assert both normalize to the identical
       `"2022-10-24"` string and that every value in the column is an instance of `str`.

    4. `test_normaliser_dato_stotter_streng_sammenligning_som_as_of` — the property plan
       05-06 relies on. From a normalized 3-game frame, assert
       `(df["GAME_DATE"] < "2022-10-26").sum() == 1` and
       `(df["GAME_DATE"] < "2022-10-24").sum() == 0`. Docstring must name this as the
       boundary and strict-`<` contract, mirroring
       `tests/test_parity.py::test_grenserad_paa_as_of_er_ekskludert`.

    5. `test_normaliser_manglende_kolonne_reiser_valueerror` — drop `PLAYER_ID`, assert
       `pytest.raises(ValueError)` and that `"PLAYER_ID"` appears in `str(excinfo.value)`.

    6. `test_normaliser_null_minutter_blir_null_float` — one row with `MIN` set to `None`
       and one with a non-numeric string; assert both become `0.0` and the column dtype is
       float. Docstring: 0.0 is the conservative direction — it makes the downstream check
       treat the player as unavailable rather than manufacturing false confidence.

    7. `test_hent_spillerlogg_hopper_over_feilet_sesong` — inject a `hent_fn` returning
       `pd.DataFrame()` for `"2023-24"` and real synthetic frames for the other two; assert
       no exception is raised, `resultat["hoppet_over"] == ["2023-24"]`,
       `resultat["sesonger_hentet"] == 2`, and the returned frame's `SESONG` set is exactly
       `{"2022-23", "2024-25"}`.

    8. `test_hent_spillerlogg_hopper_over_allerede_hentede_sesonger` — the resume path. Pass
       an `eksisterende_df` containing only `"2022-23"` rows, plus a `hent_fn` that appends
       every requested season to a list; assert that list equals `["2023-24", "2024-25"]`
       (2022-23 is never refetched), that `resultat["allerede_hentet"] == 1`, and that the
       returned frame covers all three seasons.

    9. `test_hent_spillerlogg_alle_sesonger_feiler_gir_tom_ramme` — `hent_fn` always returns
       an empty frame; assert the returned DataFrame is empty, `hoppet_over` lists all three
       seasons, and still no exception is raised.

    10. `test_lagre_og_les_bevarer_dato_som_streng` — round-trip through
        `tmp_path / "spillerlogg.csv"` using `lagre_spillerlogg` then `les_spillerlogg`;
        assert the read-back `GAME_DATE` values are `str`, equal the written values, and that
        `(lest["GAME_DATE"] < "2022-10-26").sum()` matches the pre-write count. This guards
        against pandas silently re-inferring the column as datetime on read.

    11. `test_les_spillerlogg_manglende_fil_gir_forklarende_feil` — call `les_spillerlogg` on
        a non-existent path inside `tmp_path`; assert `pytest.raises(FileNotFoundError)` and
        that `"spillerlogg.py"` appears in the message.

    Every test must pass without network access. Do not import `nba_api` endpoints anywhere
    except inside test 1's monkeypatch block. Do not add fixtures to `tests/conftest.py` —
    keep this file's fixtures local, since no other test file needs a player log.
  </action>

  <acceptance_criteria>
    - `python3 -m pytest tests/test_spillerlogg.py -q` passes with zero failures
    - `python3 -m pytest tests/test_spillerlogg.py --collect-only -q | grep -c '::test_'` outputs `11` or more
    - `python3 -m pytest tests/ -q` passes — the full suite (existing tests plus these) is green
    - `grep -c 'hent_fn=' tests/test_spillerlogg.py` outputs `3` or more
    - `grep -c 'datetime.now\|import random' tests/test_spillerlogg.py` outputs `0`
    - The suite passes with no network reachable: no test constructs a real `LeagueGameLog` object
  </acceptance_criteria>

  <verify>
    <automated>python3 -m pytest tests/test_spillerlogg.py -q</automated>
  </verify>

  <done>Eleven no-network tests cover the column schema, the ISO-string date contract, strict-`<` as-of comparability, loud missing-column failure, null-`MIN` coercion, skip-and-log, resume, all-seasons-failed, CSV round-trip and the missing-file message; the full suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Run the three free nba_api fetches and land nba_spillerlogg_raw.csv</name>

  <read_first>
    - `spillerlogg.py` (written in Task 1) — `main()`'s exit-code contract, before running it
    - `.planning/phases/05-walk-forward-backtest-engine/05-RESEARCH.md` §"Environment Availability" — the verified 25,895-rows-per-season figure and the documented fallback if `nba_api` is unreachable
    - `.gitignore` — confirm plan 05-01 already landed the exact line `nba_spillerlogg_raw.csv`
  </read_first>

  <files>nba_spillerlogg_raw.csv</files>

  <action>
    Run `python3 spillerlogg.py` from the repo root. This makes exactly three `nba_api` calls
    (one per season), spaced by `PAUSE_SEKUNDER`. `nba_api` is free and keyless — no Odds API
    credit is consumed and no `.env` value is read.

    Before running, confirm `git check-ignore -q nba_spillerlogg_raw.csv` exits 0. If it does
    not, stop and report: plan 05-01's `.gitignore` rule is missing, and a roughly 75,000-row
    CSV must not be created until that rule lands.

    Expected result per `05-RESEARCH.md`'s live verification: roughly 25,000-27,000 rows per
    season, so about 75,000-80,000 rows total, spanning `2022-10-18` through roughly
    `2025-04-13`, with three distinct `SESONG` values.

    Handling a non-zero exit:
    - If `main()` exits 1 with a non-empty `hoppet_over` list (one or two seasons failed),
      re-run `python3 spillerlogg.py` once — the resume path refetches only the missing
      seasons. If a season still fails after that retry, do not fake it and do not declare
      the task done: record the exact failing season and the printed nba_api error in the
      SUMMARY under an explicit "Ufullstendig arkiv" heading, because plan 05-06 must know
      which date ranges have no player-level coverage.
    - If `main()` exits 1 because the frame was empty (all three seasons failed, i.e.
      `nba_api` is unreachable), the CSV is deliberately not written. Stop and report this as
      a blocked task rather than working around it. `05-RESEARCH.md`'s documented fallback
      (disable the injury filter and flag the bias in every manifest) is a scope decision for
      the developer, not something this plan may take unilaterally.

    After a successful run, verify the archive with a single read-back and record these four
    numbers in the SUMMARY: total row count, per-`SESONG` row counts, `GAME_DATE` min and max,
    and the number of distinct `PLAYER_ID` values.

    Do not modify `spillerlogg.py` in this task unless a verification below fails and the
    cause is a real defect in the module. In that case fix the module, re-run Task 2's test
    command, and note the fix in the SUMMARY.

    Do not commit `nba_spillerlogg_raw.csv` — it is git-ignored by design. This task's commit
    carries only source or test files, if any changed.
  </action>

  <acceptance_criteria>
    - `git check-ignore -q nba_spillerlogg_raw.csv` exits 0
    - `test -f nba_spillerlogg_raw.csv` exits 0
    - `git status --porcelain nba_spillerlogg_raw.csv` produces no output — the file is ignored and never staged
    - `head -1 nba_spillerlogg_raw.csv` equals `SESONG,PLAYER_ID,PLAYER_NAME,TEAM_ID,GAME_ID,GAME_DATE,MIN`
    - `python3 -c "import spillerlogg as s; d=s.les_spillerlogg(); assert len(d) in range(60000, 200000); assert sorted(d['SESONG'].unique()) == ['2022-23','2023-24','2024-25']; assert d['PLAYER_ID'].isna().sum() == 0; assert d['GAME_DATE'].isna().sum() == 0; print(len(d), d['GAME_DATE'].min(), d['GAME_DATE'].max(), d['PLAYER_ID'].nunique())"` exits 0
    - `python3 -c "import spillerlogg as s; d=s.les_spillerlogg(); v=d.groupby('SESONG').size(); assert v.min() in range(15000, 40000); print(v.to_dict())"` exits 0
    - `python3 -c "import spillerlogg as s; d=s.les_spillerlogg(); assert d['GAME_DATE'].min() in [str(x.date()) for x in __import__('pandas').date_range('2022-10-01','2022-11-15')]; print(d['GAME_DATE'].min())"` exits 0 — the archive starts inside the 2022-23 season opening window, not at some stray date
    - `python3 -m pytest tests/ -q` passes
  </acceptance_criteria>

  <verify>
    <automated>python3 -c "import spillerlogg as s; d=s.les_spillerlogg(); assert len(d) in range(60000, 200000); assert sorted(d['SESONG'].unique()) == ['2022-23','2023-24','2024-25']; print(len(d), d['GAME_DATE'].min(), d['GAME_DATE'].max())" &amp;&amp; python3 -m pytest tests/ -q</automated>
  </verify>

  <done>`nba_spillerlogg_raw.csv` exists on disk, is git-ignored and unstaged, carries the exact seven-column header, holds 60,000+ player-game rows across all three seasons with no null `PLAYER_ID`/`GAME_DATE`, and the full pytest suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stats.nba.com (`nba_api`) → local process | Untrusted external JSON crosses here. It is unauthenticated, keyless, free, and its player-mode column set is not declared in the package's own `expected_data` — so its schema is a runtime assumption, not a contract. |
| local process → `nba_spillerlogg_raw.csv` | Whatever lands in this file becomes plan 05-06's only source of injury truth and therefore silently shapes every ROI number BT-01 reports. |
| working tree → git history | The output CSV is ~75k rows; committing it is effectively irreversible repo bloat. |

No secrets, no credentials, no Odds API credit, no money movement, and no user-supplied
input are involved — consistent with `05-RESEARCH.md` §Security Domain's assessment that
this phase adds no new external attack surface. ASVS V2/V3/V4/V6 do not apply; V5 applies
only marginally, to the shape of the data crossing the first boundary above.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | nba_api response schema → `normaliser_spillerlogg` | mitigate | Task 1 step (a) validates every name in `KILDEKOLONNER` and raises `ValueError` naming the missing ones. Without this, a renamed upstream column would yield an empty groupby in plan 05-06 and pass every team unconditionally — Pitfall 1's exact silent-bias failure. Covered by `test_normaliser_manglende_kolonne_reiser_valueerror`. |
| T-05-05-02 | Tampering | `MIN` column coercion | mitigate | Nulls and unparseable values become `0.0`, which biases the downstream check toward "unavailable" (bet flagged uncertain). The opposite default would manufacture false confidence about a player who may not have played. Covered by `test_normaliser_null_minutter_blir_null_float`. |
| T-05-05-03 | Spoofing | An empty or partially-empty archive masquerading as complete | mitigate | `main()` refuses to write the CSV at all when the frame is empty and exits 1; a partial run writes what it has but still exits 1 with `hoppet_over` populated, and Task 3 requires the failing season to be recorded in the SUMMARY. Covered by `test_hent_spillerlogg_alle_sesonger_feiler_gir_tom_ramme`. |
| T-05-05-04 | Information Disclosure | `nba_spillerlogg_raw.csv` entering git history | mitigate | Plan 05-01 (wave 1) landed the `.gitignore` rule before this file can exist; Task 3 re-verifies with `git check-ignore` before fetching and asserts `git status --porcelain` stays empty for the path. The data itself is public NBA box-score data, so the real risk is irreversible repo bloat rather than secret exposure. |
| T-05-05-05 | Denial of Service | Repeated hammering of the free, unauthenticated nba_api endpoint | mitigate | `time.sleep(PAUSE_SEKUNDER)` between calls (same courtesy `01_hent_data.py:48` and `skadefilter.py:62` already apply), plus the `eksisterende_df` resume path, so a re-run refetches only missing seasons instead of all three. Covered by `test_hent_spillerlogg_hopper_over_allerede_hentede_sesonger`. |
| T-05-05-06 | Tampering | Date semantics drifting between `nba_features.csv` and the player log | mitigate | `GAME_DATE` is forced to a `YYYY-MM-DD` string, identical to `nba_features.csv`'s `GAME_DATE_HJEMME`, so plan 05-06's `< as_of` string comparison is a real as-of filter rather than an accidental datetime/string mismatch that silently compares nothing. Covered by `test_normaliser_dato_stotter_streng_sammenligning_som_as_of` and `test_lagre_og_les_bevarer_dato_som_streng`. |
| T-05-05-SC | Tampering | npm/pip/cargo installs | accept | No package is installed by this plan and none is installed anywhere in Phase 5. `05-RESEARCH.md` §Package Legitimacy Audit records that every library used here (`pandas`, `nba_api`, stdlib `time`/`sys`) is already an installed, in-use project dependency. No legitimacy checkpoint applies. |
</threat_model>

<verification>
Run after all three tasks:

1. `python3 -m pytest tests/ -q` — full suite green (existing tests plus the 11 new ones)
2. `python3 -c "import ast; t=ast.parse(open('spillerlogg.py').read()); assert all(n.__class__.__name__ in ('Expr','Import','ImportFrom','Assign','FunctionDef','If') for n in t.body); print('ingen modulnivaa-kall')"`
3. `python3 -c "import spillerlogg as s; d=s.les_spillerlogg(); print(len(d)); print(d.groupby('SESONG').size().to_dict()); print(d['GAME_DATE'].min(), d['GAME_DATE'].max()); print(list(d.columns))"`
4. `git check-ignore -q nba_spillerlogg_raw.csv &amp;&amp; git status --porcelain nba_spillerlogg_raw.csv &amp;&amp; echo IGNORERT-OG-USTAGET`
5. `git diff --name-only` — lists only `spillerlogg.py` and `tests/test_spillerlogg.py`; no `skadefilter.py`, no `odds.py`, no `verdi_deteksjon.py`, no `tests/test_odds.py` (plan 05-04 owns those in this same wave)
</verification>

<success_criteria>
- `spillerlogg.py` exists, imports without any network activity, and exposes
  `hent_sesong_logg`, `normaliser_spillerlogg`, `hent_spillerlogg`, `lagre_spillerlogg`,
  `les_spillerlogg` and `main`
- A failed single-season fetch is skipped and logged; the remaining seasons still land, and
  a re-run refetches only the missing ones
- An all-seasons-failed run writes nothing and exits non-zero
- `tests/test_spillerlogg.py` holds 11 deterministic, network-free tests and is green
- `nba_spillerlogg_raw.csv` exists with 60,000+ rows across all three seasons, header
  `SESONG,PLAYER_ID,PLAYER_NAME,TEAM_ID,GAME_ID,GAME_DATE,MIN`, `GAME_DATE` as
  `YYYY-MM-DD` strings, and no null `PLAYER_ID`/`GAME_DATE`
- The file is git-ignored and never staged
- Zero Odds API credits were spent; exactly three free `nba_api` calls were made (plus at
  most three more if a retry was needed)
- `python3 -m pytest tests/ -q` is green
- Pitfall 1's data gap is closed: plan 05-06 now has a real player-level, as-of-filterable
  source, so BT-01's injury-filter stage can be replayed rather than disabled
</success_criteria>

<output>
Create `.planning/phases/05-walk-forward-backtest-engine/05-05-SUMMARY.md` when done.

The summary MUST include a `## Spillerlogg-arkiv` section recording the total row count,
per-`SESONG` row counts, the `GAME_DATE` min/max, and the distinct `PLAYER_ID` count —
plan 05-06 reads these as its upstream data contract and needs to know exactly which
seasons and date range are covered.

If any season was skipped, the summary MUST additionally carry an `## Ufullstendig arkiv`
heading naming the season and the nba_api error, so plan 05-06 knows which date ranges have
no player-level coverage and must be treated as injury-filter-blind.
</output>
