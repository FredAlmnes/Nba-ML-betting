---
phase: 05-walk-forward-backtest-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-walk-forward-backtest-engine/05-BESLUTNINGER.md
  - config.py
  - .gitignore
  - tests/test_strategy.py
  - tests/test_oppsett.py
autonomous: false
requirements: [BT-03]

must_haves:
  truths:
    - "The developer has explicitly locked the exact holdout boundary date before any backtest code exists"
    - "The developer has explicitly locked the burn-in/early-months reporting policy that plan 05-08 will implement"
    - "The developer has explicitly locked what 'flat' means in the BT-07 Kelly sweep that plan 05-09 will implement"
    - "config.HOLDOUT_START_DATO is importable and equals the locked value"
    - "The seven live strategy constants in config.py are provably unchanged by this plan"
    - "backtests/ run artifacts and nba_spillerlogg_raw.csv are git-ignored before they are ever created"
    - "The full existing pytest suite is still green"
  artifacts:
    - path: ".planning/phases/05-walk-forward-backtest-engine/05-BESLUTNINGER.md"
      provides: "Machine-greppable record of D-05-01..D-05-04, read by plans 05-07/05-08/05-09"
      contains: "| D-05-01 |"
    - path: "config.py"
      provides: "HOLDOUT_START_DATO constant — the single code-resident holdout boundary"
      contains: "HOLDOUT_START_DATO"
    - path: ".gitignore"
      provides: "Ignore rules for backtests/ and nba_spillerlogg_raw.csv"
      contains: "backtests/"
    - path: "tests/test_strategy.py"
      provides: "Tripwire assertion locking HOLDOUT_START_DATO's exact value"
      contains: "HOLDOUT_START_DATO"
    - path: "tests/test_oppsett.py"
      provides: "Assertion that the two new .gitignore entries are present"
      contains: "nba_spillerlogg_raw.csv"
  key_links:
    - from: "tests/test_strategy.py::test_config_values"
      to: "config.HOLDOUT_START_DATO"
      via: "import config + equality assertion"
      pattern: "config\\.HOLDOUT_START_DATO"
    - from: ".gitignore"
      to: "backtests/<run_id>/manifest.json"
      via: "git check-ignore"
      pattern: "^backtests/"
---

<objective>
Lock the three open decisions that every later Phase 5 plan depends on, then land the two
one-line infrastructure changes those decisions imply.

Nothing downstream in this phase can be written safely until the holdout boundary is a
fixed value: plan 05-07's `_sikre_ikke_holdout()` compares against it, plan 05-09's Kelly
sweep raises on it, and plan 05-13 spends it exactly once and irreversibly. `05-RESEARCH.md`
left three items explicitly unresolved (Assumption A1, Open Question 3, Pitfall 6) and each
one changes generated code, not just prose. This plan resolves them at a blocking
checkpoint, records them in a greppable phase artifact, and then adds
`HOLDOUT_START_DATO` to `config.py` plus the two `.gitignore` entries that keep
backtest run output and the new player-game log out of git.

Purpose: BT-03's "enforced by the code, not just convention" needs a constant to enforce
against, and that constant's value is a developer decision — not a planner default.
Output: `05-BESLUTNINGER.md`, `config.HOLDOUT_START_DATO`, two `.gitignore` rules, and two
new test assertions locking both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-walk-forward-backtest-engine/05-CONTEXT.md
@.planning/phases/05-walk-forward-backtest-engine/05-RESEARCH.md
@.planning/phases/05-walk-forward-backtest-engine/05-PATTERNS.md

<interfaces>
<!-- Current contracts the executor edits. No codebase exploration needed. -->

config.py — full current constant block (7 live values, all locked by a tripwire test):
```python
MIN_VALUE_TERSKEL = 0.05            # Flagg bets der vi er 5%+ over bookmaker
MIN_ODDS = 1.50                     # Ikke bett på favoritter med veldig lave odds
MAX_ODDS = 4.00                     # Ikke bett på store outsidere (over 4x = for usikkert)

KELLY_FRAKSJON = 0.5      # Halvt Kelly (konservativt – reduserer varians)
MAX_INNSATS = 150.0    # Aldri mer enn 150 kr på ett bet
MIN_INNSATS = 20.0     # Aldri mindre enn 20 kr
STARTKAPITAL = 1000.0   # kr
```

tests/test_strategy.py — the existing tripwire this plan extends (lines 25-37):
```python
def test_config_values():
    # D-07 låser disse verdiene til Phase 5s backtest har validert alternativer.
    assert config.MIN_VALUE_TERSKEL == 0.05
    assert config.MIN_ODDS == 1.50
    ...
    assert config.STARTKAPITAL == 1000.0
```

tests/test_strategy.py — the source-reading assertion style to mirror (lines 40-48):
```python
def test_config_har_ingen_hemmeligheter():
    config_kilde = (pathlib.Path(__file__).resolve().parent.parent / "config.py").read_text(
        encoding="utf-8"
    )
    assert "ODDS_API" not in config_kilde
```

tests/test_oppsett.py — full current contents:
```python
def test_repo_rot_er_importerbar():
    import modell_utils
    assert hasattr(modell_utils, "KalibrertModell")
```

strategy.py::beregn_innsats — the signature the "flat" decision (D-05-03) must NOT change:
```python
def beregn_innsats(saldo, modell_prob, odds, kelly_fraksjon, min_innsats, max_innsats)
```
Returns 0.0 for non-positive Kelly edge, so `kelly_fraksjon=0` yields 0.0 for every bet —
it is not a flat-stake mode (05-RESEARCH.md Pitfall 6).
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Blocking pre-flight decision gate — holdout date, burn-in policy, flat-stake definition, scratch disposition</name>

  <read_first>
    - `.planning/phases/05-walk-forward-backtest-engine/05-RESEARCH.md` — Assumptions Log A1 (holdout date), Open Questions 2 and 3, Common Pitfalls 4 and 6
    - `.planning/phases/05-walk-forward-backtest-engine/05-CONTEXT.md` — "Walk-Forward Retraining & Holdout Definition" and "Post-Research Resolution" items 2 and 3
    - `.planning/STATE.md` — "Blockers/Concerns", specifically the deferred D-08 scratch-artifact entry
  </read_first>

  <files>.planning/phases/05-walk-forward-backtest-engine/05-BESLUTNINGER.md</files>

  <decision>
Four items must be locked before any Phase 5 code is written. Present all four in one
message, with the recommended answer marked, and wait for a single combined response.

**D-05-01 — Exact value of `HOLDOUT_START_DATO`**
The 2024-25 season is the locked, spend-once holdout. Its constant value is unresolved
(05-RESEARCH.md Assumption A1 / Open Question 2). Once plan 05-13 runs, this is
irreversible for the milestone.

**D-05-02 — Burn-in / early-months reporting policy**
The walk-forward loop's first months train on ~1-2 weeks of games. Phase 3 already
found that even a 172-row calibration set produced worse-than-uncalibrated log-loss on
this data, so the earliest months will be noisy. Policy must be decided now because
plan 05-08 writes the manifest fields it implies.

**D-05-03 — Definition of "flat" in the BT-07 Kelly sweep**
`strategy.beregn_innsats` has no flat-stake mode; `kelly_fraksjon=0` returns 0.0 for every
bet, not a flat stake (05-RESEARCH.md Pitfall 6). Plan 05-09 needs an exact formula.
`strategy.py` is shared with the live bot and must not be modified for this.

**D-05-04 — Scratch-artifact and working-tree disposition (confirmation, not a new choice)**
`_linux_pkgs/`, `_pip_tmp/`, `_pip_home/`, `_wheels/`, `_test.bin`, `test_write.tmp` are
still present on disk and still gitignore-only per Phase 1's D-08. `git status` is
otherwise clean (only `.planning/config.json` modified and this phase's untracked
planning docs). Confirming this keeps Phase 5's commits scoped.
  </decision>

  <context>
Every one of these four produces different generated code or different committed state.
Deferring any of them means a later plan either guesses or stalls mid-wave:

- D-05-01 is read by plan 05-07 (`_sikre_ikke_holdout`), 05-09 (sweep guard), 05-10
  (`--holdout` CLI path) and 05-13 (the single holdout run).
- D-05-02 is read by plan 05-08 (which manifest metric fields exist) and 05-12 (what the
  human-verify checkpoint judges as plausible).
- D-05-03 is read by plan 05-09 (the sweep's fourth entry).
- D-05-04 governs whether Task 3's `.gitignore` edit is the only repo-state change this
  plan makes.
  </context>

  <options>
    <option id="d-05-01-a">
      <name>D-05-01 = "2024-10-01" (RECOMMENDED — 05-RESEARCH.md A1, 05-CONTEXT.md)</name>
      <pros>Clean calendar-month boundary; unambiguous to read in code and in the manifest; behaviourally identical to 2024-10-22 because the 2024-10-01..2024-10-21 window contains zero games in nba_features.csv (verified in research); matches the phrase "2024-10 through 2025-04-13" already written into 05-CONTEXT.md</pros>
      <cons>Names a date on which no NBA game was played, so a reader may briefly wonder whether ~3 weeks of data were discarded (they do not exist)</cons>
    </option>
    <option id="d-05-01-b">
      <name>D-05-01 = "2024-10-22" (first actual 2024-25 game date)</name>
      <pros>Every date in the holdout range corresponds to real games</pros>
      <cons>Reads as an arbitrary magic number; must be re-derived if nba_features.csv is ever regenerated; produces the exact same train/calibrate slice as option A</cons>
    </option>
    <option id="d-05-02-a">
      <name>D-05-02 = include all months in the ledger, report headline metrics twice — full-period AND excluding the first 2-3 months (RECOMMENDED — 05-RESEARCH.md Open Question 3, 05-CONTEXT.md Post-Research Resolution item 3)</name>
      <pros>No data is dropped, so bankroll and drawdown stay continuous; no appearance of cherry-picking the window; both numbers are just different date filters over the same cached ledger, so the cost is near zero; the burn-in sensitivity becomes visible instead of hidden</pros>
      <cons>Two ROI/CI pairs in manifest.json instead of one — the reader must be told which is headline (specify: full-period is headline, ex-burn-in is the sensitivity check)</cons>
    </option>
    <option id="d-05-02-b">
      <name>D-05-02 = include all months, report full-period metrics only</name>
      <pros>Single unambiguous headline number</pros>
      <cons>Hides how much of the reported ROI comes from the noisiest, smallest-training-window months — the exact question Phase 3's calibration finding raises</cons>
    </option>
    <option id="d-05-03-a">
      <name>D-05-03 = flat stake is a backtest.py-local branch: a fixed 2% of config.STARTKAPITAL (20.0 kr) per bet, applied whenever the sweep's fraction label is "flat", with strategy.py untouched (RECOMMENDED — 05-RESEARCH.md Pitfall 6)</name>
      <pros>Genuinely flat — independent of edge, odds and current bankroll, which is what a flat-stake baseline is supposed to isolate; keeps strategy.py's pure, live-shared contract byte-identical; 20.0 kr coincides with config.MIN_INNSATS so the flat arm is directly comparable to the Kelly arms' floor</pros>
      <cons>Introduces one staking rule that does not live in strategy.py — must be documented in backtest.py's docstring as deliberate, not an oversight</cons>
    </option>
    <option id="d-05-03-b">
      <name>D-05-03 = flat stake tracks the running bankroll (2% of current saldo)</name>
      <pros>Compounds like the Kelly arms, so the comparison isolates only edge-scaling</pros>
      <cons>Not actually flat; reintroduces bankroll path-dependence into the arm that exists to remove it</cons>
    </option>
    <option id="d-05-04-a">
      <name>D-05-04 = unchanged, ignore-only (RECOMMENDED — matches Phase 1 D-08)</name>
      <pros>No destructive disk operation; consistent with the standing decision; Phase 5 commits stay scoped to Phase 5 files</pros>
      <cons>The ~100MB _test.bin and pip scratch trees stay on disk (already the accepted status quo)</cons>
    </option>
    <option id="d-05-04-b">
      <name>D-05-04 = delete the scratch artifacts from disk now</name>
      <pros>Reclaims disk; removes a long-standing source of confusion</pros>
      <cons>Destructive and entirely out of Phase 5's scope; deserves its own explicit decision, not a side effect of a backtest phase</cons>
    </option>
  </options>

  <action>
    Present all four decisions in a single message with the recommended option marked for
    each, then WAIT. Do not write any file before the developer responds.

    After the response, create
    `.planning/phases/05-walk-forward-backtest-engine/05-BESLUTNINGER.md` recording the
    outcomes. The file MUST contain a markdown table whose rows begin exactly with
    `| D-05-01 |` through `| D-05-04 |` (four rows, in order), with columns
    `ID | Beslutning | Låst verdi | Leses av`. The `Låst verdi` cell for D-05-01 MUST be the
    chosen date string in double quotes, e.g. `"2024-10-01"`. Below the table, add one short
    Norwegian paragraph per decision explaining the rationale in the developer's own terms,
    following the same "why, not what" convention CLAUDE.md documents for code comments.

    If the developer chooses any non-recommended option, use their choice verbatim in the
    table and carry it into Task 2 — the recommended values are defaults, not constraints.
    If D-05-01 comes back as anything other than `"2024-10-01"`, that same value must be
    used in Task 2's `config.py` line and in Task 2's tripwire assertion.

    If D-05-04 comes back as option b (delete), do NOT delete anything in this plan —
    record the decision in the table and stop to report it, because deletion is out of this
    plan's declared `files_modified` scope and needs its own plan.
  </action>

  <acceptance_criteria>
    - `.planning/phases/05-walk-forward-backtest-engine/05-BESLUTNINGER.md` exists
    - `grep -c '^| D-05-0[1-4] |' .planning/phases/05-walk-forward-backtest-engine/05-BESLUTNINGER.md` outputs exactly `4`
    - `grep -c 'HOLDOUT_START_DATO' .planning/phases/05-walk-forward-backtest-engine/05-BESLUTNINGER.md` outputs `1` or more
    - `git status --porcelain` shows no modification to `strategy.py`, `config.py` or `.gitignore` yet (this task records decisions only)
  </acceptance_criteria>

  <verify>
    <automated>test $(grep -c '^| D-05-0[1-4] |' .planning/phases/05-walk-forward-backtest-engine/05-BESLUTNINGER.md) -eq 4 &amp;&amp; git status --porcelain | grep -qv 'strategy.py' &amp;&amp; echo OK</automated>
  </verify>

  <done>All four decisions are answered by the developer and recorded as four exactly-formatted table rows in 05-BESLUTNINGER.md; no source file has been touched yet.</done>

  <resume-signal>Answer all four: D-05-01 (date string), D-05-02 (a or b), D-05-03 (a or b), D-05-04 (a or b). Replying "anbefalt" / "recommended" accepts a, a, a, a with D-05-01 = "2024-10-01".</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add HOLDOUT_START_DATO to config.py and lock its value with the existing tripwire test</name>

  <read_first>
    - `.planning/phases/05-walk-forward-backtest-engine/05-BESLUTNINGER.md` — the D-05-01 row is the authoritative value; do not read it from research or context
    - `config.py` (21 lines, read in full) — constant block and docstring
    - `tests/test_strategy.py` lines 1-50 — `test_config_values` and `test_config_har_ingen_hemmeligheter`
    - `.planning/phases/05-walk-forward-backtest-engine/05-PATTERNS.md` §"`config.py` (modified — add `HOLDOUT_START_DATO`)" — the exact constant style to match
  </read_first>

  <files>config.py, tests/test_strategy.py</files>

  <action>
    In `config.py`, add a single module-level constant `HOLDOUT_START_DATO` whose value is
    the date string locked as D-05-01 in `05-BESLUTNINGER.md` (default `"2024-10-01"`).
    Place it after `STARTKAPITAL`, separated by one blank line, as a bare string literal
    with no type hint — matching every other constant in the file. Give it an inline
    trailing comment in Norwegian, per CLAUDE.md's "inline trailing comments annotate config
    constants with their practical meaning" convention, stating that dates on or after this
    value belong to the locked 2024-25 holdout and may only be evaluated through
    `kjor_endelig_holdout_backtest()` (BT-03).

    Extend `config.py`'s module docstring with two or three additional Norwegian sentences
    explaining that this file now also owns the holdout boundary, that changing
    `HOLDOUT_START_DATO` after plan 05-13 has spent the holdout invalidates the holdout's
    "checked exactly once" guarantee, and that the seven strategy values above it stay
    frozen until the backtest produces evidence (per 05-CONTEXT.md's Deferred Ideas).
    Do NOT introduce the literal text `ODDS_API` or `apiKey` anywhere — the existing
    `test_config_har_ingen_hemmeligheter` guard asserts their absence from this file's
    source and will fail otherwise.

    Do NOT change any of the seven existing values (`MIN_VALUE_TERSKEL` 0.05, `MIN_ODDS`
    1.50, `MAX_ODDS` 4.00, `KELLY_FRAKSJON` 0.5, `MAX_INNSATS` 150.0, `MIN_INNSATS` 20.0,
    `STARTKAPITAL` 1000.0). 05-CONTEXT.md's Deferred Ideas explicitly forbids it in this
    phase.

    In `tests/test_strategy.py::test_config_values`, append one assertion,
    `assert config.HOLDOUT_START_DATO == "<locked value>"`, below the existing
    `STARTKAPITAL` assertion. Extend that test's leading comment block with two Norwegian
    sentences explaining that this assertion is a deliberate tripwire of the same kind as
    the seven above it: BT-03 requires the holdout boundary to be enforced by code, and a
    silent edit to the constant would silently move the boundary, so any change must be made
    together with a deliberate change to this test. Add nothing else to the file.
  </action>

  <acceptance_criteria>
    - `grep -v '^#' config.py | grep -c 'HOLDOUT_START_DATO'` outputs `1`
    - `python3 -c "import config; print(config.HOLDOUT_START_DATO)"` prints the value recorded in the D-05-01 row of 05-BESLUTNINGER.md
    - `python3 -m pytest tests/test_strategy.py -q` passes with zero failures
    - `python3 -c "import config; assert (config.MIN_VALUE_TERSKEL, config.MIN_ODDS, config.MAX_ODDS, config.KELLY_FRAKSJON, config.MAX_INNSATS, config.MIN_INNSATS, config.STARTKAPITAL) == (0.05, 1.50, 4.00, 0.5, 150.0, 20.0, 1000.0)"` exits 0
    - `git diff --stat config.py tests/test_strategy.py` shows exactly two files changed and no other file
  </acceptance_criteria>

  <verify>
    <automated>python3 -m pytest tests/test_strategy.py -q</automated>
  </verify>

  <done>`config.HOLDOUT_START_DATO` imports and equals the D-05-01 value, `test_config_values` asserts it, and the seven live strategy constants are provably unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Gitignore backtests/ and nba_spillerlogg_raw.csv before either artifact exists, with a test asserting both rules</name>

  <read_first>
    - `.gitignore` (39 lines, read in full) — existing section headers and Norwegian comment style
    - `tests/test_oppsett.py` (11 lines, read in full) — the file this task extends
    - `tests/test_strategy.py` lines 40-48 — the `pathlib`-read-the-source assertion pattern to mirror
    - `.planning/phases/05-walk-forward-backtest-engine/05-CONTEXT.md` §"Package & Module Structure" — why `backtests/` must stay structurally separate from `bankroll.json`/`bets.json`
  </read_first>

  <files>.gitignore, tests/test_oppsett.py</files>

  <action>
    Append one new section to `.gitignore`, placed after the existing "Bankroll og
    bet-historikk" block and before the "Dashboard" block, with a Norwegian header comment
    in the same `# ...` style as the file's other sections. The section adds exactly two
    rules: `backtests/` (directory form, trailing slash) and `nba_spillerlogg_raw.csv`.
    The header comment must state why both exist: `backtests/` holds simulated run output
    (manifest.json, ledger.csv, kelly_sweep.json) that must never be mistaken for or mixed
    with the real paper-trading state in `bankroll.json`/`bets.json`, and
    `nba_spillerlogg_raw.csv` is a large regenerable nba_api artifact of the same class as
    the already-ignored `nba_kamper_raw.csv`.

    Both rules must land before plan 05-05 creates `nba_spillerlogg_raw.csv` and before plan
    05-08 creates the first `backtests/<run_id>/` directory — that ordering is the whole
    reason this sits in wave 1.

    Change nothing else in `.gitignore`; the six scratch-artifact rules at the bottom stay
    exactly as they are per D-05-04.

    In `tests/test_oppsett.py`, add one test named
    `test_backtest_artefakter_er_gitignorert` that reads `.gitignore` from the repo root via
    `pathlib.Path(__file__).resolve().parent.parent`, splits it into stripped non-empty
    lines, and asserts that `"backtests/"` and `"nba_spillerlogg_raw.csv"` are both present
    as exact line entries — matching against whole lines, not substrings, so a rule buried
    inside a comment cannot satisfy the test. Give it a Norwegian docstring explaining that
    these two rules must exist before the artifacts do, because a committed backtest ledger
    or a 77k-row player log is far harder to remove from history than to prevent. Add the
    `pathlib` import at the top of the file if it is not already there. Do not modify the
    existing `test_repo_rot_er_importerbar`.
  </action>

  <acceptance_criteria>
    - `git check-ignore -q backtests/20240101-000000-abc/manifest.json` exits 0
    - `git check-ignore -q nba_spillerlogg_raw.csv` exits 0
    - `grep -c '^backtests/$' .gitignore` outputs `1`
    - `grep -c '^nba_spillerlogg_raw.csv$' .gitignore` outputs `1`
    - `grep -c '^_test.bin$' .gitignore` outputs `1` (scratch rules untouched, D-05-04)
    - `python3 -m pytest tests/test_oppsett.py -q` passes with 2 tests collected
    - `python3 -m pytest tests/ -q` passes — the full suite (129 existing + the 1 new test) is green
  </acceptance_criteria>

  <verify>
    <automated>git check-ignore -q backtests/x/manifest.json &amp;&amp; git check-ignore -q nba_spillerlogg_raw.csv &amp;&amp; python3 -m pytest tests/ -q</automated>
  </verify>

  <done>Both future artifact paths resolve as ignored via `git check-ignore`, a test asserts both rules as exact `.gitignore` lines, and the full pytest suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree → git history | `config.py` and `.gitignore` are committed; anything written into them is permanent and public-to-any-clone. This is the only boundary this plan crosses. |
| developer decision → generated code | The D-05-01 value flows directly into a constant that four later plans branch on; a misrecorded value silently changes what the holdout is. |

No network boundary, no user input, no secrets, no money movement is introduced by this
plan — consistent with `05-RESEARCH.md` §Security Domain's assessment that this phase adds
no new external attack surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | `config.py` (git-committed) | mitigate | Task 2's action forbids the literal strings `ODDS_API`/`apiKey`; the pre-existing `tests/test_strategy.py::test_config_har_ingen_hemmeligheter` reads config.py's source and asserts their absence, and Task 2's acceptance criteria run that test |
| T-05-02 | Tampering | `config.HOLDOUT_START_DATO` | mitigate | Task 2 adds an equality assertion in `test_config_values`, so silently moving the holdout boundary (which would retroactively invalidate BT-03's spend-once guarantee after plan 05-13) fails the suite; changing it requires a deliberate, reviewable test edit — the same tripwire mechanism D-07 already applies to the seven live strategy values |
| T-05-03 | Information Disclosure | `backtests/<run_id>/` and `nba_spillerlogg_raw.csv` | mitigate | Task 3 lands both `.gitignore` rules in wave 1, strictly before plan 05-05 creates the player log and plan 05-08 creates the first run directory; verified with `git check-ignore` on not-yet-existing paths plus an exact-line assertion in `tests/test_oppsett.py` |
| T-05-04 | Repudiation | Phase 5 decision provenance | mitigate | `05-BESLUTNINGER.md`'s fixed four-row table format makes the locked values greppable by plans 05-07/05-08/05-09, so no later plan can quietly substitute a different holdout date, burn-in policy or flat-stake formula and claim it was the decision |
| T-05-05 | Tampering | `strategy.py` (live-shared pure module) | mitigate | D-05-03's recommended option confines the flat-stake rule to a `backtest.py`-local branch; `strategy.py` is deliberately absent from this plan's `files_modified`, and Task 1's acceptance criteria assert `git status --porcelain` shows it unmodified |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package is installed by this plan and none is installed anywhere in Phase 5 — `05-RESEARCH.md` §Package Legitimacy Audit records that every needed library (pandas, numpy, xgboost, scikit-learn, sqlite3) is already an installed, in-use dependency, and the Wilson z-score is a hardcoded constant specifically to avoid adding `scipy`. No legitimacy checkpoint applies. |
</threat_model>

<verification>
Run after all three tasks:

1. `python3 -m pytest tests/ -q` — full suite green (129 existing + 1 new test in
   `test_oppsett.py`; `test_config_values` gains one assertion rather than a new test)
2. `python3 -c "import config; print(config.HOLDOUT_START_DATO)"` — prints the D-05-01 value
3. `git check-ignore -q backtests/x/manifest.json && git check-ignore -q nba_spillerlogg_raw.csv && echo IGNORERT`
4. `grep -c '^| D-05-0[1-4] |' .planning/phases/05-walk-forward-backtest-engine/05-BESLUTNINGER.md` — outputs `4`
5. `git diff --name-only` — lists only `config.py`, `.gitignore`, `tests/test_strategy.py`,
   `tests/test_oppsett.py` (plus the untracked `05-BESLUTNINGER.md`); no `strategy.py`, no
   scratch-artifact deletions
</verification>

<success_criteria>
- D-05-01, D-05-02, D-05-03 and D-05-04 are answered by the developer and recorded as four
  exactly-formatted rows in `05-BESLUTNINGER.md`, readable by plans 05-07 through 05-13
- `config.HOLDOUT_START_DATO` exists, imports, and equals the locked D-05-01 value
- `tests/test_strategy.py::test_config_values` asserts that value, so it cannot drift silently
- The seven live strategy constants are byte-identical to their pre-plan values
- `backtests/` and `nba_spillerlogg_raw.csv` are git-ignored, proven with `git check-ignore`
  against paths that do not yet exist, and asserted as exact lines by a new test
- `python3 -m pytest tests/ -q` is green
- BT-03 now has a code-resident boundary constant for plan 05-07's structural guard to
  enforce against
</success_criteria>

<output>
Create `.planning/phases/05-walk-forward-backtest-engine/05-01-SUMMARY.md` when done.

The summary MUST restate the four locked decisions verbatim (including the exact
`HOLDOUT_START_DATO` string) under a `## Låste beslutninger` heading, because plans 05-07,
05-08 and 05-09 read this summary as their upstream contract.
</output>
