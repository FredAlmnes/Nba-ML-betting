---
phase: 04-historical-odds-acquisition-live-refactor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skadefilter.py
  - 05_skadefilter.py
  - tests/test_skadefilter.py
autonomous: true
requirements: [ODDS-02]

must_haves:
  truths:
    - "Importing the injury filter no longer triggers any NBA API network call"
    - "The injury-filter decision logic can be called as a function with injected player data"
    - "05_skadefilter.py still runs standalone and writes the same value_bets_med_skadefilter.csv it always did"
  artifacts:
    - path: "skadefilter.py"
      provides: "Importable injury-filter core (filtrer_bets_for_skader, sjekk_lag_helse, hent_spillerstatistikk)"
      contains: "def filtrer_bets_for_skader"
      min_lines: 120
    - path: "05_skadefilter.py"
      provides: "Thin CLI wrapper preserving standalone behaviour"
      contains: "if __name__ == \"__main__\":"
    - path: "tests/test_skadefilter.py"
      provides: "No-network-on-import proof and injected-data filter tests"
  key_links:
    - from: "05_skadefilter.py"
      to: "skadefilter.filtrer_bets_for_skader"
      via: "import + call inside __main__ guard"
      pattern: "from skadefilter import"
    - from: "skadefilter.py"
      to: "teams.finn_lag_id"
      via: "shared team resolver (no new lookup dict)"
      pattern: "from teams import finn_lag_id"
---

<objective>
Extract `05_skadefilter.py`'s core logic into an importable `skadefilter.py`, following the exact `features.py`/`strategy.py`/`teams.py` precedent from Phase 2 (D-05). Today the entire file body — including four `nba_api` network calls — executes at import time, which is precisely why `06_bot.py` had to shell out to it instead of importing it.

Purpose: This is the half of ODDS-02 with zero dependency on the odds work, so it runs in Wave 1 alongside the archive layer. `06_bot.py` (plan 04-08) cannot import the injury filter until this exists.
Output: `skadefilter.py` (new), `05_skadefilter.py` reduced to a thin CLI wrapper, `tests/test_skadefilter.py` (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-historical-odds-acquisition-live-refactor/04-CONTEXT.md
@.planning/phases/04-historical-odds-acquisition-live-refactor/04-RESEARCH.md
@05_skadefilter.py
@teams.py
@features.py

<interfaces>
<!-- Contracts this plan CREATES. Plan 04-08 (06_bot.py refactor) calls filtrer_bets_for_skader directly. -->

skadefilter.py (new module, repo root):

```python
ANTALL_TOPPSPILLERE = 3
MIN_MINUTTER = 20

def gjeldende_sesong():                                   # uendret logikk fra 05_skadefilter.py:_gjeldende_sesong
def hent_spillerdata(season_type, sesong, last_n=0):      # var _hent_spillerdata, tar nå sesong som argument
def hent_spillerstatistikk(sesong=None):                  # -> (siste3, sesong_snitt), gjør de 4 nba_api-kallene
def hent_toppspillere_for_lag(sesong_snitt, team_id, antall=ANTALL_TOPPSPILLERE)
def sjekk_spiller(siste3, spiller_id, spiller_navn, sesong_min)   # -> (ok, melding)
def sjekk_lag_helse(siste3, sesong_snitt, team_id, lagnavn)       # -> dict(lagnavn, tilgjengelig, advarsler)
def filtrer_bets_for_skader(value_df, siste3=None, sesong_snitt=None)  # -> DataFrame m/ Skadestatus + Skadeinfo
def skriv_skadefilter_csv(resultat_df, sti="value_bets_med_skadefilter.csv")
```

Existing shared contract this module MUST reuse (do not rebuild a lookup dict — Phase 2 D-03):
```python
# teams.py
def finn_lag_id(navn):   # -> int | None
```

Output columns appended by filtrer_bets_for_skader (exact strings, consumed by 06_bot.py's
`df[df["Skadestatus"].str.contains("OK")]` filter — changing them breaks the bot):
  "Skadestatus" in {"✅ OK", "⚠️  USIKKER"}   (note: two spaces after the warning emoji)
  "Skadeinfo"   free text, " | "-joined warnings
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Opprett skadefilter.py med all beslutningslogikk som importerbare funksjoner</name>
  <files>skadefilter.py, tests/test_skadefilter.py</files>
  <read_first>
    - 05_skadefilter.py (all 245 lines — every behaviour below must be preserved verbatim)
    - teams.py (finn_lag_id contract and the "returnerer None for ukjente navn" note)
    - features.py (module docstring style for an extracted shared module)
    - tests/conftest.py (fixture conventions; note it builds pandas DataFrames inline with no randomness)
  </read_first>
  <behavior>
    - `import skadefilter` performs zero `nba_api` calls — proven by reloading the module with `leaguedashplayerstats.LeagueDashPlayerStats` monkeypatched to a function that raises `AssertionError`
    - `sjekk_spiller` returns `(False, ...)` when the player is absent from the last-3 frame, and the message contains the player name and "0 kamper siste 3"
    - `sjekk_spiller` returns `(False, ...)` when `GP < 2` or `MIN < 10`
    - `sjekk_spiller` returns `(True, ...)` when `GP >= 2` and `MIN >= 10`
    - `hent_toppspillere_for_lag` returns at most `ANTALL_TOPPSPILLERE` players, all with season `MIN >= MIN_MINUTTER`, sorted descending by `MIN`
    - `filtrer_bets_for_skader(value_df, siste3=..., sesong_snitt=...)` with injected frames makes zero network calls and returns a DataFrame whose row count equals the input row count
    - A bet whose home team's top player has 0 games in the last 3 gets `Skadestatus == "⚠️  USIKKER"` and a non-empty `Skadeinfo`
    - A bet where every top player on both teams played gets `Skadestatus == "✅ OK"` and `Skadeinfo == "Alle nøkkelspillere spilte siste 3 kamper"`
    - An unresolvable team name (`finn_lag_id` returns `None`) is skipped without raising, matching today's `continue` behaviour
  </behavior>
  <action>
    Create `skadefilter.py` at repo root. Norwegian identifiers/comments/docstrings, snake_case, no type hints — mirror `features.py`/`teams.py`. Write `tests/test_skadefilter.py` first (RED), then implement (GREEN).

    Move, do not rewrite: every decision rule, threshold, print string and emoji from `05_skadefilter.py` must be carried over character-for-character unless this action says otherwise. The point of the extraction is that behaviour is provably unchanged.

    Structural changes required to kill the import-time side effects:
    - `SESONG` is currently a module-level constant computed at import (`05_skadefilter.py:34`) followed by a bare `print`. Replace with a `gjeldende_sesong()` function; callers pass the season down. Remove the module-level `print(f"Bruker sesong: {SESONG}")` — that print moves into the CLI wrapper in Task 2.
    - `_hent_spillerdata(season_type, last_n=0)` becomes `hent_spillerdata(season_type, sesong, last_n=0)` (public, season injected). Keep the broad `try/except Exception` returning an empty DataFrame and the `time.sleep(1.0)` rate-limit pause verbatim — this is the documented project convention for `nba_api` calls.
    - Lines 55-88's four-call fetch-and-merge block becomes `hent_spillerstatistikk(sesong=None)` returning the tuple `(siste3, sesong_snitt)`. Default `sesong=None` means "call `gjeldende_sesong()`". Preserve the merge semantics exactly: `siste3` concatenates Playoffs then Regular Season and drops duplicate `PLAYER_ID` keeping first; `sesong_snitt` groups by `PLAYER_ID` summing `MIN` and taking first `PLAYER_NAME`/`TEAM_ID`; both fall back to whichever frame is non-empty.
    - `hent_toppspillere_for_lag`, `sjekk_spiller` and `sjekk_lag_helse` currently close over the module-level `sesong_snitt`/`siste3`. Make those explicit leading parameters as shown in the interfaces block.
    - Lines 167-217's per-bet loop becomes `filtrer_bets_for_skader(value_df, siste3=None, sesong_snitt=None)`. When either frame is `None`, call `hent_spillerstatistikk()` once to fill both — that keyword-injection point is the entire reason the function is testable without network. Keep the per-`lagnavn` cache dict, the `" vs "` split, the `teams.finn_lag_id` call (import it — never build a new lookup dict, Phase 2 D-03), the `continue` on unresolved team, and every print string. Return `pd.DataFrame(filtrerte_bets)`.
    - `skriv_skadefilter_csv(resultat_df, sti="value_bets_med_skadefilter.csv")` does the `to_csv(..., index=False)` write and its confirmation print.

    Known duplication to preserve and document, not fix: `gjeldende_sesong()` here and `gjeldende_sesong()` in `04_value_detector.py` are the same NBA-season derivation. Both are being extracted this phase but CONTEXT.md does not scope a shared season module, and it was never one of Phase 2 D-03's four listed duplicates. Add a Norwegian comment above `gjeldende_sesong()` naming the twin in `verdi_deteksjon.py` (plan 04-06) and flagging consolidation as a Phase 5 item — same treatment 02-05-SUMMARY.md gave the DIFF_-column divergence. Record it in this plan's SUMMARY too.

    Test fixtures: build the `siste3` and `sesong_snitt` DataFrames inline in the test file with columns `PLAYER_ID`, `PLAYER_NAME`, `TEAM_ID`, `MIN`, `GP` (the last-3 frame needs `GP`; the season frame does not). Use two real NBA team IDs resolvable by `teams.finn_lag_id`, e.g. Miami Heat `1610612748` and Toronto Raptors `1610612761`, and build the `value_df` with the exact columns `04_value_detector.py` writes: `Kamp`, `KampDato`, `Bet`, `Odds`, `Bookmaker`, `Modell_prob`, `Modell %`, `Bookmaker %`, `Value`, `Forv. EV`. No random values, no `datetime.now()`.
  </action>
  <verify>
    <automated>venv/bin/python -m pytest tests/test_skadefilter.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `venv/bin/python -m pytest tests/test_skadefilter.py -x -q` exits 0 with at least 6 tests passing
    - A test literally named `test_import_skadefilter_gjor_ingen_nettverkskall` exists and passes
    - `venv/bin/python -c "import skadefilter; print('OK')"` prints only `OK` — no "Bruker sesong" or "Henter spillerstatistikk" output, proving no import-time side effects
    - `grep -c "from teams import finn_lag_id" skadefilter.py` returns `1`
    - `grep -vc '^#' skadefilter.py` confirms the file is non-trivial; `grep -c "nba_teams.get_teams()" skadefilter.py` returns `0` (no rebuilt lookup dict)
    - `grep -c ') ->' skadefilter.py` returns `0` (no type hints)
    - The literal strings `"✅ OK"` and `"Alle nøkkelspillere spilte siste 3 kamper"` both appear in `skadefilter.py`
  </acceptance_criteria>
  <done>`skadefilter.py` holds every injury-filter rule as importable functions with player data injectable as keyword arguments; importing it does nothing observable; tests prove the decision rules and the no-network-on-import property.</done>
</task>

<task type="auto">
  <name>Task 2: Reduser 05_skadefilter.py til en tynn CLI-wrapper</name>
  <files>05_skadefilter.py</files>
  <read_first>
    - 05_skadefilter.py (current full file — lines 146-244 are the CLI behaviour that stays)
    - skadefilter.py (as created in Task 1)
    - 04_value_detector.py lines 40-46 (the `sys.exit(1)` convention and its explanatory comment)
  </read_first>
  <action>
    Rewrite `05_skadefilter.py` so its only remaining content is: the existing module docstring (updated to say the logic now lives in `skadefilter.py` and that this file is the standalone CLI entry point), imports from `skadefilter`, a `main()` function, and an `if __name__ == "__main__": main()` guard.

    `main()` must reproduce today's standalone behaviour exactly, in this order:
    1. Print `f"Bruker sesong: {sesong}"` using `skadefilter.gjeldende_sesong()` (the print moved out of module scope in Task 1).
    2. Print the `"=" * 60` header block with `"SKADEFILTER FOR DAGENS VALUE BETS"`.
    3. `pd.read_csv("value_bets_idag.csv")` inside `try/except FileNotFoundError`, printing the existing "Finner ikke 'value_bets_idag.csv' – kjør 04_value_detector.py først!" message. Replace the current bare `exit()` with `sys.exit(1)` and carry over the same explanatory comment used at `04_value_detector.py:45` — a bare `exit()` returns exit code 0 and hides the failure from any caller.
    4. Print `f"Sjekker {len(value_df)} value bets\n"`.
    5. Empty-input short circuit: keep today's behaviour of writing an empty CSV with the input columns plus `Skadestatus` and `Skadeinfo`, printing "Ingen value bets å sjekke – skriver tom fil og avslutter.", then returning (do not `sys.exit(1)` here — an empty day is not an error).
    6. `resultat_df = skadefilter.filtrer_bets_for_skader(value_df)` — no injected frames, so the real `nba_api` fetch happens here.
    7. The summary block from lines 219-244 verbatim: the `ok_bets`/`usikre_bets` split via `str.contains("OK")` / `str.contains("USIKKER")`, the three print branches, then `skadefilter.skriv_skadefilter_csv(resultat_df)` and the closing "⚠️  Husk: Spill alltid ansvarlig." line.

    No decision logic, no thresholds and no `nba_api` import may remain in this file.
  </action>
  <verify>
    <automated>venv/bin/python -m pytest -q &amp;&amp; test "$(grep -c 'nba_api' 05_skadefilter.py)" = "0" &amp;&amp; test "$(grep -c 'leaguedashplayerstats' 05_skadefilter.py)" = "0" &amp;&amp; echo GRØNN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "nba_api" 05_skadefilter.py` returns `0`
    - `grep -c "ANTALL_TOPPSPILLERE\|MIN_MINUTTER" 05_skadefilter.py` returns `0` — thresholds live in `skadefilter.py` only
    - `grep -c 'if __name__ == "__main__":' 05_skadefilter.py` returns `1`
    - `grep -c "^exit()" 05_skadefilter.py` returns `0` and `grep -c "sys.exit(1)" 05_skadefilter.py` returns at least `1`
    - `wc -l < 05_skadefilter.py` is under `90` (down from 245)
    - `venv/bin/python -m pytest -q` (full suite) exits 0
    - `venv/bin/python -c "import ast,sys; ast.parse(open('05_skadefilter.py').read())"` exits 0
  </acceptance_criteria>
  <done>`05_skadefilter.py` is a docstring plus a `main()` that orchestrates `skadefilter.py`'s functions; running it standalone still produces `value_bets_med_skadefilter.csv` with identical columns and console output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nba_api (stats.nba.com) → this repo | Untrusted third-party JSON/DataFrames cross here |
| `value_bets_idag.csv` → filter | Locally produced but unvalidated CSV crosses into the decision path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Denial of service | `nba_api` call hanging or failing during `hent_spillerdata` | mitigate | Preserve the existing broad `try/except Exception` returning an empty DataFrame so the pipeline degrades to "no warnings" rather than crashing |
| T-04-07 | Tampering | Import-time network calls firing in unexpected contexts (e.g. a test run or a `06_bot.py` import) | mitigate | Task 1 removes all module-level side effects; `test_import_skadefilter_gjor_ingen_nettverkskall` enforces it |
| T-04-08 | Elevation of privilege | Malformed team name reaching `finn_lag_id` | accept | `teams.finn_lag_id` returns `None` for unknown names by contract (documented in `teams.py`); the caller's `continue` handles it |
| T-04-09 | Repudiation | Silent behaviour drift during extraction | mitigate | Every threshold, message string and emoji carried over verbatim; acceptance criteria grep for the exact status strings `06_bot.py` filters on |
</threat_model>

<verification>
- `venv/bin/python -m pytest -q` — full suite green
- `venv/bin/python -c "import skadefilter"` produces no output and makes no network call
- `git diff --stat` shows `05_skadefilter.py` shrinking by roughly 160 lines, plus new `skadefilter.py` and `tests/test_skadefilter.py`
</verification>

<success_criteria>
- The injury-filter logic is callable in-process by `06_bot.py` (plan 04-08) with no subprocess
- Standalone `venv/bin/python 05_skadefilter.py` still behaves as documented in KOMME_I_GANG.md
- No behaviour, threshold or output string changed
</success_criteria>

<output>
Create `.planning/phases/04-historical-odds-acquisition-live-refactor/04-02-SUMMARY.md` when done
</output>
