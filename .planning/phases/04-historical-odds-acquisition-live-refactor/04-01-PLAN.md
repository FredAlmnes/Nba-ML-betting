---
phase: 04-historical-odds-acquisition-live-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/research/STACK.md
  - odds.py
  - tests/test_odds.py
  - .gitignore
autonomous: true
requirements: [ODDS-01]

must_haves:
  truths:
    - "Every source document that names the historical endpoint names the sport-wide endpoint, matching D-03's amendment"
    - "A permanent SQLite archive file can be opened/created idempotently and holds odds rows"
    - "Archiving the same odds row twice never creates a duplicate"
    - "Every API call's credit cost can be read back out of the archive afterwards"
  artifacts:
    - path: "odds.py"
      provides: "SQLite archive layer for historical odds (apne_arkiv, er_allerede_arkivert, arkiver_odds_rader, logg_kreditt)"
      contains: "CREATE TABLE IF NOT EXISTS odds_arkiv"
      min_lines: 90
    - path: "tests/test_odds.py"
      provides: "Idempotency and existence-check tests for the archive layer"
      contains: "def test_dobbel_insert_er_idempotent"
  key_links:
    - from: "odds.py"
      to: "sqlite3 stdlib"
      via: "import sqlite3 + executescript(SKJEMA)"
      pattern: "import sqlite3"
    - from: ".gitignore"
      to: "odds_arkiv.db"
      via: "ignore rule"
      pattern: "odds_arkiv\\.db"
---

<objective>
Correct the stale "per-event endpoint" claim that survives in three planning documents (superseded by D-03's 2026-08-23 amendment), and lay down `odds.py`'s SQLite archive layer — the persistence half of ODDS-01, with zero network code and zero credit spend.

Purpose: The archive layer is what makes re-running the backfill free (D-04). It must exist and be proven idempotent before any credit-spending code is written on top of it.
Output: `odds.py` (archive functions + schema), `tests/test_odds.py` (new), corrected REQUIREMENTS.md / ROADMAP.md / research/STACK.md, `.gitignore` entry for `odds_arkiv.db`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-historical-odds-acquisition-live-refactor/04-CONTEXT.md
@.planning/phases/04-historical-odds-acquisition-live-refactor/04-RESEARCH.md
@.planning/phases/04-historical-odds-acquisition-live-refactor/04-VALIDATION.md
@teams.py
@config.py
@pytest.ini

<interfaces>
<!-- Contracts this plan CREATES. Later plans (04-03, 04-04, 04-05) build on these exact names. -->
<!-- Norwegian snake_case, no type hints, per CLAUDE.md conventions. -->

odds.py (new module, repo root — flat, per D-08):

```python
ARKIV_FIL = "odds_arkiv.db"

def apne_arkiv(sti=ARKIV_FIL):
    """Åpner (og oppretter ved behov) SQLite-arkivet. Returnerer sqlite3.Connection."""

def er_allerede_arkivert(con, kamp_dato, snapshot_type):
    """True hvis arkivet allerede har minst én rad for (kamp_dato, snapshot_type)."""

def arkiver_odds_rader(con, rader):
    """INSERT OR IGNORE + commit. Returnerer antall NYE rader som faktisk ble lagt inn."""

def logg_kreditt(con, endepunkt, forespurt_dato, headers, antall_rader):
    """Logger faktisk kredittforbruk fra x-requests-last/x-requests-remaining."""
```

Radformat forventet av `arkiver_odds_rader` (tuple, 16 felt, i denne rekkefølgen):
`(sport, event_id, kamp_dato, hjemmelag, bortelag, hjemme_lag_id, borte_lag_id,
  commence_time, snapshot_type, snapshot_timestamp, bookmaker, marked,
  utfall_navn, odds, hentet_tidspunkt)` — 15 felt (id er AUTOINCREMENT).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Korriger foreldet «per-event»-påstand i REQUIREMENTS.md, ROADMAP.md og research/STACK.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/research/STACK.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 30 — ODDS-01 wording, and line 67 — the open budget decision note)
    - .planning/ROADMAP.md (Phase 4 section, ~lines 113-126 — Goal, "Decision point (phase entry)" note, Success Criteria #1)
    - .planning/research/STACK.md (grep for "per-event" and "10x" to locate the superseded cost claim)
    - .planning/phases/04-historical-odds-acquisition-live-refactor/04-CONTEXT.md (D-02 and D-03, both marked "AMENDED 2026-08-23")
  </read_first>
  <action>
    Three documents still assert the per-event historical endpoint, which D-03's amendment reversed after verified research (04-RESEARCH.md "CRITICAL FINDING"). Correct all three so no downstream agent builds against the budget-breaking premise. Do not invent new requirements — only replace the endpoint claim and mark superseded text.

    1. `.planning/REQUIREMENTS.md`, ODDS-01 (currently: "Historical odds are fetched via The Odds API's per-event historical endpoint and archived permanently in SQLite, so re-running/iterating on the backtest costs no further API credits"). Rewrite the endpoint clause to name the sport-wide endpoint `/v4/historical/sports/{sport}/odds`, one call per unique game date, and keep the archival/no-further-credits clause verbatim. Append a short parenthetical: "(endpoint amended 2026-08-23 per Phase 4 D-03 — both endpoints cost 10 x markets x regions, but per-event charges per game queried; sport-wide charges once per snapshot)".
    2. `.planning/REQUIREMENTS.md`, the "**Open budget decision:**" paragraph near line 67: mark it RESOLVED — the user purchased one month of the 20,000-credit tier on 2026-08-23. Keep the paragraph, prefix with "RESOLVED 2026-08-23:".
    3. `.planning/ROADMAP.md`, Phase 4 Success Criteria #1: same endpoint substitution as step 1.
    4. `.planning/ROADMAP.md`, Phase 4 "**Decision point (phase entry):**" paragraph: prefix with "RESOLVED 2026-08-23:" and state the paid tier is purchased, so ODDS-01 is unblocked.
    5. `.planning/research/STACK.md`: locate its per-event-vs-sport-wide cost comparison (grep for "per-event"). Do NOT delete it. Insert an explicit superseded marker immediately above/inside that section: "SUPERSEDED 2026-08-23 by .planning/phases/04-historical-odds-acquisition-live-refactor/04-RESEARCH.md — both historical endpoints share the identical cost formula 10 x markets x regions; the per-event endpoint charges it per game queried, making it ~72,760 credits for this project's 3,638 games versus ~10-20K for sport-wide. Use the sport-wide endpoint."

    Do not touch the ROADMAP.md "Plans:" list under Phase 4 — the planner owns that.
  </action>
  <verify>
    <automated>grep -q "sport-wide" .planning/REQUIREMENTS.md &amp;&amp; grep -q "/v4/historical/sports/{sport}/odds" .planning/REQUIREMENTS.md &amp;&amp; grep -q "sport-wide" .planning/ROADMAP.md &amp;&amp; grep -q "SUPERSEDED 2026-08-23" .planning/research/STACK.md &amp;&amp; grep -q "RESOLVED 2026-08-23" .planning/ROADMAP.md &amp;&amp; echo GRØNN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "per-event historical endpoint" .planning/REQUIREMENTS.md` returns `0`
    - `grep -c "per-event historical endpoint" .planning/ROADMAP.md` returns `0`
    - ODDS-01's line in `.planning/REQUIREMENTS.md` contains the literal string `/v4/historical/sports/{sport}/odds`
    - `.planning/research/STACK.md` contains the literal string `SUPERSEDED 2026-08-23`
    - `.planning/ROADMAP.md` Phase 4 contains the literal string `RESOLVED 2026-08-23`
    - The verify command above prints `GRØNN`
  </acceptance_criteria>
  <done>All three documents name the sport-wide endpoint; both budget-decision notes are marked resolved; STACK.md's stale cost claim carries a superseded marker instead of being silently deleted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Opprett odds.py med SQLite-arkivlaget (skjema, eksistenssjekk, idempotent innsetting, kredittlogg)</name>
  <files>odds.py, tests/test_odds.py, .gitignore</files>
  <read_first>
    - teams.py (module docstring + finn_lag_id — the shared-module style this file must mirror)
    - config.py (module docstring style, UPPER_SNAKE constants, the explicit "secrets do not live here" note)
    - tests/test_oppsett.py and tests/conftest.py (existing pytest conventions in this repo)
    - .planning/phases/04-historical-odds-acquisition-live-refactor/04-RESEARCH.md § "Pattern 2: SQLite idempotent insert if not exists" (the schema sketch and the commit-per-snapshot rationale)
    - .gitignore (the "Data og modell" block where the new ignore rule belongs)
  </read_first>
  <behavior>
    - `apne_arkiv(":memory:")` returns a usable connection and the `odds_arkiv` table exists afterwards
    - `er_allerede_arkivert(con, "2023-01-15", "bet_time")` is `False` against an empty archive
    - After archiving one row for `("2023-01-15", "bet_time")`, the same call returns `True`
    - `er_allerede_arkivert(con, "2023-01-15", "closing")` stays `False` after only a `bet_time` row was archived (snapshot types are tracked independently)
    - `arkiver_odds_rader(con, rader)` called twice with the identical 2-row list returns `2` the first time and `0` the second; `SELECT COUNT(*) FROM odds_arkiv` stays at `2` (test name: `test_dobbel_insert_er_idempotent`)
    - Inserting a row with `snapshot_type='ugyldig'` raises `sqlite3.IntegrityError` (the CHECK constraint holds)
    - `logg_kreditt` stores `x-requests-last` and `x-requests-remaining` as integers in `kreditt_logg`, and tolerates missing headers by storing `NULL`
  </behavior>
  <action>
    Create `odds.py` at repo root (flat module, per D-08 — no `nba_betting/` package). Norwegian identifiers/comments/docstrings, snake_case, no type hints, no linter directives — match `teams.py`/`strategy.py`/`config.py` exactly in style. Write the tests in `tests/test_odds.py` first (RED), then the implementation (GREEN).

    Module docstring (Norwegian): state that this is the single, shared Odds API-modul for both the live path and the Phase 5 backtest (D-07), that the SQLite archive is permanent and append-only with no TTL because historical odds for a past timestamp never change (D-04), and that the credit-saving mechanism is the SELECT-before-fetch check, not INSERT OR IGNORE.

    Constants: `ARKIV_FIL = "odds_arkiv.db"`, `SPORT = "basketball_nba"`, `MARKED = "h2h"` (moneyline only — v1 scope is moneyline, do not add spreads/totals), `REGION = "eu"` (matches 04_value_detector.py's current live region so backtest and live see the same books).

    `SKJEMA` (module-level triple-quoted string, executed via `con.executescript`):
    - Table `odds_arkiv` with columns in this exact order: `id INTEGER PRIMARY KEY AUTOINCREMENT`, `sport TEXT NOT NULL`, `event_id TEXT NOT NULL`, `kamp_dato TEXT NOT NULL`, `hjemmelag TEXT NOT NULL`, `bortelag TEXT NOT NULL`, `hjemme_lag_id INTEGER`, `borte_lag_id INTEGER`, `commence_time TEXT NOT NULL`, `snapshot_type TEXT NOT NULL CHECK (snapshot_type IN ('bet_time', 'closing'))`, `snapshot_timestamp TEXT NOT NULL`, `bookmaker TEXT NOT NULL`, `marked TEXT NOT NULL`, `utfall_navn TEXT NOT NULL`, `odds REAL NOT NULL`, `hentet_tidspunkt TEXT NOT NULL`, and `UNIQUE(event_id, snapshot_type, bookmaker, marked, utfall_navn)`.
    - Comment `kamp_dato` inline as the join key to `nba_features.csv`'s `GAME_DATE_HJEMME` (plain `YYYY-MM-DD` strings, verified).
    - Comment `snapshot_timestamp` inline as the API's actually-returned `timestamp`, which may differ from the requested `date` — never overwrite it with the requested value (Pitfall 4's warning sign depends on this being the real value).
    - `CREATE INDEX IF NOT EXISTS idx_odds_arkiv_dato_type ON odds_arkiv(kamp_dato, snapshot_type)`.
    - Table `kreditt_logg`: `id INTEGER PRIMARY KEY AUTOINCREMENT`, `tidspunkt TEXT NOT NULL`, `endepunkt TEXT NOT NULL`, `forespurt_dato TEXT`, `kreditt_brukt INTEGER`, `kreditt_igjen INTEGER`, `antall_rader INTEGER`. This is the audit trail that proves what the backfill actually cost.

    `apne_arkiv(sti=ARKIV_FIL)`: `sqlite3.connect(sti)`, `executescript(SKJEMA)`, `commit()`, return the connection.

    `er_allerede_arkivert(con, kamp_dato, snapshot_type)`: single indexed `SELECT 1 ... WHERE kamp_dato = ? AND snapshot_type = ? LIMIT 1`, return `rad is not None`. Docstring must say, in Norwegian, that this is called BEFORE the network call and is the actual credit-saving check (D-04).

    `arkiver_odds_rader(con, rader)`: if `rader` is empty return `0`. Record `con.total_changes` before, run `executemany("INSERT OR IGNORE INTO odds_arkiv (...15 named columns...) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)", rader)`, `con.commit()` immediately (commit per snapshot IS the resumability — an interrupted run must keep everything already paid for), return `con.total_changes` minus the before-value.

    `logg_kreditt(con, endepunkt, forespurt_dato, headers, antall_rader)`: read `headers.get("x-requests-last")` and `headers.get("x-requests-remaining")`, convert to int when present and `None` when absent or unparseable, insert one row into `kreditt_logg` with `datetime.now().isoformat(timespec="seconds")` as `tidspunkt`, commit.

    Add `odds_arkiv.db` to `.gitignore` under the existing "Data og modell" block with a Norwegian comment noting it is a large, regenerable-but-expensive local artifact.
  </action>
  <verify>
    <automated>venv/bin/python -m pytest tests/test_odds.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `venv/bin/python -m pytest tests/test_odds.py -x -q` exits 0 with at least 5 tests passing, including a test literally named `test_dobbel_insert_er_idempotent`
    - `venv/bin/python -m pytest -q` (full suite) still exits 0 — no existing test regressed
    - `grep -c "odds_arkiv.db" .gitignore` returns at least `1`
    - `grep -c "import requests" odds.py` returns `0` — this plan adds no network code
    - `grep -c "UNIQUE(event_id, snapshot_type, bookmaker, marked, utfall_navn)" odds.py` returns `1`
    - `grep -c "CREATE TABLE IF NOT EXISTS kreditt_logg" odds.py` returns `1`
    - `venv/bin/python -c "import odds; c=odds.apne_arkiv(':memory:'); print(sorted(r[0] for r in c.execute(\"SELECT name FROM sqlite_master WHERE type='table'\")))"` prints a list containing both `kreditt_logg` and `odds_arkiv`
    - No type hints anywhere in `odds.py` (`grep -c ') ->' odds.py` returns `0`)
  </acceptance_criteria>
  <done>`odds.py` exists with a working, tested, idempotent SQLite archive layer and a credit audit log; `odds_arkiv.db` is gitignored; the full pytest suite is green; no network code has been written yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| The Odds API → this repo | Untrusted JSON crosses here in later plans; this plan defines the storage schema it lands in |
| Local filesystem → git | `odds_arkiv.db` must never enter version control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `arkiver_odds_rader` SQL | mitigate | Parameterised `executemany` with `?` placeholders only; no f-string/format SQL anywhere in `odds.py` |
| T-04-02 | Information disclosure | `odds_arkiv.db` committed to git | mitigate | `.gitignore` rule added in Task 2; acceptance criterion greps for it |
| T-04-03 | Tampering | Invalid `snapshot_type` corrupting the archive's meaning | mitigate | `CHECK (snapshot_type IN ('bet_time','closing'))` enforced at schema level, covered by a test |
| T-04-04 | Repudiation | No record of what API credits were actually spent | mitigate | `kreditt_logg` table + `logg_kreditt()` records `x-requests-last`/`x-requests-remaining` per call |
| T-04-05 | Denial of service | Corrupt/partial archive after interrupted run | accept | SQLite is ACID per transaction and `arkiver_odds_rader` commits per snapshot; worst case is one lost in-flight snapshot |
</threat_model>

<verification>
- `venv/bin/python -m pytest -q` — full suite green
- `git diff --stat` shows exactly: `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/research/STACK.md`, `.gitignore`, plus new files `odds.py` and `tests/test_odds.py`
- `git status --short` shows no untracked `odds_arkiv.db`
</verification>

<success_criteria>
- No planning document still instructs a future agent to use the per-event historical endpoint
- `odds.py` can create the archive from scratch, tell you whether a `(kamp_dato, snapshot_type)` pair is already archived, and refuse to duplicate rows
- Zero API credits spent by this plan
</success_criteria>

<output>
Create `.planning/phases/04-historical-odds-acquisition-live-refactor/04-01-SUMMARY.md` when done
</output>
