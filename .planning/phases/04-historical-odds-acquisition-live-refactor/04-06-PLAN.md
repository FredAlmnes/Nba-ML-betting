---
phase: 04-historical-odds-acquisition-live-refactor
plan: 06
type: execute
wave: 4
depends_on: [04-04]
files_modified:
  - verdi_deteksjon.py
  - 04_value_detector.py
  - tests/test_verdi_deteksjon.py
autonomous: true
requirements: [ODDS-02]

must_haves:
  truths:
    - "Importing the value detector triggers no network call, no model load and no CSV write"
    - "The value/EV decision can be called as a function with odds and team stats injected"
    - "The live odds fetch comes from odds.py, not from an inline HTTP call"
    - "04_value_detector.py still runs standalone and writes value_bets_idag.csv with the same ten columns"
  artifacts:
    - path: "verdi_deteksjon.py"
      provides: "Importable value-detection core (finn_value_bets, last_modell, skriv_value_bets_csv)"
      contains: "def finn_value_bets"
      min_lines: 110
    - path: "04_value_detector.py"
      provides: "Thin CLI wrapper preserving standalone behaviour"
      contains: "if __name__ == \"__main__\":"
    - path: "tests/test_verdi_deteksjon.py"
      provides: "Output-regression test proving extraction changed nothing"
      contains: "def test_finn_value_bets_uendret_output"
  key_links:
    - from: "verdi_deteksjon.py"
      to: "odds.hent_live_odds"
      via: "shared odds client (D-07)"
      pattern: "import odds"
    - from: "verdi_deteksjon.py"
      to: "strategy.fjern_vigorish / strategy.beregn_value_og_ev"
      via: "already-shared decision core from Phase 2"
      pattern: "from strategy import"
---

<objective>
Extract `04_value_detector.py`'s core into an importable `verdi_deteksjon.py` (D-05) and repoint its odds fetch at `odds.py` (D-07). Today the whole file executes at import — it loads the pickle, calls The Odds API and writes a CSV as a side effect of being imported, which is why `06_bot.py` had to run it as a subprocess.

Purpose: The second half of ODDS-02. After this plan and 04-02, `06_bot.py` has two importable functions to call instead of two subprocesses.
Output: `verdi_deteksjon.py` (new), `04_value_detector.py` reduced to a CLI wrapper, `tests/test_verdi_deteksjon.py` (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-historical-odds-acquisition-live-refactor/04-CONTEXT.md
@.planning/phases/04-historical-odds-acquisition-live-refactor/04-RESEARCH.md
@.planning/phases/04-historical-odds-acquisition-live-refactor/04-04-SUMMARY.md
@04_value_detector.py
@features.py
@strategy.py
@config.py
@modell_utils.py

<interfaces>
<!-- Already shared from Phase 2 — import these, never reimplement: -->
```python
# strategy.py
def fjern_vigorish(odds_hjemme, odds_borte)          # -> (impl_hjemme, impl_borte)
def beregn_value_og_ev(modell_prob, odds, impl_prob) # -> (value, ev)
# features.py
STATS_KOLONNER
def snitt_fra_kamplogg(df_logg)
def bygg_feature_rad(hjemme_stats, borte_stats)
# teams.py
def finn_lag_id(navn)
# config.py
MIN_VALUE_TERSKEL = 0.05, MIN_ODDS = 1.50, MAX_ODDS = 4.00
# modell_utils.py
class KalibrertModell                                 # må importeres for at pickle skal kunne lastes
```

<!-- New from plan 04-04 (D-07): -->
```python
# odds.py
def hent_live_odds(api_nokkel=None, regions="eu", markets="h2h", sport="basketball_nba")  # -> liste av kamp-dicts
def hent_api_nokkel()
```

<!-- This plan CREATES: -->
```python
# verdi_deteksjon.py
KOLONNER = ["Kamp", "KampDato", "Bet", "Odds", "Bookmaker", "Modell_prob",
            "Modell %", "Bookmaker %", "Value", "Forv. EV"]

def gjeldende_sesong()
def hent_siste_lagstats(team_id, antall_kamper=10, sesong=None)   # -> dict | None
def last_modell(sti="nba_modell.pkl")                              # -> (modell, feature_kolonner)
def finn_value_bets(modell, feature_kolonner, kamper=None, api_nokkel=None, hent_lagstats=None)  # -> liste av dicts
def skriv_value_bets_csv(value_bets, sti="value_bets_idag.csv")
```

Bet dict schema produced by `finn_value_bets` — exact keys and formatting, consumed by
`skadefilter.filtrer_bets_for_skader` and by `06_bot.py::plasser_bets`:
```python
{"Kamp": "Miami Heat vs Toronto Raptors", "KampDato": "2023-01-15",
 "Bet": "Hjemme (Miami Heat)", "Odds": 2.10, "Bookmaker": "Unibet",
 "Modell_prob": 0.6123,          # round(..., 4), rå float til Kelly
 "Modell %": "61.2%", "Bookmaker %": "47.6%",
 "Value": "+13.6%", "Forv. EV": "+28.6%"}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Opprett verdi_deteksjon.py med finn_value_bets som ren, injiserbar funksjon</name>
  <files>verdi_deteksjon.py, tests/test_verdi_deteksjon.py</files>
  <read_first>
    - 04_value_detector.py (all 254 lines — every rule, threshold, print string and dict key below must survive verbatim)
    - odds.py (hent_live_odds signature and its printed output, so the wrapper does not print the same line twice)
    - features.py, strategy.py, config.py (the already-shared functions this module composes)
    - tests/test_strategy.py (existing test style for the decision core)
  </read_first>
  <behavior>
    - `import verdi_deteksjon` performs no network call, loads no pickle and writes no file
    - `finn_value_bets` with an injected `kamper` fixture, an injected `hent_lagstats` stub and a stub model returning a fixed `predict_proba` produces a bet list whose single entry equals an exact expected dict, key for key, string for string (test name: `test_finn_value_bets_uendret_output`)
    - A game whose home or away team cannot be resolved by `finn_lag_id` is skipped and produces no bet
    - A game where `hent_lagstats` returns `None` for either team is skipped
    - A game with no h2h outcomes (best odds stay 0) is skipped
    - A bet with `value <= MIN_VALUE_TERSKEL` is not flagged
    - A bet whose odds fall outside `MIN_ODDS`..`MAX_ODDS` is not flagged even when the value clears the threshold
    - The best odds per side are the maximum across all bookmakers, and `Bookmaker` names the book offering it
    - `KampDato` is the first 10 characters of the game's `commence_time`
    - `skriv_value_bets_csv([])` writes a CSV containing exactly the `KOLONNER` header and no data rows
  </behavior>
  <action>
    Create `verdi_deteksjon.py` at repo root. Norwegian identifiers/comments/docstrings, snake_case, no type hints — mirror `features.py`/`strategy.py`. Write `tests/test_verdi_deteksjon.py` first (RED), then implement (GREEN).

    Move, do not rewrite. Every threshold, comparison, rounding, format specifier, print string and dict key from `04_value_detector.py` carries over character-for-character. The regression test exists specifically to prove that.

    Structural changes required:
    - No module-level work at all. The pickle load (lines 51-57), the API key read (lines 39-45), the odds fetch (lines 61-85) and the CSV write (lines 234-250) all become function bodies.
    - `gjeldende_sesong()` and `hent_siste_lagstats(team_id, antall_kamper=10, sesong=None)` move over unchanged, except that the season becomes a parameter defaulting to `gjeldende_sesong()`. Keep the `time.sleep(0.5)` rate-limit pause and the `if len(df) < 3: return None` guard verbatim.
    - `last_modell(sti="nba_modell.pkl")` opens the pickle and returns `(data["modell"], data["feature_kolonner"])`. It must import `KalibrertModell` from `modell_utils` at module top — the pickle cannot unpickle without that name being importable (Phase 1 HYG-02).
    - `finn_value_bets(modell, feature_kolonner, kamper=None, api_nokkel=None, hent_lagstats=None)`:
      - `kamper is None` -> `kamper = odds.hent_live_odds(api_nokkel)`. This is D-07: the inline `requests.get` block disappears from this codebase entirely.
      - `hent_lagstats is None` -> use this module's `hent_siste_lagstats`. That keyword is what makes the whole function testable without `nba_api`.
      - The body is the existing loop from lines 127-225, unchanged: resolve both team IDs (keeping the explanatory comment about `teams.finn_lag()`'s resolution order that 02-04-SUMMARY.md documents), fetch both teams' stats, `bygg_feature_rad`, select `feature_kolonner`, `predict_proba(X)[0][1]`, find best odds per side across bookmakers, `fjern_vigorish`, `beregn_value_og_ev` for each side, print the six-line per-game block, then append to `value_bets` under the two threshold checks.
      - Return the `value_bets` list.
    - `skriv_value_bets_csv(value_bets, sti="value_bets_idag.csv")` reproduces lines 234-250 exactly, including the always-write behaviour and its multi-line Norwegian comment explaining the 2026-08-19 bug: if the file is not rewritten on a no-bets day, `05`/`06` silently reuse yesterday's (or last month's) already-played games as today's recommendations. That comment is load-bearing safety documentation — carry it over word for word.
    - Define `KOLONNER` as a module constant exactly as in the interfaces block.

    Known duplication to preserve and document, not fix: `gjeldende_sesong()` here is the twin of `skadefilter.gjeldende_sesong()` (plan 04-02). Add a Norwegian comment naming the twin and flagging consolidation as a Phase 5 item, matching the note plan 04-02 adds on its side. Record it in the SUMMARY.

    Test fixtures: build the `kamper` list inline in the test file with the Odds API shape (`home_team`, `away_team`, `commence_time`, `bookmakers` -> `markets` -> `outcomes`), using Miami Heat and Toronto Raptors so `teams.finn_lag_id` resolves them without network. The stub model is a tiny class with a `predict_proba(X)` returning a fixed nested list. The `hent_lagstats` stub returns a fixed dict with the keys `bygg_feature_rad` expects. Compute the one expected bet dict by hand from the fixture numbers and assert equality against the literal — not against a recomputation, which would just re-run the code under test.
  </action>
  <verify>
    <automated>venv/bin/python -m pytest tests/test_verdi_deteksjon.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `venv/bin/python -m pytest tests/test_verdi_deteksjon.py -x -q` exits 0 with at least 8 tests passing
    - A test literally named `test_finn_value_bets_uendret_output` exists and asserts an exact dict equality (name fixed by 04-VALIDATION.md)
    - `venv/bin/python -c "import verdi_deteksjon; print('OK')"` prints only `OK` — no model-loading or odds-fetching output
    - `venv/bin/python -m pytest -q` (full suite) exits 0
    - `grep -c "requests.get" verdi_deteksjon.py` returns `0` — the odds fetch comes from `odds.py`
    - `grep -c "^import odds" verdi_deteksjon.py` returns `1`
    - `grep -c "from strategy import" verdi_deteksjon.py` returns `1` and `grep -c "from features import" verdi_deteksjon.py` returns `1`
    - `grep -c ') ->' verdi_deteksjon.py` returns `0` (no type hints)
    - The literal strings `Modell_prob`, `Forv. EV` and `KampDato` all appear in `verdi_deteksjon.py`
  </acceptance_criteria>
  <done>The value/EV decision is an importable, injectable function with a proven-identical output contract, and the last inline Odds API call in this repo is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Reduser 04_value_detector.py til en tynn CLI-wrapper</name>
  <files>04_value_detector.py</files>
  <read_first>
    - 04_value_detector.py (current full file)
    - verdi_deteksjon.py (as created in Task 1)
    - odds.py (hent_api_nokkel — the fail-fast key read that moved out of this file)
    - 05_skadefilter.py (as rewritten by plan 04-02 — mirror that wrapper shape exactly)
  </read_first>
  <action>
    Rewrite `04_value_detector.py` so it contains only: the existing module docstring (updated to say the logic now lives in `verdi_deteksjon.py` and the odds fetch in `odds.py`, keeping the `.env` / `ODDS_API_NOKKEL` instructions from Phase 1 intact), imports, a `main()`, and an `if __name__ == "__main__": main()` guard.

    `main()` reproduces today's standalone run exactly, in order:
    1. `api_nokkel = odds.hent_api_nokkel()` — the fail-fast `sys.exit(1)` behaviour and its guidance lines now live in `odds.py`; do not duplicate them here.
    2. Print `Laster inn trent modell...`, call `verdi_deteksjon.last_modell()`, print `Modell lastet!`. Wrap the load in `try/except FileNotFoundError` printing a Norwegian instruction to run `03_tren_modell.py` first, then `sys.exit(1)` — today an unhandled traceback is what a missing `nba_modell.pkl` produces, and the wrapper is the right place to make that a clean message.
    3. Print the `\nHenter dagens NBA-odds...` line, then `\nHenter fersk lagstatistikk fra NBA...`, then the `"="*60` / `VALUE BETS FOR DAGENS NBA-KAMPER` banner — same three narration blocks, same order as today.
    4. `value_bets = verdi_deteksjon.finn_value_bets(modell, feature_kolonner, api_nokkel=api_nokkel)`.
    5. Print the `"="*60` / `OPPSUMMERING: VALUE BETS I DAG` banner, then `verdi_deteksjon.skriv_value_bets_csv(value_bets)`.
    6. Print the two-line closing warning (the `ADVARSEL: Bruk dette som læringsverktøy` line and the responsible-gambling line) verbatim.

    No thresholds, no `requests`, no `nba_api`, no `pickle` and no `pandas` computation may remain in this file. Keep the file runnable exactly as `KOMME_I_GANG.md` documents it.
  </action>
  <verify>
    <automated>venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "requests" 04_value_detector.py` returns `0`
    - `grep -c "MIN_VALUE_TERSKEL\|MIN_ODDS\|MAX_ODDS" 04_value_detector.py` returns `0` — thresholds live in `config.py`, used by `verdi_deteksjon.py`
    - `grep -c "nba_api\|pickle" 04_value_detector.py` returns `0`
    - `grep -c 'if __name__ == "__main__":' 04_value_detector.py` returns `1`
    - `wc -l < 04_value_detector.py` is under `80` (down from 254)
    - `venv/bin/python -c "import ast; ast.parse(open('04_value_detector.py').read())"` exits 0
    - `venv/bin/python -m pytest -q` (full suite) exits 0
    - `ODDS_API_NOKKEL= venv/bin/python 04_value_detector.py` exits `1` and prints the Phase 1 guidance about copying `.env.example` — proving the fail-fast key check survived the move into `odds.py`
  </acceptance_criteria>
  <done>`04_value_detector.py` is a docstring plus a `main()` orchestrating `odds.py` and `verdi_deteksjon.py`; running it standalone still produces `value_bets_idag.csv` with identical columns and console output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| The Odds API → value detection | Untrusted odds JSON crosses into the staking decision |
| nba_api → feature building | Untrusted team stats cross into the model input |
| `nba_modell.pkl` → process | `pickle.load` executes arbitrary code by design; the file is local and gitignored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-26 | Tampering | `pickle.load` on `nba_modell.pkl` | accept | Locally produced by `03_tren_modell.py`, gitignored, never fetched from a remote source; the project has used this artefact format since before this milestone |
| T-04-27 | Tampering | Silent output drift during extraction changing which bets are flagged | mitigate | `test_finn_value_bets_uendret_output` asserts an exact dict; thresholds imported from `config.py` unchanged |
| T-04-28 | Denial of service | Import-time side effects firing during a test run or a `06_bot.py` import | mitigate | All module-level work moved into functions; a dedicated test asserts a silent import |
| T-04-29 | Information disclosure | API key printed by the detector | mitigate | The key is read only by `odds.hent_api_nokkel()` and passed as an argument; `verdi_deteksjon.py` never prints it |
| T-04-30 | Repudiation | A no-bets day leaving yesterday's stale CSV in place, which the bot would then bet on | mitigate | `skriv_value_bets_csv` always writes, including the empty-header case; carried over verbatim with its explanatory comment, covered by a test |
</threat_model>

<verification>
- `venv/bin/python -m pytest -q` — full suite green
- `venv/bin/python -c "import verdi_deteksjon"` produces no output and makes no network call
- `git diff --stat` shows `04_value_detector.py` shrinking by roughly 180 lines, plus new `verdi_deteksjon.py` and `tests/test_verdi_deteksjon.py`
- `grep -rn "requests.get" --include=*.py . | grep -v venv/` returns only `odds.py`
</verification>

<success_criteria>
- The value-detection logic is callable in-process by `06_bot.py` (plan 04-08) with no subprocess
- Standalone `venv/bin/python 04_value_detector.py` still behaves as documented in KOMME_I_GANG.md
- Exactly one place in the repo makes an HTTP call to The Odds API
</success_criteria>

<output>
Create `.planning/phases/04-historical-odds-acquisition-live-refactor/04-06-SUMMARY.md` when done
</output>
