---
phase: 02-shared-core-extraction-test-foundation
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - strategy.py
  - tests/test_strategy.py
  - 04_value_detector.py
  - 06_bot.py
autonomous: true
requirements: [CORE-01, CORE-03]
user_setup: []

must_haves:
  truths:
    - "D-06: vig removal, value/EV calculation and half-Kelly stake sizing exist as pure functions in strategy.py — no I/O, no global state, no project imports"
    - "D-11: pytest covers beregn_innsats (negative edge, min clamp, max clamp, half-Kelly arithmetic) and the bet-dedup key logic, including the legacy-record fallback that caused the 2026-08-19 stale-row bug"
    - "D-07: the extracted functions produce byte-identical arithmetic to the code they replace — no rounding, clamping or threshold behavior was altered"
    - "04_value_detector.py contains no inline vig/value/EV arithmetic and 06_bot.py contains no beregn_innsats definition — both import from strategy.py"
    - "strategy.py imports nothing from this project (not config.py, not pandas) — config values arrive as call-site parameters"
  artifacts:
    - path: "strategy.py"
      provides: "Pure value/EV/vig/Kelly/dedup functions shared by the live path and the future Phase 5 backtest"
      contains: "def beregn_innsats"
      min_lines: 60
    - path: "tests/test_strategy.py"
      provides: "CORE-03 money-math and dedup unit tests, extending the config tests from plan 02-02"
      contains: "from strategy import"
  key_links:
    - from: "06_bot.py"
      to: "strategy.py"
      via: "from strategy import beregn_innsats, finn_bet_nokkel, bygg_bet_nokler, er_duplikat"
      pattern: "from strategy import"
    - from: "04_value_detector.py"
      to: "strategy.py"
      via: "from strategy import fjern_vigorish, beregn_value_og_ev"
      pattern: "from strategy import"
---

<objective>
Extract the money math into `strategy.py` as pure functions and put the repo's first real tests on it. Today the vig-removal + value/EV arithmetic is inline in `04_value_detector.py:210-225`, `beregn_innsats` is a method-shaped global-reading function in `06_bot.py:202-222`, and the bet-dedup key is an anonymous set comprehension in `06_bot.py:264-266`. None of it is callable, testable, or reusable by the Phase 5 backtest.

Purpose: CORE-03 names `beregn_innsats` and the dedup logic as the two functions that must be tested — they are money-critical and have already produced one real bug (the stale-row double-bet fixed 2026-08-19, documented in `06_bot.py:260-263`). They cannot be tested until they are pure functions. CORE-01 requires them to live in one place the backtest can import.
Output: `strategy.py`, an extended `tests/test_strategy.py`, and `04`/`06` calling into the shared module instead of carrying their own copies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-shared-core-extraction-test-foundation/02-CONTEXT.md
@.planning/phases/02-shared-core-extraction-test-foundation/02-RESEARCH.md
@.planning/phases/02-shared-core-extraction-test-foundation/02-VALIDATION.md
@.planning/phases/02-shared-core-extraction-test-foundation/02-01-SUMMARY.md
@.planning/phases/02-shared-core-extraction-test-foundation/02-02-SUMMARY.md
</context>

<phase_ordering_note>
Binding for this plan, unchanged from 02-01: stage with explicit pathspecs only. `git add -A` and `git add .` are forbidden. `03_tren_modell.py` is never staged (D-09). `06_bot.py` carries the developer's pre-existing WIP — the disposition recorded in `02-01-SUMMARY.md` governs; restate it in this plan's commit body.
</phase_ordering_note>

<interfaces>
<!-- Verbatim current source this plan extracts from. Do not re-derive — copy the arithmetic exactly. -->

04_value_detector.py:207-225 (inline vig + value + EV, inside the per-game loop):
  impl_sann_hjemme = 1 / beste_hjemme_odds
  impl_sann_borte  = 1 / beste_borte_odds
  total = impl_sann_hjemme + impl_sann_borte
  impl_sann_hjemme /= total
  impl_sann_borte  /= total
  value_hjemme = modell_sann_hjemme - impl_sann_hjemme
  value_borte  = modell_sann_borte  - impl_sann_borte
  ev_hjemme = (modell_sann_hjemme * beste_hjemme_odds) - 1
  ev_borte  = (modell_sann_borte  * beste_borte_odds)  - 1

06_bot.py:202-222 (beregn_innsats — reads KELLY_FRAKSJON / MIN_INNSATS / MAX_INNSATS as module globals):
  def beregn_innsats(saldo, modell_prob, odds):
      b = odds - 1.0
      p = modell_prob
      q = 1.0 - p
      kelly = (b * p - q) / b
      if kelly <= 0:
          return 0.0
      innsats = saldo * kelly * KELLY_FRAKSJON
      innsats = max(MIN_INNSATS, min(MAX_INNSATS, innsats))
      return round(innsats, 2)

06_bot.py:264-274 (dedup key construction and check, inside plasser_bets):
  allerede_bettet = {
      (b["kamp"], b["bet"], b.get("kamp_dato") or b["dato"]) for b in bets
  }
  ...
  kamp_dato_rad = rad["KampDato"] if "KampDato" in rad and pd.notna(rad["KampDato"]) else str(date.today())
  nøkkel = (rad["Kamp"], rad["Bet"], kamp_dato_rad)
  if nøkkel in allerede_bettet:
      ... continue

06_bot.py call sites that must be updated because beregn_innsats gains parameters:
  line 281:  innsats = beregn_innsats(saldo, modell_prob, float(rad["Odds"]))
  (the only call site — verified by grep this planning session)

config.py (created by plan 02-02) exports: MIN_VALUE_TERSKEL, MIN_ODDS, MAX_ODDS,
KELLY_FRAKSJON, MAX_INNSATS, MIN_INNSATS, STARTKAPITAL
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create strategy.py with the extracted pure functions</name>
  <files>strategy.py</files>
  <read_first>
    - `04_value_detector.py` lines 204-232 (the inline vig/value/EV block being extracted, in its loop context)
    - `06_bot.py` lines 198-225 (`beregn_innsats` and its docstring)
    - `06_bot.py` lines 256-295 (`plasser_bets` — the dedup set comprehension, the `kamp_dato_rad` fallback, and the `saldo - innsats < MIN_INNSATS * 2` guard that stays in the orchestrator)
    - `.planning/phases/02-shared-core-extraction-test-foundation/02-RESEARCH.md` sections "Pattern 3: Pure-function `strategy.py`", "Anti-Patterns to Avoid", "Pitfall 1", "Pitfall 4"
    - `modell_utils.py` (the existing shared-module style to match: Norwegian module docstring, no type hints)
  </read_first>
  <action>
    Create `strategy.py` at repo root (flat module, D-01 — not a package). Norwegian module docstring stating it is the shared strategy core: pure functions with no I/O and no global state, imported today by `04_value_detector.py` and `06_bot.py` and later by the Phase 5 backtest, so both paths compute value and stake identically.

    `strategy.py` must import NOTHING from this project — not `config`, not `pandas`, not `numpy`. Config values arrive as parameters (RESEARCH.md Pattern 3 / assumption A2, adopted). This keeps every function testable with edge-case parameter values that differ from the live config, and keeps the module import-cycle-free. No type hints (project convention).

    Define exactly these five functions, with the arithmetic copied verbatim from the `<interfaces>` block — no reordering, no "cleaner" formula, no different rounding (Pitfall 1):

    1. `fjern_vigorish(odds_hjemme, odds_borte)` — computes `1/odds` for each side, sums them, divides each by the total, returns `(impl_hjemme, impl_borte)`. Docstring explains in Norwegian that bookmaker implied probabilities sum to >100% because of the margin, and that normalising is what makes "value" mean genuine model edge rather than manufactured margin.

    2. `beregn_value_og_ev(modell_prob, odds, impl_prob)` — returns `(value, ev)` where `value = modell_prob - impl_prob` and `ev = (modell_prob * odds) - 1`. Note in the docstring that `impl_prob` must be the vig-free probability from `fjern_vigorish`, not a raw `1/odds`.

    3. `beregn_innsats(saldo, modell_prob, odds, kelly_fraksjon, min_innsats, max_innsats)` — the exact body from the `<interfaces>` block, with the three former module globals replaced by the three new trailing parameters. Keep `if kelly <= 0: return 0.0`, keep `max(min_innsats, min(max_innsats, innsats))`, keep `round(innsats, 2)`. Carry over the existing Norwegian docstring text about half-Kelly reducing variance, and add one line noting that the min-clamp raises tiny positive-edge stakes UP to `min_innsats` — surprising but deliberate and preserved from the original.

    4. `finn_bet_nokkel(kamp, bet, kamp_dato)` — returns the 3-tuple `(kamp, bet, kamp_dato)`. Docstring: this is the dedup key that prevents betting the same physical game twice, keyed on the game's own date rather than the bot's run date.

    5. `bygg_bet_nokler(bets)` — takes a list of bet-record dicts and returns a set of keys, using `b.get("kamp_dato") or b["dato"]` as the date component. Docstring must state WHY the fallback exists: bet records written before the 2026-08-19 fix have no `kamp_dato` field, and falling back to the bot's run date is what keeps those legacy records participating in dedup at all.

    6. `er_duplikat(nokkel, eksisterende_nokler)` — returns `nokkel in eksisterende_nokler`.

    Do NOT move into `strategy.py`: the `saldo - innsats < MIN_INNSATS * 2` bankroll guard, the `kamp_dato_rad < str(date.today())` staleness skip, or anything that touches `date.today()`. Those are orchestration and stay in `06_bot.py` (RESEARCH.md Architectural Responsibility Map) — a function reading the wall clock is not pure and cannot be replayed by a backtest.

    Verify the module is genuinely dependency-free with `./venv/bin/python -c "import strategy"` and by grepping its import lines.

    Stage and commit: `git add strategy.py` then commit with message `refactor(02-03): opprett strategy.py med rene value/EV/Kelly-funksjoner (CORE-01)`.
  </action>
  <acceptance_criteria>
    - `./venv/bin/python -c "import strategy"` exits 0
    - `grep -c -E '^(import|from) ' strategy.py` returns 0 (module has zero imports — no config, no pandas)
    - `grep -c -E '^def (fjern_vigorish|beregn_value_og_ev|beregn_innsats|finn_bet_nokkel|bygg_bet_nokler|er_duplikat)' strategy.py` returns 6
    - `./venv/bin/python -c "import strategy; print(strategy.beregn_innsats(1000.0, 0.60, 2.00, 0.5, 20.0, 150.0))"` prints `100.0`
    - `./venv/bin/python -c "import strategy; print(strategy.beregn_innsats(1000.0, 0.40, 2.50, 0.5, 20.0, 150.0))"` prints `0.0`
    - `grep -c 'date.today\|datetime\|open(\|print(' strategy.py` returns 0 (no I/O, no clock — pure functions only)
    - `grep -c ': *[A-Za-z]*\]* *=\|-> ' strategy.py` returns 0 (no type hints, project convention)
    - `git log -1 --stat` shows exactly 1 file: `strategy.py`
  </acceptance_criteria>
  <verify>
    <automated>./venv/bin/python -c "import strategy; assert strategy.beregn_innsats(1000.0,0.60,2.00,0.5,20.0,150.0)==100.0; assert strategy.beregn_innsats(1000.0,0.40,2.50,0.5,20.0,150.0)==0.0; assert strategy.er_duplikat(('a','b','c'), {('a','b','c')}); print('strategy OK')"</automated>
  </verify>
  <done>`strategy.py` exists with six pure functions, imports nothing, reads no clock, does no I/O, and reproduces the reference stake values exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test the money math and the dedup key (CORE-03)</name>
  <files>tests/test_strategy.py</files>
  <behavior>
    - `test_fjern_vigorish_summerer_til_en`: `fjern_vigorish(1.90, 1.90)` returns two probabilities that each equal `pytest.approx(0.5)` and sum to `pytest.approx(1.0)` — the raw `1/1.90 + 1/1.90 = 1.0526` margin is removed
    - `test_fjern_vigorish_bevarer_rekkefolge`: `fjern_vigorish(1.50, 3.00)` returns `(pytest.approx(2/3, abs=1e-9), pytest.approx(1/3, abs=1e-9))` — the favourite (lower odds) gets the higher implied probability, not the other way round
    - `test_beregn_value_og_ev`: `beregn_value_og_ev(0.60, 2.00, 0.50)` returns `value == pytest.approx(0.10)` and `ev == pytest.approx(0.20)`
    - `test_beregn_value_og_ev_negativ_edge`: `beregn_value_og_ev(0.45, 2.00, 0.50)` returns `value == pytest.approx(-0.05)` and `ev == pytest.approx(-0.10)` — negative value and negative EV are reported, not clamped
    - `test_beregn_innsats_halv_kelly`: `beregn_innsats(1000.0, 0.60, 2.00, 0.5, 20.0, 150.0) == 100.0` — full Kelly would be 0.20 of bankroll (200 kr); half-Kelly must be exactly half of that
    - `test_beregn_innsats_null_edge_gir_null`: `beregn_innsats(1000.0, 0.40, 2.50, 0.5, 20.0, 150.0) == 0.0` — model probability exactly equals the fair implied probability, Kelly is 0, the `<= 0` branch fires
    - `test_beregn_innsats_negativ_edge_gir_null`: `beregn_innsats(1000.0, 0.30, 2.50, 0.5, 20.0, 150.0) == 0.0`
    - `test_beregn_innsats_klemmer_mot_maks`: `beregn_innsats(100000.0, 0.90, 1.60, 0.5, 20.0, 150.0) == 150.0`
    - `test_beregn_innsats_klemmer_opp_til_min`: `beregn_innsats(100.0, 0.55, 2.00, 0.5, 20.0, 150.0) == 20.0` — raw half-Kelly stake is 5.00 kr, the min-clamp raises it to 20.00; this is a real 4x stake increase on a thin edge and is pinned deliberately so the behaviour cannot change unnoticed
    - `test_bygg_bet_nokler_bruker_kamp_dato`: a record with both `dato` (bot run date) and `kamp_dato` (game date) yields a key containing `kamp_dato`, not `dato`
    - `test_bygg_bet_nokler_faller_tilbake_til_dato`: a legacy record with no `kamp_dato` key yields a key containing `dato` — pre-2026-08-19 records still participate in dedup
    - `test_dedup_fanger_samme_kamp_paa_ulik_kjoredato`: two records for the same game with the same `kamp_dato` but different `dato` collapse to ONE key, so the second run's bet is detected as a duplicate — this is the exact stale-row shape of the 2026-08-19 bug
    - `test_dedup_slipper_gjennom_ulik_kamp_dato`: same `kamp` and `bet` but a different `kamp_dato` is NOT a duplicate — a rematch between the same two teams is a legitimately new bet
    - `test_dedup_tom_historikk`: `er_duplikat(nokkel, bygg_bet_nokler([]))` is `False`
  </behavior>
  <read_first>
    - `strategy.py` (as written in Task 1)
    - `tests/test_strategy.py` (as written by plan 02-02 — two config tests already present; append, do not rewrite)
    - `06_bot.py` lines 256-280 (the real dedup code shape the tests must model, including the comment at 260-263 describing the 2026-08-19 bug)
    - `.planning/phases/02-shared-core-extraction-test-foundation/02-RESEARCH.md` "Pitfall 4: Dedup-key test passes but doesn't test the real risk"
  </read_first>
  <action>
    Append to the existing `tests/test_strategy.py` — do not delete or rewrite `test_config_values` / `test_config_har_ingen_hemmeligheter` from plan 02-02. Add `import pytest` and `from strategy import fjern_vigorish, beregn_value_og_ev, beregn_innsats, finn_bet_nokkel, bygg_bet_nokler, er_duplikat` at the top, and update the module docstring to say the file now covers `config.py` and `strategy.py`.

    Write the fourteen tests listed in `<behavior>` with exactly those function names. Test function names are English (pytest convention); the comment above each assertion explains in Norwegian WHY the case matters — specifically that these are the money-math functions CORE-03 exists to protect and that untested stake sizing has already cost this project real (paper) money.

    Use `pytest.approx` for every float comparison EXCEPT the `beregn_innsats` results — those are `round(..., 2)` outputs and compare exactly, which is itself part of the contract.

    Build dedup test records as plain dicts matching the real `bets.json` shape at minimum `{"kamp": ..., "bet": ..., "dato": ..., "kamp_dato": ...}`. For `test_dedup_fanger_samme_kamp_paa_ulik_kjoredato` use two records with `dato` `"2026-08-20"` and `"2026-08-21"` but the same `kamp_dato` `"2026-08-22"`, and assert `len(bygg_bet_nokler([r1, r2])) == 1`.

    Do not import `06_bot` in this test file — it is a numeric-prefixed module that executes top-level code and cannot be imported; the dedup logic is tested through `strategy.py` only.

    Run `./venv/bin/python -m pytest tests/test_strategy.py -v` and confirm 16 tests pass (2 from plan 02-02 + 14 new).

    Stage and commit: `git add tests/test_strategy.py` then commit with message `test(02-03): dekk beregn_innsats og dedup-nokkel med enhetstester (CORE-03)`.
  </action>
  <acceptance_criteria>
    - `./venv/bin/python -m pytest tests/test_strategy.py -v` exits 0 and reports `16 passed`
    - `./venv/bin/python -m pytest tests/test_strategy.py -k innsats -v` exits 0 and reports `5 passed`
    - `./venv/bin/python -m pytest tests/test_strategy.py -k dedup -v` exits 0 and reports at least `3 passed`
    - `grep -c 'def test_config_values' tests/test_strategy.py` returns 1 (plan 02-02's tests survived)
    - `grep -c 'import 06_bot\|import bot' tests/test_strategy.py` returns 0
    - `./venv/bin/python -m pytest -v` (whole suite) exits 0 with 17 tests passing
    - Deliberate-break check: temporarily change `strategy.beregn_innsats`'s `kelly_fraksjon` multiplication to a division, confirm `pytest tests/test_strategy.py -k innsats` FAILS, then revert and confirm it passes. Record both outcomes in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>./venv/bin/python -m pytest tests/test_strategy.py -v && ./venv/bin/python -m pytest -v</automated>
  </verify>
  <done>16 tests pass in `tests/test_strategy.py`, covering vig removal, value/EV, all five `beregn_innsats` branches, and four dedup cases including the legacy-record fallback and the stale-row shape; the deliberate-break check confirmed the stake tests actually fire.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire 04_value_detector.py and 06_bot.py onto strategy.py and delete the duplicates</name>
  <files>04_value_detector.py, 06_bot.py</files>
  <read_first>
    - `strategy.py` (the exact signatures written in Task 1)
    - `04_value_detector.py` lines 18-32 (import block) and lines 204-232 (the block being replaced)
    - `06_bot.py` lines 20-40 (import block, already importing from `config` after plan 02-02) and lines 198-225 (`beregn_innsats` definition to delete) and lines 256-295 (`plasser_bets` dedup + call site)
    - `.planning/phases/02-shared-core-extraction-test-foundation/02-02-SUMMARY.md` (confirm plan 02-02's `from config import ...` lines are already present, so this task adds a second import line rather than duplicating one)
    - `.planning/phases/02-shared-core-extraction-test-foundation/02-RESEARCH.md` "Pitfall 1: Extracting logic while accidentally changing a value or edge case"
  </read_first>
  <action>
    Replace the duplicated implementations with imports. This is extraction only — the numbers printed and written to `value_bets_idag.csv`, and the stakes written to `bets.json`, must be unchanged.

    In `04_value_detector.py`:
    1. Add `from strategy import fjern_vigorish, beregn_value_og_ev` to the import block (after the existing `from modell_utils import KalibrertModell` line, matching the established shared-module import position).
    2. Replace lines 207-225 (the comment block plus the nine arithmetic lines) with:
       `impl_sann_hjemme, impl_sann_borte = fjern_vigorish(beste_hjemme_odds, beste_borte_odds)`
       `value_hjemme, ev_hjemme = beregn_value_og_ev(modell_sann_hjemme, beste_hjemme_odds, impl_sann_hjemme)`
       `value_borte,  ev_borte  = beregn_value_og_ev(modell_sann_borte,  beste_borte_odds,  impl_sann_borte)`
       Keep a two-line Norwegian comment above it explaining that the vig-free normalisation and the value/EV formulas now live in `strategy.py` so the Phase 5 backtest computes them identically. Everything downstream (the `print` block at 227-232, the threshold checks at 238/252) keeps using the same variable names and needs no edit.

    In `06_bot.py`:
    3. Add `from strategy import beregn_innsats, finn_bet_nokkel, bygg_bet_nokler, er_duplikat` to the import block.
    4. Delete the entire `def beregn_innsats(...)` definition (lines 202-222) including its docstring. Leave the `# --- 2. Beregn innsats ... ---` section banner in place with a one-line Norwegian note that the function now lives in `strategy.py`, so the file's numbered-step narration stays intact.
    5. Update the single call site (line 281) to pass the config values explicitly:
       `innsats = beregn_innsats(saldo, modell_prob, float(rad["Odds"]), KELLY_FRAKSJON, MIN_INNSATS, MAX_INNSATS)`
       Those three names are already in scope from plan 02-02's `from config import ...` line.
    6. Replace the `allerede_bettet = {...}` set comprehension (lines 264-266) with `allerede_bettet = bygg_bet_nokler(bets)`, keeping the existing four-line Norwegian comment above it verbatim — it documents the 2026-08-19 bug and is the reason the key includes `kamp_dato`.
    7. Replace `nøkkel = (rad["Kamp"], rad["Bet"], kamp_dato_rad)` with `nøkkel = finn_bet_nokkel(rad["Kamp"], rad["Bet"], kamp_dato_rad)` and `if nøkkel in allerede_bettet:` with `if er_duplikat(nøkkel, allerede_bettet):`.
    8. Do NOT touch the `saldo - innsats < MIN_INNSATS * 2` guard, the `kamp_dato_rad < str(date.today())` staleness skip, the `nytt_bet` dict, or anything in `generer_dashboard`. Those are orchestration and stay put.

    Prove nothing broke: `./venv/bin/python -m py_compile 04_value_detector.py 06_bot.py` (do not import them — both execute top-level code, and `04` would fire a live Odds API call).

    Then prove the rewiring is behaviour-identical on `06_bot.py`'s live state: run
    `./venv/bin/python -c "import json, strategy; from config import KELLY_FRAKSJON, MIN_INNSATS, MAX_INNSATS; bets=json.load(open('bets.json')); print(len(strategy.bygg_bet_nokler(bets)), 'unike nokler av', len(bets), 'bets')"`
    and record both numbers in the SUMMARY. If `bets.json` does not exist, record that instead — do not create it.

    Stage and commit: `git add 04_value_detector.py 06_bot.py` then commit with message `refactor(02-03): 04 og 06 bruker strategy.py i stedet for egne kopier (CORE-01)`. State in the commit body which disposition the developer chose in plan 01 Task 1, since `06_bot.py`'s pre-existing WIP rides along.
  </action>
  <acceptance_criteria>
    - `./venv/bin/python -m py_compile 04_value_detector.py 06_bot.py` exits 0
    - `grep -c '^def beregn_innsats' 06_bot.py` returns 0
    - `grep -c 'from strategy import' 06_bot.py` returns 1 and `grep -c 'from strategy import' 04_value_detector.py` returns 1
    - `grep -v '^ *#' 04_value_detector.py | grep -c 'impl_sann_hjemme = 1 / \|impl_sann_hjemme /= total'` returns 0
    - `grep -v '^ *#' 04_value_detector.py | grep -c -E 'ev_(hjemme|borte) *= *\(modell_sann'` returns 0
    - `grep -v '^ *#' 06_bot.py | grep -c 'b\.get("kamp_dato") or b\["dato"\]'` returns 0 (the set comprehension is gone; that fallback now lives only in `strategy.py`)
    - `grep -c 'bygg_bet_nokler(bets)' 06_bot.py` returns 1
    - `grep -c 'KELLY_FRAKSJON, MIN_INNSATS, MAX_INNSATS' 06_bot.py` returns 1 (the updated call site)
    - `./venv/bin/python -m pytest -v` still exits 0 with 17 tests passing
    - `git status --short 03_tren_modell.py` still shows ` M` (untouched, D-09)
  </acceptance_criteria>
  <verify>
    <automated>./venv/bin/python -m py_compile 04_value_detector.py 06_bot.py && ./venv/bin/python -m pytest -v && grep -c '^def beregn_innsats' 06_bot.py; git log -1 --stat</automated>
  </verify>
  <done>`04_value_detector.py` and `06_bot.py` import their value/EV/stake/dedup logic from `strategy.py`, contain zero copies of it, still byte-compile, and the full test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Odds API JSON → `fjern_vigorish` | `beste_hjemme_odds`/`beste_borte_odds` originate in untrusted third-party JSON and are now passed across a new module boundary into shared arithmetic. |
| `bets.json` on disk → `bygg_bet_nokler` | Persisted bet records (including legacy records missing `kamp_dato`) are read from disk and fed into the dedup key builder. |
| working tree → git history (public repo) | `06_bot.py` still carries the developer's ~1009 uninspected WIP lines. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering (financial logic) | `beregn_innsats` gaining three new parameters — a call site passing them in the wrong order (e.g. `min` and `max` swapped) would silently invert stake clamping | mitigate | Single call site, updated explicitly by name order in Task 3 step 5 and grep-asserted; `test_beregn_innsats_klemmer_mot_maks` and `test_beregn_innsats_klemmer_opp_til_min` pin both clamps to exact values, so a swap fails the suite |
| T-02-07 | Tampering (financial logic) | dedup regression — losing the `b.get("kamp_dato") or b["dato"]` legacy fallback would drop pre-2026-08-19 records out of the dedup set and re-bet already-settled games | mitigate | `test_bygg_bet_nokler_faller_tilbake_til_dato` and `test_dedup_fanger_samme_kamp_paa_ulik_kjoredato` model the exact historical bug; Task 3 verifies the live `bets.json` still produces the expected key count |
| T-02-08 | Denial of Service (division by zero) | `fjern_vigorish` with `odds == 0`, or `beregn_innsats` with `odds == 1.0` (`b = 0` → ZeroDivisionError) | accept | Identical to current behaviour — `04_value_detector.py:204` already `continue`s when either best odds is 0, and `MIN_ODDS = 1.50` gates the stake path. Adding validation inside the extracted functions would be a behaviour change, which Pitfall 1 forbids in an extraction phase. Recorded as residual; revisit if Phase 5's backtest feeds unfiltered archived odds. |
| T-02-05 | Tampering | silent change to money-critical arithmetic during extraction | mitigate | Arithmetic copied verbatim from the `<interfaces>` block; grep gates assert the old inline copies are deleted rather than left to drift; 14 unit tests pin every branch |
| T-02-01 | Information Disclosure | `git add 06_bot.py` sweeping uninspected WIP into a public commit | mitigate | Disposition decided by the developer in plan 01 Task 1 and restated in this plan's commit body; explicit-pathspec staging only |
</threat_model>

<verification>
- `./venv/bin/python -m pytest -v` exits 0 with 17 tests passing
- `./venv/bin/python -m py_compile strategy.py 04_value_detector.py 06_bot.py` exits 0
- `grep -c -E '^(import|from) ' strategy.py` returns 0 — the shared module has no project dependencies
- `git log --oneline -3` shows three commits touching only the files in `files_modified`
- `git status --short 03_tren_modell.py` still shows ` M`
</verification>

<success_criteria>
- Vig removal, value/EV and half-Kelly stake sizing exist exactly once in the repo, in `strategy.py`
- The bet-dedup key logic exists exactly once, in `strategy.py`, with the legacy-record fallback preserved
- `beregn_innsats` produces identical stakes to the pre-extraction implementation for every tested branch
- `strategy.py` is importable by a future backtest with zero project dependencies
- 17 tests pass; the deliberate-break check proved the stake tests are not vacuous
</success_criteria>

<output>
Create `.planning/phases/02-shared-core-extraction-test-foundation/02-03-SUMMARY.md` when done. Record: the final test count, the deliberate-break check result from Task 2, the `bets.json` unique-key-vs-record count from Task 3, and the exact `beregn_innsats` signature (plan 04/05/06 and Phase 5 all call it).
</output>
