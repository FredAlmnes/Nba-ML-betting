---
phase: 01-repo-hygiene-config-remediation
plan: 04
type: execute
wave: 4
depends_on: ["01-02", "01-03"]
files_modified: [KALIBRERING_RAPPORT.md, ENDRINGER_SUMMARY.txt]
autonomous: true
requirements: [HYG-03]
user_setup: []

must_haves:
  truths:
    - "A reader of KALIBRERING_RAPPORT.md learns within the first screen that its proposed values were never applied to running code"
    - "A reader of ENDRINGER_SUMMARY.txt learns the same thing"
    - "Both documents name the values that are actually running (MIN_VALUE_TERSKEL 0.05, MAX_ODDS 4.00) so there is exactly one unambiguous source of truth for live config"
    - "Both documents state that validated replacement thresholds will come from the Phase 5 walk-forward backtest, not from these documents"
    - "The running thresholds in 04_value_detector.py are unchanged by this plan"
    - "Both documents are tracked in git, so the supersession note is visible to a fresh clone and not just to this machine"
  artifacts:
    - path: "KALIBRERING_RAPPORT.md"
      provides: "Historical calibration report, explicitly marked as never-deployed"
      contains: "SUPERSEDED"
    - path: "ENDRINGER_SUMMARY.txt"
      provides: "Historical change summary, explicitly marked as never-deployed"
      contains: "SUPERSEDED"
  key_links:
    - from: "KALIBRERING_RAPPORT.md"
      to: "04_value_detector.py"
      via: "supersession note naming the actual live threshold values"
      pattern: "MIN_VALUE_TERSKEL"
---

<objective>
Close HYG-03: reconcile the two documents that describe a threshold/calibration fix which was drafted on 2026-04-06 and never applied, so they stop implying their proposed values are live.

Purpose: `KALIBRERING_RAPPORT.md` says "Status: Implementert" and `ENDRINGER_SUMMARY.txt` says "STATUS: IMPLEMENTERT OG KLAR FOR BRUK", but verified this session: `04_value_detector.py` still runs `MIN_VALUE_TERSKEL = 0.05` and `MAX_ODDS = 4.00`, not the documented 0.20 / 2.50 — and the documented `KALIBRERING_FAKTOR` / `MIN_SIKKERHET` do not exist in the code at all. Both files are also currently untracked, so none of this is recorded in git.
Output: an unambiguous supersession note at the top of both documents, both committed. Zero code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-repo-hygiene-config-remediation/01-CONTEXT.md
@.planning/phases/01-repo-hygiene-config-remediation/01-RESEARCH.md
@KALIBRERING_RAPPORT.md
@ENDRINGER_SUMMARY.txt
</context>

<location_decision>
CONTEXT.md leaves it to Claude's discretion whether these two files stay at the repo root or move into a `docs/` or `.planning/` archive. **Decision: keep them at the repo root.** There is no `docs/` directory in this repo, the other narrative docs (`KOMME_I_GANG.md` and friends) live at root, and moving an untracked file while simultaneously adding it to git makes its first commit needlessly hard to read. The supersession note — not the location — is what closes HYG-03.
</location_decision>

<scope_guard>
**This plan changes zero lines of Python.** D-05 explicitly forbids applying the documented values (`MIN_VALUE_TERSKEL` 0.20, `MAX_ODDS` 2.50) to `04_value_detector.py`. Those numbers were never backtest-validated; applying them now would repeat the exact unvalidated-threshold-change pattern this project exists to stop. Validated thresholds come out of Phase 5. An acceptance criterion below asserts the running values are still 0.05 / 4.00.
</scope_guard>

<staging_rule>
**NEVER run `git add -A` or `git add .` in this phase.** Stage by explicit filename only. `debug_kamp.py` stays untracked (D-09) and the scratch artifacts stay ignored (plan 02).
</staging_rule>

<tasks>

<task type="auto">
  <name>Task 1: Add an unambiguous supersession note to both historical documents</name>
  <files>KALIBRERING_RAPPORT.md, ENDRINGER_SUMMARY.txt</files>
  <read_first>
    - `KALIBRERING_RAPPORT.md` in full — note the title line, the `**Dato:** April 6, 2026` and `**Status:** Implementert` lines, and the specific values it proposes; the note must name those values accurately
    - `ENDRINGER_SUMMARY.txt` in full — note that it is plain text with `====` rule lines and `STATUS: IMPLEMENTERT OG KLAR FOR BRUK`, not markdown; the note must match that plain-text style rather than using markdown blockquote syntax
    - `04_value_detector.py` lines 27-34 — read the live values directly rather than trusting this plan's text; the note must quote what is actually there
    - `.planning/phases/01-repo-hygiene-config-remediation/01-RESEARCH.md` — "Superseded-doc header note (example wording)" and Pitfall 4
  </read_first>
  <action>
    Insert a supersession note at the very top of each file, above all existing content, so neither document can be read without seeing it first.

    Per RESEARCH.md Pitfall 4 the note must state three facts plainly, with no hedging — not "may be outdated", not "might not be current":
    1. These specific proposed values were never applied to `04_value_detector.py`. Name them: `MIN_VALUE_TERSKEL` 0.05 to 0.20, `MAX_ODDS` 4.00 to 2.50, plus the calibration factor and minimum-confidence filter that were never added to the code at all.
    2. The values actually running are `MIN_VALUE_TERSKEL = 0.05` and `MAX_ODDS = 4.00` — read them out of `04_value_detector.py` and quote what you find there.
    3. Validated replacement thresholds will come from the Phase 5 walk-forward backtest, not from this document. Do not apply these numbers to running code.

    Also neutralise the two now-false status claims in the bodies: `**Status:** Implementert` in `KALIBRERING_RAPPORT.md` and `STATUS: IMPLEMENTERT OG KLAR FOR BRUK` in `ENDRINGER_SUMMARY.txt`. Change each to state clearly that the work was drafted but never deployed, for example `Status: IKKE DEPLOYERT — se merknaden øverst`. Leaving those lines intact would mean each body contradicts its own header two screens down.

    Include the marker word `SUPERSEDED` in each note so it is greppable. Wording beyond the three required facts is your discretion per CONTEXT.md. Write it in Norwegian to match both documents. Change nothing else in either file — the analysis content is kept deliberately as historical context (CONTEXT.md: do not delete them, they are useful history).
  </action>
  <verify>
    <automated>for f in KALIBRERING_RAPPORT.md ENDRINGER_SUMMARY.txt; do head -20 "$f" | grep -q 'SUPERSEDED' || echo "MANGLER SUPERSEDED: $f"; head -20 "$f" | grep -q '0.05' || echo "MANGLER LIVE-VERDI 0.05: $f"; head -20 "$f" | grep -q '4.00' || echo "MANGLER LIVE-VERDI 4.00: $f"; head -20 "$f" | grep -qi 'fase 5' || echo "MANGLER FASE 5-HENVISNING: $f"; done; grep -q 'Status:\*\* Implementert' KALIBRERING_RAPPORT.md && echo "GAMMEL STATUS STAAR IGJEN: KALIBRERING_RAPPORT.md"; grep -q 'STATUS: IMPLEMENTERT OG KLAR FOR BRUK' ENDRINGER_SUMMARY.txt && echo "GAMMEL STATUS STAAR IGJEN: ENDRINGER_SUMMARY.txt"; grep -q 'MIN_VALUE_TERSKEL = 0.05' 04_value_detector.py && grep -q 'MAX_ODDS = 4.00' 04_value_detector.py && echo "KODE UENDRET OK"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints no `MANGLER` lines: both files carry `SUPERSEDED`, both live values (`0.05`, `4.00`), and a "fase 5" reference within their first 20 lines
    - The verify command prints no `GAMMEL STATUS STAAR IGJEN` lines: neither `**Status:** Implementert` nor `STATUS: IMPLEMENTERT OG KLAR FOR BRUK` survives
    - The verify command prints `KODE UENDRET OK`: `04_value_detector.py` still has `MIN_VALUE_TERSKEL = 0.05` and `MAX_ODDS = 4.00` (D-05 guard)
    - `git status --porcelain 04_value_detector.py 05_skadefilter.py 06_bot.py 03_tren_modell.py` shows nothing changed by this task
    - `head -3` of each file shows note text, not the original title-only opening
  </acceptance_criteria>
  <done>Both documents open with a plain, unhedged note stating the proposed values were never deployed, naming the values actually running, and pointing at Phase 5 for validated replacements; their internal "implemented" status claims are corrected; no Python changed.</done>
</task>

<task type="auto">
  <name>Task 2: Commit both documents so the supersession is recorded in git</name>
  <files>KALIBRERING_RAPPORT.md, ENDRINGER_SUMMARY.txt</files>
  <read_first>
    - Run before staging: `git status --short` — both files are currently untracked (`??`), so this is their first commit
    - `.planning/phases/01-repo-hygiene-config-remediation/01-RESEARCH.md` "Pattern 3" — explicit-filename staging mechanics
  </read_first>
  <action>
    Stage exactly two paths by explicit filename with `git add KALIBRERING_RAPPORT.md ENDRINGER_SUMMARY.txt`. Run `git status --short` and confirm the staged set is exactly `A  ENDRINGER_SUMMARY.txt` and `A  KALIBRERING_RAPPORT.md` and nothing else; unstage anything unexpected with `git restore --staged` before committing. Commit with message `docs(01-04): mark KALIBRERING_RAPPORT and ENDRINGER_SUMMARY as superseded, never deployed`.

    Committing them rather than leaving them untracked is what makes HYG-03's "exactly one source of truth" durable — an untracked file carrying a correction note is invisible to a fresh clone, which would leave the drift unfixed for everyone but this machine.

    Do not run `git add -A`, `git add .`, or `git commit -a`.
  </action>
  <verify>
    <automated>git ls-files --error-unmatch KALIBRERING_RAPPORT.md ENDRINGER_SUMMARY.txt && git show --stat --name-only --format= HEAD | sort | tr '\n' ' ' && echo "" && git show HEAD:KALIBRERING_RAPPORT.md | head -20 | grep -q 'SUPERSEDED' && git show HEAD:ENDRINGER_SUMMARY.txt | head -20 | grep -q 'SUPERSEDED' && git status --porcelain | grep -q 'debug_kamp.py' && echo "COMMIT OK"</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files --error-unmatch` exits 0 for both files (both now tracked)
    - `git show --stat --name-only --format= HEAD` lists exactly `ENDRINGER_SUMMARY.txt` and `KALIBRERING_RAPPORT.md` and no other path
    - The committed blobs (`git show HEAD:<file>`) both contain `SUPERSEDED` within their first 20 lines — the note is in git, not just on disk
    - `debug_kamp.py` still appears as untracked in `git status --porcelain` (D-09 respected)
    - Verify command prints `COMMIT OK`
  </acceptance_criteria>
  <done>Both historical reports are tracked in git with their supersession notes, committed as a two-file commit that touched nothing else.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git history (public repo) | This plan commits two previously-untracked files. Anything staged becomes permanently public. |
| documentation → future operator decisions | A doc that misstates live config is an integrity failure: it causes a future reader (human or agent) to act on values that are not running. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Tampering (integrity of operational truth) | `KALIBRERING_RAPPORT.md` / `ENDRINGER_SUMMARY.txt` asserting "Implementert" for a change that never shipped | mitigate | Unhedged supersession note at the top of both files naming the actual live values, plus correction of the in-body status lines; verified by grepping the first 20 lines of each committed blob |
| T-01-12 | Information Disclosure | Committing two previously-untracked root files that were never reviewed for secrets | mitigate | Both files were read in full during research and this plan's `read_first`; they contain thresholds and prose only, no credentials. Two-path explicit staging plus a `git show --stat` assertion that nothing else entered the commit |
| T-01-13 | Elevation of Privilege (scope creep into strategy behaviour) | Temptation to "just apply" the documented 0.20 / 2.50 thresholds while editing the docs that describe them | mitigate | Explicit `scope_guard` block; acceptance criterion greps `04_value_detector.py` for `MIN_VALUE_TERSKEL = 0.05` and `MAX_ODDS = 4.00` and fails the task if either changed |
</threat_model>

<verification>
- Both files carry `SUPERSEDED`, both live values, and a Phase 5 reference in their first 20 lines
- Neither stale "IMPLEMENTERT" status line survives
- `04_value_detector.py` thresholds unchanged (0.05 / 4.00)
- Both files tracked; HEAD commit contains exactly those two paths
</verification>

<success_criteria>
HYG-03 is closed: there is exactly one unambiguous source of truth for what config is live. The two historical reports are preserved but can no longer be mistaken for a description of running behaviour, they name the real running values, and they point at the Phase 5 backtest as the source of validated replacements. No strategy behaviour changed.
</success_criteria>

<output>
Create `.planning/phases/01-repo-hygiene-config-remediation/01-04-SUMMARY.md` when done. Include the final supersession note text for both files verbatim as HYG-03 evidence, and confirmation that the live thresholds were left at 0.05 / 4.00.
</output>
