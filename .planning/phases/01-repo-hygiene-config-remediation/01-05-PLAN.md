---
phase: 01-repo-hygiene-config-remediation
plan: 05
type: execute
wave: 5
depends_on: ["01-02", "01-03", "01-04"]
files_modified: []
autonomous: false
requirements: [HYG-01, HYG-02, HYG-03]
user_setup:
  - service: the-odds-api.com
    why: "The key committed in c058a1a is public. Only the account owner can regenerate it, and until they do, the exposed value still works."
    env_vars:
      - name: ODDS_API_NOKKEL
        source: "the-odds-api.com dashboard -> regenerate API key, then paste into a local .env (copied from .env.example)"
    dashboard_config:
      - task: "Regenerate / revoke the existing API key so the publicly-committed value stops working"
        location: "the-odds-api.com account dashboard"

must_haves:
  truths:
    - "The API key value that is public in commit c058a1a no longer works against The Odds API"
    - "A local, git-ignored .env exists containing a rotated key, and 04_value_detector.py reads it successfully"
    - "The key in .env is verifiably not the leaked value"
    - "All three phase requirements (HYG-01, HYG-02, HYG-03) pass their 01-VALIDATION.md checks in one final battery run"
    - "The developer has confirmed the supersession notes read unambiguously, since that is a judgement call no grep can make"
    - "The two deferred items (git history scrubbing, scratch-artifact deletion) are recorded as open decisions rather than silently dropped"
  artifacts: []
  key_links:
    - from: ".env"
      to: "04_value_detector.py"
      via: "python-dotenv load_dotenv() into os.environ"
      pattern: "ODDS_API_NOKKEL"
---

<objective>
Close the human half of HYG-01 (key rotation — the only action in this phase Claude cannot perform) and run the full Phase 1 verification battery from `01-VALIDATION.md` as a single phase gate.

Purpose: plan 03 stopped the key from being hardcoded going forward, but it did not neutralise the value that is already public in commit `c058a1a`. Until the developer regenerates the key at the-odds-api.com, the exposure is live. D-03 rules out scrubbing git history, which makes rotation the entire mitigation — it is not optional cleanup.
Output: rotated key in a local `.env`, all four VALIDATION.md checks green, deferred items recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-repo-hygiene-config-remediation/01-CONTEXT.md
@.planning/phases/01-repo-hygiene-config-remediation/01-VALIDATION.md
@.planning/phases/01-repo-hygiene-config-remediation/01-03-SUMMARY.md
@.planning/phases/01-repo-hygiene-config-remediation/01-04-SUMMARY.md
@.env.example
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Rotate the exposed Odds API key and create the local .env</name>
  <files></files>
  <read_first>
    - `.env.example` (created in plan 03) — the exact variable name and file format to copy
    - `.planning/phases/01-repo-hygiene-config-remediation/01-03-SUMMARY.md` — confirms the code now reads `ODDS_API_NOKKEL`
    - `.gitignore` line 7 (`.env`) — the reason a real key can safely live in `.env` on disk
  </read_first>
  <action>
    Present the rotation instructions below to the developer and stop. Do NOT create `.env`, do NOT write any key value, and do NOT attempt to rotate anything through the API — this is a human-only account action per D-02. After the developer reports back, verify their work with the checks in the acceptance criteria rather than taking "done" at face value.
  </action>
  <what-built>
    Plan 03 removed the hardcoded key from `04_value_detector.py` and made the script read `ODDS_API_NOKKEL` from a git-ignored `.env`, failing fast with exit code 1 if it is missing. `.env.example` documents the variable name. No `.env` has been created and no real key has been written by any agent — deliberately, per D-02 and RESEARCH.md Pitfall 1.

    What remains is the part only you can do: the key currently sitting in public commit `c058a1a` (value beginning `afc4f6...`) still authenticates against The Odds API. Removing it from the working tree does not revoke it. Because D-03 defers git-history scrubbing, rotation is the complete mitigation for that exposure.
  </what-built>
  <how-to-verify>
    1. Log in at https://the-odds-api.com and open your account dashboard.
    2. Regenerate / revoke the existing API key. Confirm the dashboard now shows a different key than the one starting `afc4f6`.
    3. In the repo root run `cp .env.example .env`.
    4. Edit `.env` and replace the placeholder so the line reads `ODDS_API_NOKKEL=<your new key>`. Keep it on one line with no quotes and no trailing spaces.
    5. Run `git status --short` and confirm `.env` does NOT appear (it is ignored by `.gitignore` line 7). If it appears as `??`, stop and tell the executor — do not commit it.
    6. Tell the executor "rotated" when done, or "skip rotation" if you deliberately want to defer it (the executor will then record HYG-01 as only partially closed, with the live exposure still open).
  </how-to-verify>
  <acceptance_criteria>
    - Developer confirmed the key was regenerated on the-odds-api.com, so the value in commit `c058a1a` is dead
    - `.env` exists at the repo root and contains an `ODDS_API_NOKKEL=` line
    - `git status --short` does not list `.env` in any state
    - `git check-ignore -v .env` reports a match against `.gitignore` line 7
    - The developer's answer ("rotated" or "skip rotation") is recorded verbatim in the SUMMARY; if "skip rotation", the SUMMARY must state plainly that HYG-01 is not fully closed and the exposure remains live
  </acceptance_criteria>
  <verify>
    <automated>test -f .env && echo ".env finnes" && git check-ignore -v .env && test -z "$(git status --porcelain .env)" && echo ".env er ignorert og ikke staged" && ! grep -q 'afc4f647c551e760f59f837769f5a3a1' .env && echo "ikke den lekkede noekkelen"</automated>
  </verify>
  <done>The developer has regenerated the Odds API key at the-odds-api.com and placed it in a local, git-ignored `.env`, verified not to be the leaked literal — or has explicitly chosen "skip rotation", in which case the SUMMARY records that HYG-01 remains only partially closed.</done>
  <resume-signal>Type "rotated" or "skip rotation"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run the full Phase 1 verification battery</name>
  <files></files>
  <read_first>
    - `.planning/phases/01-repo-hygiene-config-remediation/01-VALIDATION.md` — the "Per-Task Verification Map" defines the four checks that constitute this phase's gate (task IDs 01-01-01, 01-01-02, 01-02-01, 01-03-01, mapping to HYG-01 fail-fast, HYG-01 static, HYG-02 clone import, HYG-03 doc review)
    - `.planning/phases/01-repo-hygiene-config-remediation/01-03-SUMMARY.md` and `01-04-SUMMARY.md` — the evidence recorded per plan, to cross-check against a fresh run
  </read_first>
  <action>
    Re-run every VALIDATION.md check in one pass, from the repo root, and capture the output verbatim for the SUMMARY. Do not rely on the per-plan evidence — the point of this task is a single end-state proof after all four earlier plans have landed.

    The rotation check is the new one: load `.env` through `python-dotenv` and assert the value present is neither empty nor equal to the leaked literal `afc4f647c551e760f59f837769f5a3a1`. That is what distinguishes "a key exists" from "the key was actually rotated". If the developer answered "skip rotation" in Task 1, expect and record a `MANGLER` result here rather than treating it as a hard failure — but the SUMMARY must then state HYG-01 is only partially closed.

    The fail-fast check must temporarily move `.env` aside so the missing-key path is genuinely exercised; the verify command below does this and restores it. Confirm `.env` is back in place afterwards.

    Do not run the full `04_value_detector.py` pipeline here — it spends an API credit and makes a long series of NBA stats calls. The live end-to-end run is the developer's job in Task 3.
  </action>
  <verify>
    <automated>echo "== HYG-01 static ==" && test "$(grep -v '^[[:space:]]*#' 04_value_detector.py | grep -c 'afc4f647c551e760f59f837769f5a3a1')" = "0" && ! grep -qE '^API_NØKKEL = "' 04_value_detector.py && grep -q 'os.environ.get("ODDS_API_NOKKEL")' 04_value_detector.py && echo "OK" && echo "== HYG-01 rotasjon ==" && ./venv/bin/python -c "from dotenv import load_dotenv; import os; load_dotenv(); v=os.environ.get('ODDS_API_NOKKEL',''); print('MANGLER' if not v else ('FARE: GAMMEL LEKKET NOKKEL' if v=='afc4f647c551e760f59f837769f5a3a1' else 'ROTERT OK'))"</automated>
    <automated>echo "== HYG-01 fail-fast ==" && { if [ -f .env ]; then mv .env .env.verifybak; fi; OUT=$(env -u ODDS_API_NOKKEL ./venv/bin/python 04_value_detector.py 2>&1); RC=$?; if [ -f .env.verifybak ]; then mv .env.verifybak .env; fi; echo "$OUT" | head -5; echo "exit=$RC"; echo "$OUT" | grep -q 'ODDS_API_NOKKEL' && test "$RC" = "1" && ! echo "$OUT" | grep -q 'Laster inn trent modell' && echo "FAILFAST OK"; } && test -f .env && echo ".env gjenopprettet"</automated>
    <automated>echo "== HYG-02 fresh clone ==" && REPO=$(pwd) && TMP=$(mktemp -d) && git clone -q "$REPO" "$TMP/klone" && cd "$TMP/klone" && "$REPO/venv/bin/python" -c "from modell_utils import KalibrertModell; print('IMPORT OK')" && test ! -f .env && echo "ingen .env i klonen (korrekt)" && cd "$REPO" && rm -rf "$TMP"</automated>
    <automated>echo "== HYG-03 dokumenter ==" && for f in KALIBRERING_RAPPORT.md ENDRINGER_SUMMARY.txt; do git show "HEAD:$f" | head -20 | grep -q 'SUPERSEDED' && echo "$f OK" || echo "$f MANGLER"; done && grep -q 'MIN_VALUE_TERSKEL = 0.05' 04_value_detector.py && grep -q 'MAX_ODDS = 4.00' 04_value_detector.py && echo "terskler uendret OK" && echo "== scratch ==" && (git status --porcelain | grep -E '_test\.bin|_wheels|_pip_tmp|_linux_pkgs|test_write\.tmp' || echo "ingen scratch i git status")</automated>
  </verify>
  <acceptance_criteria>
    - HYG-01 static block prints `OK`: zero occurrences of the leaked literal in non-comment lines, no `API_NØKKEL = "` literal assignment, `os.environ.get("ODDS_API_NOKKEL")` present
    - HYG-01 rotation block prints `ROTERT OK` (or `MANGLER` only if the developer chose "skip rotation" in Task 1). Printing `FARE: GAMMEL LEKKET NOKKEL` is a hard failure — the developer put the leaked key back into `.env`
    - HYG-01 fail-fast block prints `FAILFAST OK` and `.env gjenopprettet`: exit code exactly 1, message names `ODDS_API_NOKKEL`, no `Laster inn trent modell` in the output, and `.env` restored afterwards
    - HYG-02 block prints `IMPORT OK` and `ingen .env i klonen (korrekt)` from inside a temp clone of HEAD
    - HYG-03 block prints `KALIBRERING_RAPPORT.md OK`, `ENDRINGER_SUMMARY.txt OK` and `terskler uendret OK`
    - Scratch block prints `ingen scratch i git status`
    - All captured output is pasted verbatim into the SUMMARY as the phase gate evidence
  </acceptance_criteria>
  <done>Every check in `01-VALIDATION.md`'s Per-Task Verification Map has been re-run against the final state of the phase, with rotation additionally proven, and the output is recorded as evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase acceptance — live run and judgement calls</name>
  <files></files>
  <read_first>
    - `KALIBRERING_RAPPORT.md` first 20 lines
    - `ENDRINGER_SUMMARY.txt` first 20 lines
    - `KOMME_I_GANG.md` "Steg 4" section
    - The Task 2 verification output
  </read_first>
  <action>
    Summarise the Task 2 verification output for the developer, then present the four acceptance items below and stop. If the developer reports that a supersession note reads ambiguously, rewrite the note in the affected file, re-run the HYG-03 verification block from Task 2, amend or add a `docs(01-05)` commit staging only that file by explicit filename, and re-present for approval. Do not close the phase on unaddressed feedback.
  </action>
  <what-built>
    All three requirements are mechanically green: the key comes from `ODDS_API_NOKKEL` with a fail-fast guard (HYG-01), `modell_utils.py` is tracked and a temp clone imports it (HYG-02), and both historical reports carry supersession notes and are committed (HYG-03). Two things are left that no grep can decide.
  </what-built>
  <how-to-verify>
    1. **Live run.** With your rotated `.env` in place, run `./venv/bin/python 04_value_detector.py`. Confirm it gets past the config section and reaches "Henter dagens NBA-odds..." — that proves the new key authenticates. (This spends one API credit. If The Odds API returns a non-200, the script exits 1 and prints the status code, which means the key or account state is wrong, not the code.)
    2. **Supersession clarity (HYG-03's manual check).** Read the note now at the top of `KALIBRERING_RAPPORT.md` and `ENDRINGER_SUMMARY.txt`. Confirm each states plainly — not hedged — that (a) the proposed values were never applied, (b) the values actually running are 0.05 / 4.00, and (c) validated replacements come from the Phase 5 backtest. If either reads as "this might be outdated", say so and it will be rewritten.
    3. **Setup guide.** Read "Steg 4" in `KOMME_I_GANG.md`. Confirm it now tells you to use `.env` and never mentions pasting a key into `04_value_detector.py`.
    4. **Deferred items.** Acknowledge that two things remain open by design and will be recorded in the SUMMARY and STATE.md rather than done here: git-history scrubbing of the old key (D-03 — destructive force-push on a public repo, needs its own decision) and deletion of the scratch artifacts if you chose `ignore-only` in plan 01 (D-08).
  </how-to-verify>
  <acceptance_criteria>
    - Developer confirmed the live run reached the odds-fetch step with the rotated key, or explicitly recorded why it did not
    - Developer confirmed both supersession notes are unambiguous (this satisfies the "manual review" row for HYG-03 in `01-VALIDATION.md`)
    - Developer confirmed `KOMME_I_GANG.md` Steg 4 teaches the `.env` convention
    - Both deferred items are written into the SUMMARY, and STATE.md's Deferred Items / Blockers section is updated to carry the git-history-scrubbing decision forward
    - Any "rewrite it" feedback on the notes is applied and re-verified before the phase is called done
  </acceptance_criteria>
  <verify>
    <automated>head -20 KALIBRERING_RAPPORT.md && echo "=====" && head -20 ENDRINGER_SUMMARY.txt && echo "=====" && grep -n -A6 'Steg 4' KOMME_I_GANG.md</automated>
  </verify>
  <done>The developer has confirmed the live run authenticates with the rotated key, that both supersession notes read unambiguously, and that the setup guide teaches the `.env` convention; the two deferred items are recorded in the SUMMARY and STATE.md.</done>
  <resume-signal>Type "approved" or describe what needs rewording</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the-odds-api.com account → local `.env` | The rotated secret crosses from the provider dashboard into a local git-ignored file. This is the only place a real key exists after this phase. |
| public git history (`c058a1a`) → the internet | The old key is already across this boundary and cannot be recalled without rewriting history. |
| local process → api.the-odds-api.com | Live credential use during the acceptance run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | Information Disclosure | The key published in commit `c058a1a` remains valid | mitigate | Blocking human rotation at the-odds-api.com (Task 1); Task 2 asserts programmatically that the value in `.env` is not the leaked literal, so "rotation" cannot be silently faked by reusing the old key |
| T-01-15 | Information Disclosure | Leaked key still readable in git history after rotation | accept | D-03 defers history scrubbing: it needs a destructive force-push on a repo that has been public and may have been cloned or forked. Rotation makes the historical value worthless. Recorded as an explicit open decision in STATE.md rather than silently dropped |
| T-01-16 | Information Disclosure | Newly created `.env` accidentally committed | mitigate | `.gitignore` line 7 predates this phase; Task 1 acceptance requires `git status --short` to show no `.env` and `git check-ignore -v .env` to match; Task 2 additionally asserts a fresh clone of HEAD contains no `.env` |
| T-01-17 | Spoofing | Developer pastes the old leaked key back into `.env` (for example by copying from the old source line) | mitigate | Task 2's rotation check compares the loaded value against the known leaked literal and reports `FARE: GAMMEL LEKKET NOKKEL`, which is defined as a hard failure |
</threat_model>

<verification>
- `.env` exists, is ignored by git, and contains a value that is not the leaked literal
- All four `01-VALIDATION.md` checks re-run green against the final phase state
- Live run reaches the odds-fetch step with the rotated key
- Developer approved the clarity of both supersession notes
- Deferred items recorded in SUMMARY and STATE.md
</verification>

<success_criteria>
Phase 1 is complete: a fresh clone can be configured from `.env.example` and `KOMME_I_GANG.md` and runs without an `ImportError` (HYG-02) and without a secret in source (HYG-01), the previously-exposed key is dead (HYG-01 rotation), and there is exactly one unambiguous source of truth for the live strategy config (HYG-03) — with the two deliberately deferred items carried forward as explicit open decisions rather than forgotten.
</success_criteria>

<output>
Create `.planning/phases/01-repo-hygiene-config-remediation/01-05-SUMMARY.md` when done. Include: the rotation answer verbatim, the complete Task 2 verification output, the developer's acceptance of the supersession notes, and the two deferred items. Update `.planning/STATE.md` so the git-history-scrubbing decision appears under Blockers/Concerns or Deferred Items.
</output>
